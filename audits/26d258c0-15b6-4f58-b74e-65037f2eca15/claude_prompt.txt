You are a senior UI/UX designer and expert frontend developer.

I am giving you a screenshot of a UI screen and a detailed UX audit report.
Your task is to produce a complete, self-contained HTML redesign (version 2)
that resolves every issue in the audit while preserving the original visual identity.

CRITICAL RULE — READ BEFORE ANYTHING ELSE:
Fixing an issue takes absolute priority over preserving layout or placement.
If a fix requires moving an element, restructuring a section, hiding a component,
or changing how content is presented — DO IT. The audit findings are non-negotiable.
Preservation means keeping the brand identity (colors, typography, component style).
It does NOT mean keeping broken elements in the same broken position.

====================================================================
MANDATORY FIX CHECKLIST  —  VERIFY EACH BEFORE WRITING A SINGLE LINE OF HTML
====================================================================

Before you write the HTML, mentally confirm that your design addresses EVERY item
in this list. If your HTML does not contain a concrete, visible solution for a
High or Medium issue, your response is incomplete — revise it until it does.
Some fixes require structural changes (relocating, collapsing, or restructuring
UI elements). Make those structural changes — do not style over them.

HIGH PRIORITY  (must be resolved — zero exceptions):
  NOTE: 'fixing' means the issue is GONE from the redesign — not styled differently.
  If the fix says to move, collapse, or restructure something, do exactly that.

  [01] Icon-only header buttons lack accessible labels
       Problem:          The header contains icon-only buttons (search, profile) with no visible text labels or apparent aria-labels. Screen reader users cannot determine the purpose of these interactive elements, and sighted users may also be uncertain of their function.
       What to implement: Add visually hidden aria-label attributes to each icon button (e.g., aria-label='Search' and aria-label='User profile'). Consider adding visible tooltips on hover/focus to also aid sighted users.
       Affects:          Header bar — search icon and user profile icon on the right side
       WCAG:             1.1.1 Level A

  [02] Insufficient color contrast on secondary text and metric labels
       Problem:          Secondary text elements such as metric sub-labels and chart axis labels appear in light gray on a white or near-white background, likely failing the 4.5:1 minimum contrast ratio for normal-sized text. This makes the information difficult to read for users with low vision or in bright ambient lighting.
       What to implement: Audit all text elements using a contrast checker tool and increase the color values to achieve at least 4.5:1 for normal text and 3:1 for large text. Replace light grays (#999 or lighter) with at least #595959 on white backgrounds.
       Affects:          Main content area — metric cards and chart labels
       WCAG:             1.4.3 Level AA

  [03] No visible focus indicators for interactive elements
       Problem:          There are no visible focus indicators on any interactive elements in the screenshot. Keyboard-only users cannot determine which element currently has focus, making the entire interface effectively unusable without a mouse.
       What to implement: Implement a visible focus ring (e.g., 2px solid blue outline with 2px offset) on all interactive elements. Ensure the focus indicator has at least a 3:1 contrast ratio against adjacent colors per WCAG 2.4.13 recommendations.
       Affects:          Sidebar navigation items, header buttons, and tab controls
       WCAG:             2.4.7 Level AA

  [04] Sidebar navigation lacks clear active-state indicator
       Problem:          The sidebar navigation does not provide a sufficiently distinct visual indicator for the currently active/selected page. Users cannot quickly determine their current location within the application, increasing disorientation risk.
       What to implement: Add a prominent active-state style to the current sidebar item using a combination of background color fill, left border accent, and bold text weight. Ensure the active state differs from hover state to prevent confusion.
       Affects:          Left sidebar navigation panel

MEDIUM PRIORITY  (must be resolved — no exceptions):
  NOTE: Same rule — fix means the issue does not exist in the output HTML.

  [05] Charts may rely on color alone to convey data series
       Problem:          The dashboard charts appear to use color as the sole differentiator between data series. Users with color vision deficiencies (affecting ~8% of males) may be unable to distinguish between data categories, rendering the charts uninterpretable.
       What to implement: Add pattern fills, direct labels, or distinct line styles (dashed, dotted) to each data series in charts. Include an accessible legend with both color swatches and pattern indicators. Consider offering a data table alternative.
       Affects:          Main content area — chart/graph visualizations
       WCAG:             1.4.1 Level A

  [06] Metric cards lack contextual information and comparison data
       Problem:          The dashboard metric cards display raw numbers without sufficient context such as trend direction, percentage change, or time-period comparison. Users cannot quickly assess whether a metric is good, bad, or stable without performing mental calculations.
       What to implement: Add trend indicators (up/down arrows), percentage change values, and micro-sparklines to each metric card. Use color coding (green for positive, red for negative) supplemented with icons to convey trend at a glance.
       Affects:          Main content area — top metric/KPI cards

  [07] Tab navigation provides weak affordance for interactivity
       Problem:          The tabbed interface at the top of the main content area uses minimal visual styling that fails to clearly communicate that tabs are interactive and clickable. The active tab is not sufficiently differentiated from inactive tabs.
       What to implement: Style the active tab with an underline or filled background that clearly contrasts with inactive tabs. Add a hover state with cursor change and subtle background shift to reinforce clickability. Ensure tab roles are correctly set with ARIA tablist/tab/tabpanel.
       Affects:          Main content area — tab controls above the dashboard content

  [08] Header button purpose is unclear without label
       Problem:          A button in the header next to the logo has no clear label or recognizable icon pattern, leaving users uncertain of its function. This could be a menu toggle, notification bell, or other control — but its purpose is ambiguous.
       What to implement: Add a descriptive text label next to or below the icon, or replace with a universally recognized icon (e.g., hamburger menu ≡). If space is constrained, add a tooltip and aria-label.
       Affects:          Header bar — unlabeled button element near the logo

LOW PRIORITY  (implement where possible):
  [09] Footer appears to add visual noise without clear utility — Evaluate whether the footer content (links, copyright, version info) can be moved to a settings or about page. If retained, minimize its height and use subtle styling so it does not compete with primary dashboard content.
  [10] Touch target sizes may be too small for sidebar navigation items — Increase the vertical padding on each sidebar navigation item to ensure a minimum 44×44px touch target area (recommended) or at minimum 24×24px. Add sufficient spacing between items to prevent accidental activation of adjacent links.

====================================================================
STEP 1 — LAYOUT & COMPOSITION  (reason through this BEFORE anything else)
====================================================================

Layout and content distribution are non-negotiable design decisions.
Do not assume the original layout is correct — reason from first principles.
Ask yourself: does each element sit in the right zone for its purpose?
If not, move it. The composition must serve the user's primary goal.

1. VISUAL HIERARCHY
   The most important action or information must be visually dominant.
   Use size, weight, contrast, and position to establish a clear reading order.
   Secondary content must be visually subordinate — smaller, lighter, lower.
   (Malewicz — Designing User Interfaces: contrast creates hierarchy)
   (Refactoring UI: use size and weight to communicate importance, not just color)

2. F-PATTERN & READING FLOW
   Users scan left-to-right, top-to-bottom in an F-shape on content-heavy screens.
   Place primary actions and key labels at the start of scan lines.
   Never interrupt the scan path with secondary, promotional, or personalisation content.
   (Nielsen Norman Group — F-Pattern reading behavior)

3. PROXIMITY & GROUPING (Gestalt)
   Related elements must be spatially close. Unrelated elements need clear separation.
   If two elements answer different user questions, they belong in different zones.
   Never mix contextual content with navigational content in the same region.
   (Psych 101 — Gestalt: Law of Proximity, Law of Similarity)

4. CONTENT PRIORITISATION
   Identify the user's single primary goal on this screen.
   Every element that does not serve that goal directly must be secondary in
   visual weight and position. Upsell, personalisation, and promotional content
   must never compete with or interrupt the primary task flow.
   (Don't Make Me Think — Krug: do not make users think about what to do next)
   (Psychology of Design — Hick's Law: fewer competing choices = faster decisions)

5. SPATIAL ZONES
   Divide the screen into logical zones: navigation, primary content, secondary
   context, actions. Each zone has exactly one job.
   If an element is in the wrong zone, move it to the correct zone or defer it
   to a modal or drawer. The layout must match the user's mental model of the task.
   (Universal Principles of Design — Progressive Disclosure)

6. SIGNAL-TO-NOISE
   Every element must earn its place. If an element does not directly help the
   user complete their current task, reduce its visual weight or remove it from
   the primary viewport. Clutter is a UX failure, not a neutral default.
   (Universal Principles of Design — Signal-to-Noise Ratio)

COMPOSITION RULE: If the original layout violates these principles — with or
without an explicit audit finding — correct it. Do not replicate bad composition
because it existed in the screenshot. A version 2 must be compositionally sound.

APPLY THESE COMPOSITION DECISIONS TO RESOLVE THE FOLLOWING AUDIT ISSUES:
Every High and Medium issue in the MANDATORY FIX CHECKLIST above must map to a
specific, visible structural decision in your layout. Before moving to Step 2,
trace each issue to a layout zone, spacing decision, or hierarchy change.
If the fix requires restructuring — restructure. If it requires removing an element
from the primary viewport — remove it. Composition fixes are not optional.

====================================================================
STEP 2 — VISUAL IDENTITY  (preserve brand, not broken placement)
====================================================================

Once composition is decided, preserve these brand elements exactly:
  - Colors: every background, text, brand, accent, and surface hex value
  - Typography: font families, sizes, weights, line-heights, letter-spacing
  - Spacing scale: the relative rhythm of padding/margin (not the positions)
  - Shapes: border-radius, border styles, shadows, elevation
  - Icons: style, weight, and size (placement may change if composition requires)
  - Components: card designs, button styles, badge styles, input styles

NOTE: Layout and element positioning are governed by Step 1, not this list.
Preserving the brand does NOT mean preserving where broken things sit.
The output must feel like a version 2 — same brand, better UX.

====================================================================
FULL AUDIT REPORT  —  Dashboard - Overview Metrics
====================================================================
Overall Score:        5.8/10  —  Fair
Accessibility Score:  4.5/10  (WCAG 2.2 AA)
Issues Found:         10  (4 High · 4 Medium · 2 Low)

EXECUTIVE SUMMARY
The dashboard presents key metrics and charts but suffers from significant accessibility gaps including potential contrast issues, icon-only buttons without labels, and unclear visual hierarchy. The sidebar navigation and header lack sufficient affordances, and the information density could benefit from progressive disclosure to reduce cognitive load.

ALL ISSUES  (full detail for reference):

[01] HIGH  —  Icon-only header buttons lack accessible labels
     Location:  Header bar — search icon and user profile icon on the right side
     Problem:   The header contains icon-only buttons (search, profile) with no visible text labels or apparent aria-labels. Screen reader users cannot determine the purpose of these interactive elements, and sighted users may also be uncertain of their function.
     Fix:       Add visually hidden aria-label attributes to each icon button (e.g., aria-label='Search' and aria-label='User profile'). Consider adding visible tooltips on hover/focus to also aid sighted users.
     Source:    WCAG 2.2 — 1.1.1 Non-text Content (A): functional non-text content must have a text alternative
     WCAG:      1.1.1  Level A

[02] HIGH  —  Insufficient color contrast on secondary text and metric labels
     Location:  Main content area — metric cards and chart labels
     Problem:   Secondary text elements such as metric sub-labels and chart axis labels appear in light gray on a white or near-white background, likely failing the 4.5:1 minimum contrast ratio for normal-sized text. This makes the information difficult to read for users with low vision or in bright ambient lighting.
     Fix:       Audit all text elements using a contrast checker tool and increase the color values to achieve at least 4.5:1 for normal text and 3:1 for large text. Replace light grays (#999 or lighter) with at least #595959 on white backgrounds.
     Source:    WCAG 2.2 — 1.4.3 Contrast Minimum (AA): normal text requires 4.5:1 contrast ratio
     WCAG:      1.4.3  Level AA

[03] HIGH  —  No visible focus indicators for interactive elements
     Location:  Sidebar navigation items, header buttons, and tab controls
     Problem:   There are no visible focus indicators on any interactive elements in the screenshot. Keyboard-only users cannot determine which element currently has focus, making the entire interface effectively unusable without a mouse.
     Fix:       Implement a visible focus ring (e.g., 2px solid blue outline with 2px offset) on all interactive elements. Ensure the focus indicator has at least a 3:1 contrast ratio against adjacent colors per WCAG 2.4.13 recommendations.
     Source:    WCAG 2.2 — 2.4.7 Focus Visible (AA): keyboard focus indicator must be visible
     WCAG:      2.4.7  Level AA

[04] HIGH  —  Sidebar navigation lacks clear active-state indicator
     Location:  Left sidebar navigation panel
     Problem:   The sidebar navigation does not provide a sufficiently distinct visual indicator for the currently active/selected page. Users cannot quickly determine their current location within the application, increasing disorientation risk.
     Fix:       Add a prominent active-state style to the current sidebar item using a combination of background color fill, left border accent, and bold text weight. Ensure the active state differs from hover state to prevent confusion.
     Source:    Universal Principles of Design — Visibility: system status and available actions must be visible

[05] MEDIUM  —  Charts may rely on color alone to convey data series
     Location:  Main content area — chart/graph visualizations
     Problem:   The dashboard charts appear to use color as the sole differentiator between data series. Users with color vision deficiencies (affecting ~8% of males) may be unable to distinguish between data categories, rendering the charts uninterpretable.
     Fix:       Add pattern fills, direct labels, or distinct line styles (dashed, dotted) to each data series in charts. Include an accessible legend with both color swatches and pattern indicators. Consider offering a data table alternative.
     Source:    WCAG 2.2 — 1.4.1 Use of Color (A): color must not be the only visual means of conveying information
     WCAG:      1.4.1  Level A

[06] MEDIUM  —  Metric cards lack contextual information and comparison data
     Location:  Main content area — top metric/KPI cards
     Problem:   The dashboard metric cards display raw numbers without sufficient context such as trend direction, percentage change, or time-period comparison. Users cannot quickly assess whether a metric is good, bad, or stable without performing mental calculations.
     Fix:       Add trend indicators (up/down arrows), percentage change values, and micro-sparklines to each metric card. Use color coding (green for positive, red for negative) supplemented with icons to convey trend at a glance.
     Source:    Psychology of Design — Cognitive Load Theory: minimize extraneous cognitive load to improve comprehension

[07] MEDIUM  —  Tab navigation provides weak affordance for interactivity
     Location:  Main content area — tab controls above the dashboard content
     Problem:   The tabbed interface at the top of the main content area uses minimal visual styling that fails to clearly communicate that tabs are interactive and clickable. The active tab is not sufficiently differentiated from inactive tabs.
     Fix:       Style the active tab with an underline or filled background that clearly contrasts with inactive tabs. Add a hover state with cursor change and subtle background shift to reinforce clickability. Ensure tab roles are correctly set with ARIA tablist/tab/tabpanel.
     Source:    The Design of Everyday Things (Norman) — Signifier: visible cues must indicate where to act

[08] MEDIUM  —  Header button purpose is unclear without label
     Location:  Header bar — unlabeled button element near the logo
     Problem:   A button in the header next to the logo has no clear label or recognizable icon pattern, leaving users uncertain of its function. This could be a menu toggle, notification bell, or other control — but its purpose is ambiguous.
     Fix:       Add a descriptive text label next to or below the icon, or replace with a universally recognized icon (e.g., hamburger menu ≡). If space is constrained, add a tooltip and aria-label.
     Source:    Don't Make Me Think (Krug) — Billboard Design 101: users scan, not read; make elements self-evident

[09] LOW  —  Footer appears to add visual noise without clear utility
     Location:  Footer bar at the bottom of the screen
     Problem:   A footer bar is present at the bottom of the dashboard interface but its content and purpose are not immediately clear. In a SaaS dashboard context, persistent footers consume valuable vertical space and rarely provide actionable information.
     Fix:       Evaluate whether the footer content (links, copyright, version info) can be moved to a settings or about page. If retained, minimize its height and use subtle styling so it does not compete with primary dashboard content.
     Source:    Universal Principles of Design — Signal-to-Noise Ratio: maximise relevant information, minimise clutter

[10] LOW  —  Touch target sizes may be too small for sidebar navigation items
     Location:  Left sidebar — navigation menu items
     Problem:   Sidebar navigation items appear to have compact vertical spacing that may result in touch targets smaller than 24×24 CSS pixels. This makes accurate selection difficult on touch devices and for users with motor impairments.
     Fix:       Increase the vertical padding on each sidebar navigation item to ensure a minimum 44×44px touch target area (recommended) or at minimum 24×24px. Add sufficient spacing between items to prevent accidental activation of adjacent links.
     Source:    WCAG 2.2 — 2.5.8 Target Size Minimum (AA): interactive targets must be at least 24×24 CSS pixels
     WCAG:      2.5.8  Level AA

====================================================================
SELF-CHECK — RUN THIS BEFORE OUTPUTTING THE HTML
====================================================================

For each item below, verify the SPECIFIC FIX is concretely implemented —
not just acknowledged, not just styled differently. The fix must be visible.

  [ ] [01] Icon-only header buttons lack accessible labels
       Required fix: Add visually hidden aria-label attributes to each icon button (e.g., aria-label='Search' and aria-label='User profile'). Consider adding visible tooltips on hover/focus to also aid sighted users.
       Location:     Header bar — search icon and user profile icon on the right side
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [02] Insufficient color contrast on secondary text and metric labels
       Required fix: Audit all text elements using a contrast checker tool and increase the color values to achieve at least 4.5:1 for normal text and 3:1 for large text. Replace light grays (#999 or lighter) with at least #595959 on white backgrounds.
       Location:     Main content area — metric cards and chart labels
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [03] No visible focus indicators for interactive elements
       Required fix: Implement a visible focus ring (e.g., 2px solid blue outline with 2px offset) on all interactive elements. Ensure the focus indicator has at least a 3:1 contrast ratio against adjacent colors per WCAG 2.4.13 recommendations.
       Location:     Sidebar navigation items, header buttons, and tab controls
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [04] Sidebar navigation lacks clear active-state indicator
       Required fix: Add a prominent active-state style to the current sidebar item using a combination of background color fill, left border accent, and bold text weight. Ensure the active state differs from hover state to prevent confusion.
       Location:     Left sidebar navigation panel
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [05] Charts may rely on color alone to convey data series
       Required fix: Add pattern fills, direct labels, or distinct line styles (dashed, dotted) to each data series in charts. Include an accessible legend with both color swatches and pattern indicators. Consider offering a data table alternative.
       Location:     Main content area — chart/graph visualizations
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [06] Metric cards lack contextual information and comparison data
       Required fix: Add trend indicators (up/down arrows), percentage change values, and micro-sparklines to each metric card. Use color coding (green for positive, red for negative) supplemented with icons to convey trend at a glance.
       Location:     Main content area — top metric/KPI cards
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [07] Tab navigation provides weak affordance for interactivity
       Required fix: Style the active tab with an underline or filled background that clearly contrasts with inactive tabs. Add a hover state with cursor change and subtle background shift to reinforce clickability. Ensure tab roles are correctly set with ARIA tablist/tab/tabpanel.
       Location:     Main content area — tab controls above the dashboard content
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [08] Header button purpose is unclear without label
       Required fix: Add a descriptive text label next to or below the icon, or replace with a universally recognized icon (e.g., hamburger menu ≡). If space is constrained, add a tooltip and aria-label.
       Location:     Header bar — unlabeled button element near the logo
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

If any checkbox above cannot be ticked YES, revise your HTML before outputting it.

====================================================================
OUTPUT REQUIREMENTS
====================================================================

- Output a single, complete, self-contained HTML file.
- All CSS must be in a <style> block in the <head> — no external stylesheets.
- No CDN links, no JavaScript frameworks, no external images.
  Use inline SVG for icons. Use data URIs only if absolutely necessary.
- Preserve all informational content (text, labels, data). Do not silently drop
  information the user needs. However: if a fix requires relocating, collapsing,
  or restructuring a UI element, you MUST do so — placing it in a better position
  is not removing it. The audit fix always wins over original placement.
- Do NOT add new features, sections, or content not present in the original.
- JavaScript is allowed only for interactions clearly present in the original.
  Keep it minimal and inline.
- The file must open in a browser as a fully working static page.
- Start your response with <!DOCTYPE html> and end the HTML with </html>.
- Do NOT reference Mobbin or any third-party design gallery. Use Saasfactor if needed.