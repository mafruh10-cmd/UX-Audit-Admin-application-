You are a senior UI/UX designer and expert frontend developer.

I am giving you a screenshot of a UI screen and a detailed UX audit report.
Your task is to produce a complete, self-contained HTML redesign (version 2)
that resolves every issue in the audit while preserving the original visual identity.

CRITICAL RULE — READ BEFORE ANYTHING ELSE:
Fixing an issue takes absolute priority over preserving layout or placement.
If a fix requires moving an element, restructuring a section, hiding a component,
or changing how content is presented — DO IT. The audit findings are non-negotiable.
Preservation means keeping the brand identity (colors, typography, component style).
It does NOT mean keeping broken elements in the same broken position.

====================================================================
MANDATORY FIX CHECKLIST  —  VERIFY EACH BEFORE WRITING A SINGLE LINE OF HTML
====================================================================

Before you write the HTML, mentally confirm that your design addresses EVERY item
in this list. If your HTML does not contain a concrete, visible solution for a
High or Medium issue, your response is incomplete — revise it until it does.
Some fixes require structural changes (relocating, collapsing, or restructuring
UI elements). Make those structural changes — do not style over them.

HIGH PRIORITY  (must be resolved — zero exceptions):
  NOTE: 'fixing' means the issue is GONE from the redesign — not styled differently.
  If the fix says to move, collapse, or restructure something, do exactly that.

  [01] Post content has insufficient text contrast
       Problem:          The gray text inside the 'Recent Posts' cards has a very low contrast ratio against the light-gray background. This makes the content difficult to read for all users and may be impossible for users with visual impairments or color vision deficiencies.
       What to implement: Darken the body text within the cards to meet the WCAG AA minimum contrast ratio of 4.5:1. For example, use a color like #595959 or darker on the #F7F7F7 background to ensure readability.
       Affects:          Your Recent Posts cards
       WCAG:             1.4.3 Level AA

  [02] Icon-only buttons lack visible text labels
       Problem:          The buttons in the right-side vertical bar use only icons to represent actions like 'Compose', 'Notifications', and 'Search'. Without visible text labels, the meaning of these icons can be ambiguous, increasing cognitive load for users.
       What to implement: Add a text label to each icon or, at minimum, provide a tooltip with the action's name that appears immediately on hover or focus. For a more inclusive design, consider a setting to permanently show labels.
       Affects:          Right-side vertical action bar

  [03] Button border is nearly invisible
       Problem:          The border of the 'Ask a Question' button is extremely faint, providing insufficient contrast with the white background. This makes its boundary difficult to perceive, causing it to blend in and not look like a clickable element.
       What to implement: Change the border color of the button to a darker gray or the brand's blue to achieve a 3:1 contrast ratio against the background. This will make the button's boundary clearly visible and communicate its interactivity.
       Affects:          'Ask a Question' button
       WCAG:             1.4.11 Level AA

MEDIUM PRIORITY  (must be resolved — no exceptions):
  NOTE: Same rule — fix means the issue does not exist in the output HTML.

  [04] Low-contrast label for sorting control
       Problem:          The 'Sort by' text label for the dropdown menu in the 'Your Recent Posts' section is a very light gray. This low contrast makes the label difficult to read and diminishes the discoverability of the sorting feature.
       What to implement: Increase the contrast of the 'Sort by' label by using a darker shade of gray or black text to meet the 4.5:1 contrast ratio required by WCAG. This will ensure the label is legible to all users.
       Affects:          'Sort by' label
       WCAG:             1.4.3 Level AA

  [05] Content overlap and ambiguous signifier in post card
       Problem:          In the second post card, hashtags are rendered on top of a gray box containing the letter 'A', making the text almost completely unreadable. Furthermore, the meaning of the gray 'A' box is unclear, acting as a poor signifier for what it represents (likely a placeholder or item type).
       What to implement: Fix the layering issue so text does not overlap with other elements. Replace the ambiguous 'A' with a more descriptive icon (e.g., an image icon if it's a media placeholder) and ensure it has a tooltip explaining its purpose.
       Affects:          'Recent Posts' card #2

  [06] Phone number is not a clickable link
       Problem:          The support phone number is displayed as plain text, not a clickable `tel:` link. This forces users, particularly on mobile devices, to manually copy and paste the number to make a call, creating unnecessary friction.
       What to implement: Wrap the phone number in an anchor tag with an `href="tel:1-866-878-3231"` attribute. This will make the number clickable, allowing users on any device to initiate a call with a single tap or click.
       Affects:          'Need Help?' section

LOW PRIORITY  (implement where possible):
  [07] Section titles may lack semantic heading structure — Ensure that these visual section titles are marked up with the appropriate heading level (e.g., <h2> for 'Looking for Something Else?' and <h3> for the sub-sections like 'Your Account').

====================================================================
STEP 1 — LAYOUT & COMPOSITION  (reason through this BEFORE anything else)
====================================================================

Layout and content distribution are non-negotiable design decisions.
Do not assume the original layout is correct — reason from first principles.
Ask yourself: does each element sit in the right zone for its purpose?
If not, move it. The composition must serve the user's primary goal.

1. VISUAL HIERARCHY
   The most important action or information must be visually dominant.
   Use size, weight, contrast, and position to establish a clear reading order.
   Secondary content must be visually subordinate — smaller, lighter, lower.
   (Malewicz — Designing User Interfaces: contrast creates hierarchy)
   (Refactoring UI: use size and weight to communicate importance, not just color)

2. F-PATTERN & READING FLOW
   Users scan left-to-right, top-to-bottom in an F-shape on content-heavy screens.
   Place primary actions and key labels at the start of scan lines.
   Never interrupt the scan path with secondary, promotional, or personalisation content.
   (Nielsen Norman Group — F-Pattern reading behavior)

3. PROXIMITY & GROUPING (Gestalt)
   Related elements must be spatially close. Unrelated elements need clear separation.
   If two elements answer different user questions, they belong in different zones.
   Never mix contextual content with navigational content in the same region.
   (Psych 101 — Gestalt: Law of Proximity, Law of Similarity)

4. CONTENT PRIORITISATION
   Identify the user's single primary goal on this screen.
   Every element that does not serve that goal directly must be secondary in
   visual weight and position. Upsell, personalisation, and promotional content
   must never compete with or interrupt the primary task flow.
   (Don't Make Me Think — Krug: do not make users think about what to do next)
   (Psychology of Design — Hick's Law: fewer competing choices = faster decisions)

5. SPATIAL ZONES
   Divide the screen into logical zones: navigation, primary content, secondary
   context, actions. Each zone has exactly one job.
   If an element is in the wrong zone, move it to the correct zone or defer it
   to a modal or drawer. The layout must match the user's mental model of the task.
   (Universal Principles of Design — Progressive Disclosure)

6. SIGNAL-TO-NOISE
   Every element must earn its place. If an element does not directly help the
   user complete their current task, reduce its visual weight or remove it from
   the primary viewport. Clutter is a UX failure, not a neutral default.
   (Universal Principles of Design — Signal-to-Noise Ratio)

COMPOSITION RULE: If the original layout violates these principles — with or
without an explicit audit finding — correct it. Do not replicate bad composition
because it existed in the screenshot. A version 2 must be compositionally sound.

APPLY THESE COMPOSITION DECISIONS TO RESOLVE THE FOLLOWING AUDIT ISSUES:
Every High and Medium issue in the MANDATORY FIX CHECKLIST above must map to a
specific, visible structural decision in your layout. Before moving to Step 2,
trace each issue to a layout zone, spacing decision, or hierarchy change.
If the fix requires restructuring — restructure. If it requires removing an element
from the primary viewport — remove it. Composition fixes are not optional.

====================================================================
STEP 2 — VISUAL IDENTITY  (preserve brand, not broken placement)
====================================================================

Once composition is decided, preserve these brand elements exactly:
  - Colors: every background, text, brand, accent, and surface hex value
  - Typography: font families, sizes, weights, line-heights, letter-spacing
  - Spacing scale: the relative rhythm of padding/margin (not the positions)
  - Shapes: border-radius, border styles, shadows, elevation
  - Icons: style, weight, and size (placement may change if composition requires)
  - Components: card designs, button styles, badge styles, input styles

NOTE: Layout and element positioning are governed by Step 1, not this list.
Preserving the brand does NOT mean preserving where broken things sit.
The output must feel like a version 2 — same brand, better UX.

====================================================================
FULL AUDIT REPORT  —  Dashboard
====================================================================
Overall Score:        7.2/10  —  Good
Accessibility Score:  6.0/10  (WCAG 2.2 AA)
Issues Found:         7  (3 High · 3 Medium · 1 Low)

EXECUTIVE SUMMARY
The Sprout Social dashboard provides a comprehensive overview of social media activity but is hampered by several UX and accessibility issues. While the information architecture is generally clear, significant accessibility violations related to color contrast and interactive elements detract from the user experience. Addressing these issues would greatly improve readability, reduce user friction, and ensure the platform is usable for a wider audience.

ALL ISSUES  (full detail for reference):

[01] HIGH  —  Post content has insufficient text contrast
     Location:  Your Recent Posts cards
     Problem:   The gray text inside the 'Recent Posts' cards has a very low contrast ratio against the light-gray background. This makes the content difficult to read for all users and may be impossible for users with visual impairments or color vision deficiencies.
     Fix:       Darken the body text within the cards to meet the WCAG AA minimum contrast ratio of 4.5:1. For example, use a color like #595959 or darker on the #F7F7F7 background to ensure readability.
     Source:    WCAG 2.2 — 1.4.3 Contrast Minimum (AA): normal text requires 4.5:1 contrast ratio
     WCAG:      1.4.3  Level AA

[02] HIGH  —  Icon-only buttons lack visible text labels
     Location:  Right-side vertical action bar
     Problem:   The buttons in the right-side vertical bar use only icons to represent actions like 'Compose', 'Notifications', and 'Search'. Without visible text labels, the meaning of these icons can be ambiguous, increasing cognitive load for users.
     Fix:       Add a text label to each icon or, at minimum, provide a tooltip with the action's name that appears immediately on hover or focus. For a more inclusive design, consider a setting to permanently show labels.
     Source:    The Design of Everyday Things (Norman) — Signifier: visible cues must indicate where to act

[03] HIGH  —  Button border is nearly invisible
     Location:  'Ask a Question' button
     Problem:   The border of the 'Ask a Question' button is extremely faint, providing insufficient contrast with the white background. This makes its boundary difficult to perceive, causing it to blend in and not look like a clickable element.
     Fix:       Change the border color of the button to a darker gray or the brand's blue to achieve a 3:1 contrast ratio against the background. This will make the button's boundary clearly visible and communicate its interactivity.
     Source:    WCAG 2.2 — 1.4.11 Non-text Contrast (AA)
     WCAG:      1.4.11  Level AA

[04] MEDIUM  —  Low-contrast label for sorting control
     Location:  'Sort by' label
     Problem:   The 'Sort by' text label for the dropdown menu in the 'Your Recent Posts' section is a very light gray. This low contrast makes the label difficult to read and diminishes the discoverability of the sorting feature.
     Fix:       Increase the contrast of the 'Sort by' label by using a darker shade of gray or black text to meet the 4.5:1 contrast ratio required by WCAG. This will ensure the label is legible to all users.
     Source:    WCAG 2.2 — 1.4.3 Contrast Minimum (AA)
     WCAG:      1.4.3  Level AA

[05] MEDIUM  —  Content overlap and ambiguous signifier in post card
     Location:  'Recent Posts' card #2
     Problem:   In the second post card, hashtags are rendered on top of a gray box containing the letter 'A', making the text almost completely unreadable. Furthermore, the meaning of the gray 'A' box is unclear, acting as a poor signifier for what it represents (likely a placeholder or item type).
     Fix:       Fix the layering issue so text does not overlap with other elements. Replace the ambiguous 'A' with a more descriptive icon (e.g., an image icon if it's a media placeholder) and ensure it has a tooltip explaining its purpose.
     Source:    Psych 101 (Kleinman) — Gestalt: Law of Proximity

[06] MEDIUM  —  Phone number is not a clickable link
     Location:  'Need Help?' section
     Problem:   The support phone number is displayed as plain text, not a clickable `tel:` link. This forces users, particularly on mobile devices, to manually copy and paste the number to make a call, creating unnecessary friction.
     Fix:       Wrap the phone number in an anchor tag with an `href="tel:1-866-878-3231"` attribute. This will make the number clickable, allowing users on any device to initiate a call with a single tap or click.
     Source:    The Design of Everyday Things (Norman) — Gulf of Execution: user cannot figure out how to do what they want

[07] LOW  —  Section titles may lack semantic heading structure
     Location:  'Looking for Something Else?' section
     Problem:   The titles 'Your Account', 'Connect Profile', etc., are styled to look like headings but may not be implemented with semantic HTML heading tags (e.g., <h2>, <h3>). If they are not true headings, screen reader users cannot use them to navigate the page structure.
     Fix:       Ensure that these visual section titles are marked up with the appropriate heading level (e.g., <h2> for 'Looking for Something Else?' and <h3> for the sub-sections like 'Your Account').
     Source:    WCAG 2.2 — 1.3.1 Info and Relationships (A)
     WCAG:      1.3.1  Level A

====================================================================
SELF-CHECK — RUN THIS BEFORE OUTPUTTING THE HTML
====================================================================

For each item below, verify the SPECIFIC FIX is concretely implemented —
not just acknowledged, not just styled differently. The fix must be visible.

  [ ] [01] Post content has insufficient text contrast
       Required fix: Darken the body text within the cards to meet the WCAG AA minimum contrast ratio of 4.5:1. For example, use a color like #595959 or darker on the #F7F7F7 background to ensure readability.
       Location:     Your Recent Posts cards
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [02] Icon-only buttons lack visible text labels
       Required fix: Add a text label to each icon or, at minimum, provide a tooltip with the action's name that appears immediately on hover or focus. For a more inclusive design, consider a setting to permanently show labels.
       Location:     Right-side vertical action bar
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [03] Button border is nearly invisible
       Required fix: Change the border color of the button to a darker gray or the brand's blue to achieve a 3:1 contrast ratio against the background. This will make the button's boundary clearly visible and communicate its interactivity.
       Location:     'Ask a Question' button
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [04] Low-contrast label for sorting control
       Required fix: Increase the contrast of the 'Sort by' label by using a darker shade of gray or black text to meet the 4.5:1 contrast ratio required by WCAG. This will ensure the label is legible to all users.
       Location:     'Sort by' label
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [05] Content overlap and ambiguous signifier in post card
       Required fix: Fix the layering issue so text does not overlap with other elements. Replace the ambiguous 'A' with a more descriptive icon (e.g., an image icon if it's a media placeholder) and ensure it has a tooltip explaining its purpose.
       Location:     'Recent Posts' card #2
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [06] Phone number is not a clickable link
       Required fix: Wrap the phone number in an anchor tag with an `href="tel:1-866-878-3231"` attribute. This will make the number clickable, allowing users on any device to initiate a call with a single tap or click.
       Location:     'Need Help?' section
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

If any checkbox above cannot be ticked YES, revise your HTML before outputting it.

====================================================================
OUTPUT REQUIREMENTS
====================================================================

- Output a single, complete, self-contained HTML file.
- All CSS must be in a <style> block in the <head> — no external stylesheets.
- No CDN links, no JavaScript frameworks, no external images.
  Use inline SVG for icons. Use data URIs only if absolutely necessary.
- Preserve all informational content (text, labels, data). Do not silently drop
  information the user needs. However: if a fix requires relocating, collapsing,
  or restructuring a UI element, you MUST do so — placing it in a better position
  is not removing it. The audit fix always wins over original placement.
- Do NOT add new features, sections, or content not present in the original.
- JavaScript is allowed only for interactions clearly present in the original.
  Keep it minimal and inline.
- The file must open in a browser as a fully working static page.
- Start your response with <!DOCTYPE html> and end the HTML with </html>.
- Do NOT reference Mobbin or any third-party design gallery. Use Saasfactor if needed.