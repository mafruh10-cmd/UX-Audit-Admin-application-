You are a senior UI/UX designer and expert frontend developer.

I am giving you a screenshot of a UI screen and a detailed UX audit report.
Your task is to produce a complete, self-contained HTML redesign (version 2)
that resolves every issue in the audit while preserving the original visual identity.

CRITICAL RULE — READ BEFORE ANYTHING ELSE:
Fixing an issue takes absolute priority over preserving layout or placement.
If a fix requires moving an element, restructuring a section, hiding a component,
or changing how content is presented — DO IT. The audit findings are non-negotiable.
Preservation means keeping the brand identity (colors, typography, component style).
It does NOT mean keeping broken elements in the same broken position.

====================================================================
MANDATORY FIX CHECKLIST  —  VERIFY EACH BEFORE WRITING A SINGLE LINE OF HTML
====================================================================

Before you write the HTML, mentally confirm that your design addresses EVERY item
in this list. If your HTML does not contain a concrete, visible solution for a
High or Medium issue, your response is incomplete — revise it until it does.
Some fixes require structural changes (relocating, collapsing, or restructuring
UI elements). Make those structural changes — do not style over them.

HIGH PRIORITY  (must be resolved — zero exceptions):
  NOTE: 'fixing' means the issue is GONE from the redesign — not styled differently.
  If the fix says to move, collapse, or restructure something, do exactly that.

  [01] Star ratings convey information through color alone
       Problem:          The rating system uses filled vs. unfilled yellow stars to communicate scores, relying entirely on color and subtle visual fill differences. Users with color vision deficiencies or low vision cannot distinguish between filled and unfilled stars. No text alternative or numeric value is provided for the star ratings in the Current/Potential columns (only Overall row has numbers).
       What to implement: Add numeric equivalents next to each star rating (e.g., '4/5'). Alternatively, provide aria-labels like 'Design: 4 out of 5 stars' for screen readers and display the numeric value visually alongside the stars.
       Affects:          Summary table — Current and Potential columns with star ratings
       WCAG:             1.4.1 Level A

  [02] Insufficient contrast on chart axis labels and secondary text
       Problem:          The axis labels on the Views Per Visit chart appear in a light gray color against the white/light background, likely falling below the 4.5:1 contrast ratio required for normal-sized text. This makes the chart data difficult to interpret for users with low vision.
       What to implement: Increase the axis label color to at least #767676 on white backgrounds to meet the 4.5:1 ratio. Use a consistent dark gray (#595959 or darker) for all chart labels to ensure readability.
       Affects:          Line chart Y-axis labels (0, 0.35, 0.7, 1.05, 1.4) and X-axis date labels
       WCAG:             1.4.3 Level AA

  [03] Bounce Rate metric shown without context or benchmarking
       Problem:          An 86% bounce rate is critically high, yet it is displayed with the same neutral visual treatment as other metrics. There is no color-coding, trend indicator, or contextual benchmark to signal this is a concerning value. Users unfamiliar with analytics may not recognize this requires urgent attention.
       What to implement: Add conditional formatting — display metrics in red/amber/green based on performance thresholds. Include a small trend arrow or comparison to industry benchmarks (e.g., 'Average: 45%'). Add a tooltip explaining what the metric means and what's considered healthy.
       Affects:          Bounce Rate card showing 86%

  [04] Icon-only action buttons lack accessible labels
       Problem:          The action icons below the chat response have no visible text labels and likely lack aria-labels. Screen reader users cannot determine the function of these buttons, and sighted users may also struggle to identify their purpose without tooltips.
       What to implement: Add aria-label attributes to each icon button (e.g., 'Copy response', 'Thumbs up', 'Undo'). Add visible tooltips on hover/focus. Consider adding text labels for the most important actions.
       Affects:          Bottom of chat panel — row of small icons (undo, thumbs up, thumbs down, copy, more)
       WCAG:             1.1.1 Level A

MEDIUM PRIORITY  (must be resolved — no exceptions):
  NOTE: Same rule — fix means the issue does not exist in the output HTML.

  [05] Split-panel layout creates competing focal points
       Problem:          The screen simultaneously displays a chat interface with AI recommendations on the left and analytics data on the right, creating two competing contexts. Users must context-switch between the conversational AI suggestions and the analytical data, increasing cognitive load.
       What to implement: Consider collapsing the chat panel when the Analytics tab is active, or provide a toggle to show/hide it. Alternatively, integrate the AI insights directly into the analytics view as contextual annotations near relevant metrics.
       Affects:          Left chat panel alongside right analytics panel

  [06] Views Per Visit chart lacks data point labels and interactivity cues
       Problem:          The line chart shows a spike around Dec 29 but provides no hover tooltips, data point markers, or exact values. Users cannot determine precise values without interacting, and there's no visual affordance suggesting the chart is interactive.
       What to implement: Add visible data point markers at each date interval. Implement hover/focus tooltips showing exact values and dates. Add a subtle 'hover for details' hint or make the chart's interactivity discoverable through cursor changes.
       Affects:          Line chart in the center of the analytics panel

  [07] No focus visible indicator apparent on selected metric card
       Problem:          The Views Per Visit card appears selected with an orange border, but it's unclear if this styling would persist as a focus indicator for keyboard users. No other cards show focus styling, suggesting keyboard navigation may not provide visible focus indicators across all interactive elements.
       What to implement: Ensure all interactive metric cards display a visible 2px+ focus ring when navigated via keyboard. Use CSS :focus-visible to apply a high-contrast outline that is distinguishable from the selected state.
       Affects:          Views Per Visit metric card (highlighted with orange border)
       WCAG:             2.4.7 Level AA

  [08] Data tables lack sorting affordances and row hover states
       Problem:          The four data tables at the bottom display static data without any sorting controls, hover highlights, or clickable row affordances. Users cannot reorder data or drill down into specific sources, pages, or countries to get deeper insights.
       What to implement: Add sortable column headers with arrow indicators. Include hover states on rows to signal clickability. Enable drill-down functionality so users can click a source or page to see detailed breakdowns.
       Affects:          Source/Visitors, Page/Visitors, Country/Visitors, Device/Visitors tables

LOW PRIORITY  (implement where possible):
  [09] Inconsistent visual hierarchy between Overall score and category rows — Show numeric scores alongside star ratings for all rows, or convert the Overall row to also use star ratings. Maintain a single, consistent rating visualization throughout the table.
  [10] Action buttons at bottom of chat lack visual prominence — Style at least one primary action as a filled button with the brand accent color. Use a stronger border or background fill to create visual hierarchy between the most impactful action and secondary suggestions.

====================================================================
STEP 1 — LAYOUT & COMPOSITION  (reason through this BEFORE anything else)
====================================================================

Layout and content distribution are non-negotiable design decisions.
Do not assume the original layout is correct — reason from first principles.
Ask yourself: does each element sit in the right zone for its purpose?
If not, move it. The composition must serve the user's primary goal.

1. VISUAL HIERARCHY
   The most important action or information must be visually dominant.
   Use size, weight, contrast, and position to establish a clear reading order.
   Secondary content must be visually subordinate — smaller, lighter, lower.
   (Malewicz — Designing User Interfaces: contrast creates hierarchy)
   (Refactoring UI: use size and weight to communicate importance, not just color)

2. F-PATTERN & READING FLOW
   Users scan left-to-right, top-to-bottom in an F-shape on content-heavy screens.
   Place primary actions and key labels at the start of scan lines.
   Never interrupt the scan path with secondary, promotional, or personalisation content.
   (Nielsen Norman Group — F-Pattern reading behavior)

3. PROXIMITY & GROUPING (Gestalt)
   Related elements must be spatially close. Unrelated elements need clear separation.
   If two elements answer different user questions, they belong in different zones.
   Never mix contextual content with navigational content in the same region.
   (Psych 101 — Gestalt: Law of Proximity, Law of Similarity)

4. CONTENT PRIORITISATION
   Identify the user's single primary goal on this screen.
   Every element that does not serve that goal directly must be secondary in
   visual weight and position. Upsell, personalisation, and promotional content
   must never compete with or interrupt the primary task flow.
   (Don't Make Me Think — Krug: do not make users think about what to do next)
   (Psychology of Design — Hick's Law: fewer competing choices = faster decisions)

5. SPATIAL ZONES
   Divide the screen into logical zones: navigation, primary content, secondary
   context, actions. Each zone has exactly one job.
   If an element is in the wrong zone, move it to the correct zone or defer it
   to a modal or drawer. The layout must match the user's mental model of the task.
   (Universal Principles of Design — Progressive Disclosure)

6. SIGNAL-TO-NOISE
   Every element must earn its place. If an element does not directly help the
   user complete their current task, reduce its visual weight or remove it from
   the primary viewport. Clutter is a UX failure, not a neutral default.
   (Universal Principles of Design — Signal-to-Noise Ratio)

COMPOSITION RULE: If the original layout violates these principles — with or
without an explicit audit finding — correct it. Do not replicate bad composition
because it existed in the screenshot. A version 2 must be compositionally sound.

APPLY THESE COMPOSITION DECISIONS TO RESOLVE THE FOLLOWING AUDIT ISSUES:
Every High and Medium issue in the MANDATORY FIX CHECKLIST above must map to a
specific, visible structural decision in your layout. Before moving to Step 2,
trace each issue to a layout zone, spacing decision, or hierarchy change.
If the fix requires restructuring — restructure. If it requires removing an element
from the primary viewport — remove it. Composition fixes are not optional.

====================================================================
STEP 2 — VISUAL IDENTITY  (preserve brand, not broken placement)
====================================================================

Once composition is decided, preserve these brand elements exactly:
  - Colors: every background, text, brand, accent, and surface hex value
  - Typography: font families, sizes, weights, line-heights, letter-spacing
  - Spacing scale: the relative rhythm of padding/margin (not the positions)
  - Shapes: border-radius, border styles, shadows, elevation
  - Icons: style, weight, and size (placement may change if composition requires)
  - Components: card designs, button styles, badge styles, input styles

NOTE: Layout and element positioning are governed by Step 1, not this list.
Preserving the brand does NOT mean preserving where broken things sit.
The output must feel like a version 2 — same brand, better UX.

====================================================================
FULL AUDIT REPORT  —  Analytics Dashboard
====================================================================
Overall Score:        6.4/10  —  Fair
Accessibility Score:  4.8/10  (WCAG 2.2 AA)
Issues Found:         10  (4 High · 4 Medium · 2 Low)

EXECUTIVE SUMMARY
The Analytics dashboard presents key metrics and visitor data but suffers from accessibility issues including color-only data encoding in star ratings, low contrast text elements, and small interactive targets. The split-panel layout creates cognitive overhead with the left chat panel competing for attention with the analytics content, and several data visualizations lack sufficient labeling or context for users to derive actionable insights.

ALL ISSUES  (full detail for reference):

[01] HIGH  —  Star ratings convey information through color alone
     Location:  Summary table — Current and Potential columns with star ratings
     Problem:   The rating system uses filled vs. unfilled yellow stars to communicate scores, relying entirely on color and subtle visual fill differences. Users with color vision deficiencies or low vision cannot distinguish between filled and unfilled stars. No text alternative or numeric value is provided for the star ratings in the Current/Potential columns (only Overall row has numbers).
     Fix:       Add numeric equivalents next to each star rating (e.g., '4/5'). Alternatively, provide aria-labels like 'Design: 4 out of 5 stars' for screen readers and display the numeric value visually alongside the stars.
     Source:    WCAG 2.2 — 1.4.1 Use of Color (A): color must not be the only visual means of conveying information
     WCAG:      1.4.1  Level A

[02] HIGH  —  Insufficient contrast on chart axis labels and secondary text
     Location:  Line chart Y-axis labels (0, 0.35, 0.7, 1.05, 1.4) and X-axis date labels
     Problem:   The axis labels on the Views Per Visit chart appear in a light gray color against the white/light background, likely falling below the 4.5:1 contrast ratio required for normal-sized text. This makes the chart data difficult to interpret for users with low vision.
     Fix:       Increase the axis label color to at least #767676 on white backgrounds to meet the 4.5:1 ratio. Use a consistent dark gray (#595959 or darker) for all chart labels to ensure readability.
     Source:    WCAG 2.2 — 1.4.3 Contrast Minimum (AA): normal text requires 4.5:1 contrast ratio
     WCAG:      1.4.3  Level AA

[03] HIGH  —  Bounce Rate metric shown without context or benchmarking
     Location:  Bounce Rate card showing 86%
     Problem:   An 86% bounce rate is critically high, yet it is displayed with the same neutral visual treatment as other metrics. There is no color-coding, trend indicator, or contextual benchmark to signal this is a concerning value. Users unfamiliar with analytics may not recognize this requires urgent attention.
     Fix:       Add conditional formatting — display metrics in red/amber/green based on performance thresholds. Include a small trend arrow or comparison to industry benchmarks (e.g., 'Average: 45%'). Add a tooltip explaining what the metric means and what's considered healthy.
     Source:    The Design of Everyday Things (Norman) — Gulf of Evaluation: user cannot tell if their action worked

[04] HIGH  —  Icon-only action buttons lack accessible labels
     Location:  Bottom of chat panel — row of small icons (undo, thumbs up, thumbs down, copy, more)
     Problem:   The action icons below the chat response have no visible text labels and likely lack aria-labels. Screen reader users cannot determine the function of these buttons, and sighted users may also struggle to identify their purpose without tooltips.
     Fix:       Add aria-label attributes to each icon button (e.g., 'Copy response', 'Thumbs up', 'Undo'). Add visible tooltips on hover/focus. Consider adding text labels for the most important actions.
     Source:    WCAG 2.2 — 1.1.1 Non-text Content (A): all functional non-text content must have text alternatives
     WCAG:      1.1.1  Level A

[05] MEDIUM  —  Split-panel layout creates competing focal points
     Location:  Left chat panel alongside right analytics panel
     Problem:   The screen simultaneously displays a chat interface with AI recommendations on the left and analytics data on the right, creating two competing contexts. Users must context-switch between the conversational AI suggestions and the analytical data, increasing cognitive load.
     Fix:       Consider collapsing the chat panel when the Analytics tab is active, or provide a toggle to show/hide it. Alternatively, integrate the AI insights directly into the analytics view as contextual annotations near relevant metrics.
     Source:    Psychology of Design — Hick's Law: too many choices slows decisions

[06] MEDIUM  —  Views Per Visit chart lacks data point labels and interactivity cues
     Location:  Line chart in the center of the analytics panel
     Problem:   The line chart shows a spike around Dec 29 but provides no hover tooltips, data point markers, or exact values. Users cannot determine precise values without interacting, and there's no visual affordance suggesting the chart is interactive.
     Fix:       Add visible data point markers at each date interval. Implement hover/focus tooltips showing exact values and dates. Add a subtle 'hover for details' hint or make the chart's interactivity discoverable through cursor changes.
     Source:    The Design of Everyday Things (Norman) — Signifier: visible cues must indicate where to act

[07] MEDIUM  —  No focus visible indicator apparent on selected metric card
     Location:  Views Per Visit metric card (highlighted with orange border)
     Problem:   The Views Per Visit card appears selected with an orange border, but it's unclear if this styling would persist as a focus indicator for keyboard users. No other cards show focus styling, suggesting keyboard navigation may not provide visible focus indicators across all interactive elements.
     Fix:       Ensure all interactive metric cards display a visible 2px+ focus ring when navigated via keyboard. Use CSS :focus-visible to apply a high-contrast outline that is distinguishable from the selected state.
     Source:    WCAG 2.2 — 2.4.7 Focus Visible (AA): keyboard focus indicator must be visible
     WCAG:      2.4.7  Level AA

[08] MEDIUM  —  Data tables lack sorting affordances and row hover states
     Location:  Source/Visitors, Page/Visitors, Country/Visitors, Device/Visitors tables
     Problem:   The four data tables at the bottom display static data without any sorting controls, hover highlights, or clickable row affordances. Users cannot reorder data or drill down into specific sources, pages, or countries to get deeper insights.
     Fix:       Add sortable column headers with arrow indicators. Include hover states on rows to signal clickability. Enable drill-down functionality so users can click a source or page to see detailed breakdowns.
     Source:    The Design of Everyday Things (Norman) — Affordance: element must communicate how it is used

[09] LOW  —  Inconsistent visual hierarchy between Overall score and category rows
     Location:  Summary table — Overall row vs. category rows
     Problem:   The Overall row uses bold text and numeric values while category rows use star ratings. This inconsistency forces users to mentally translate between two different rating systems within the same table, creating unnecessary cognitive friction.
     Fix:       Show numeric scores alongside star ratings for all rows, or convert the Overall row to also use star ratings. Maintain a single, consistent rating visualization throughout the table.
     Source:    Psych 101 (Kleinman) — Gestalt: Law of Similarity

[10] LOW  —  Action buttons at bottom of chat lack visual prominence
     Location:  'Add contact form' and 'Add mobile menu' pill buttons
     Problem:   The suggested action buttons appear as low-contrast outline pills that blend with the background. These are the primary CTAs derived from the AI analysis but lack the visual weight needed to drive user action.
     Fix:       Style at least one primary action as a filled button with the brand accent color. Use a stronger border or background fill to create visual hierarchy between the most impactful action and secondary suggestions.
     Source:    Refactoring UI — Use color to support hierarchy, not communicate it alone

====================================================================
SELF-CHECK — RUN THIS BEFORE OUTPUTTING THE HTML
====================================================================

For each item below, verify the SPECIFIC FIX is concretely implemented —
not just acknowledged, not just styled differently. The fix must be visible.

  [ ] [01] Star ratings convey information through color alone
       Required fix: Add numeric equivalents next to each star rating (e.g., '4/5'). Alternatively, provide aria-labels like 'Design: 4 out of 5 stars' for screen readers and display the numeric value visually alongside the stars.
       Location:     Summary table — Current and Potential columns with star ratings
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [02] Insufficient contrast on chart axis labels and secondary text
       Required fix: Increase the axis label color to at least #767676 on white backgrounds to meet the 4.5:1 ratio. Use a consistent dark gray (#595959 or darker) for all chart labels to ensure readability.
       Location:     Line chart Y-axis labels (0, 0.35, 0.7, 1.05, 1.4) and X-axis date labels
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [03] Bounce Rate metric shown without context or benchmarking
       Required fix: Add conditional formatting — display metrics in red/amber/green based on performance thresholds. Include a small trend arrow or comparison to industry benchmarks (e.g., 'Average: 45%'). Add a tooltip explaining what the metric means and what's considered healthy.
       Location:     Bounce Rate card showing 86%
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [04] Icon-only action buttons lack accessible labels
       Required fix: Add aria-label attributes to each icon button (e.g., 'Copy response', 'Thumbs up', 'Undo'). Add visible tooltips on hover/focus. Consider adding text labels for the most important actions.
       Location:     Bottom of chat panel — row of small icons (undo, thumbs up, thumbs down, copy, more)
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [05] Split-panel layout creates competing focal points
       Required fix: Consider collapsing the chat panel when the Analytics tab is active, or provide a toggle to show/hide it. Alternatively, integrate the AI insights directly into the analytics view as contextual annotations near relevant metrics.
       Location:     Left chat panel alongside right analytics panel
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [06] Views Per Visit chart lacks data point labels and interactivity cues
       Required fix: Add visible data point markers at each date interval. Implement hover/focus tooltips showing exact values and dates. Add a subtle 'hover for details' hint or make the chart's interactivity discoverable through cursor changes.
       Location:     Line chart in the center of the analytics panel
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [07] No focus visible indicator apparent on selected metric card
       Required fix: Ensure all interactive metric cards display a visible 2px+ focus ring when navigated via keyboard. Use CSS :focus-visible to apply a high-contrast outline that is distinguishable from the selected state.
       Location:     Views Per Visit metric card (highlighted with orange border)
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [08] Data tables lack sorting affordances and row hover states
       Required fix: Add sortable column headers with arrow indicators. Include hover states on rows to signal clickability. Enable drill-down functionality so users can click a source or page to see detailed breakdowns.
       Location:     Source/Visitors, Page/Visitors, Country/Visitors, Device/Visitors tables
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

If any checkbox above cannot be ticked YES, revise your HTML before outputting it.

====================================================================
OUTPUT REQUIREMENTS
====================================================================

- Output a single, complete, self-contained HTML file.
- All CSS must be in a <style> block in the <head> — no external stylesheets.
- No CDN links, no JavaScript frameworks, no external images.
  Use inline SVG for icons. Use data URIs only if absolutely necessary.
- Preserve all informational content (text, labels, data). Do not silently drop
  information the user needs. However: if a fix requires relocating, collapsing,
  or restructuring a UI element, you MUST do so — placing it in a better position
  is not removing it. The audit fix always wins over original placement.
- Do NOT add new features, sections, or content not present in the original.
- JavaScript is allowed only for interactions clearly present in the original.
  Keep it minimal and inline.
- The file must open in a browser as a fully working static page.
- Start your response with <!DOCTYPE html> and end the HTML with </html>.
- Do NOT reference Mobbin or any third-party design gallery. Use Saasfactor if needed.