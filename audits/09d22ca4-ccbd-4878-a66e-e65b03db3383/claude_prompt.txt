You are a senior UI/UX designer and expert frontend developer.

I am giving you a screenshot of a UI screen and a detailed UX audit report.
Your task is to produce a complete, self-contained HTML redesign (version 2)
that resolves every issue in the audit while preserving the original visual identity.

CRITICAL RULE — READ BEFORE ANYTHING ELSE:
Fixing an issue takes absolute priority over preserving layout or placement.
If a fix requires moving an element, restructuring a section, hiding a component,
or changing how content is presented — DO IT. The audit findings are non-negotiable.
Preservation means keeping the brand identity (colors, typography, component style).
It does NOT mean keeping broken elements in the same broken position.

====================================================================
MANDATORY FIX CHECKLIST  —  VERIFY EACH BEFORE WRITING A SINGLE LINE OF HTML
====================================================================

Before you write the HTML, mentally confirm that your design addresses EVERY item
in this list. If your HTML does not contain a concrete, visible solution for a
High or Medium issue, your response is incomplete — revise it until it does.
Some fixes require structural changes (relocating, collapsing, or restructuring
UI elements). Make those structural changes — do not style over them.

HIGH PRIORITY  (must be resolved — zero exceptions):
  NOTE: 'fixing' means the issue is GONE from the redesign — not styled differently.
  If the fix says to move, collapse, or restructure something, do exactly that.

  [01] Low contrast on secondary/metadata text
       Problem:          Multiple secondary text elements (e.g., 'handled', 'level', 'url', timestamp labels) appear in a muted gray that may not meet the 4.5:1 minimum contrast ratio against the white background. Users with low vision will struggle to read these critical debugging details.
       What to implement: Increase the color of all secondary text labels to at least #595959 on white backgrounds to ensure a 4.5:1 ratio. Use font weight or size differentiation rather than reduced contrast alone to denote secondary hierarchy.
       Affects:          Metadata labels like 'handled', 'level', 'url', 'Trace: Trace ID' in the Highlights section and timestamps in Activity
       WCAG:             1.4.3 Level AA

  [02] Color-only status indicators for 'Unhandled' and 'New' badges
       Problem:          The 'Unhandled' badge relies heavily on its red color to communicate urgency, while 'New' uses a neutral gray. Users with color vision deficiencies (deuteranopia, protanopia) may not distinguish the severity difference between these badges. No icon or pattern supplements the color coding.
       What to implement: Add a warning icon (e.g., ⚠) to the 'Unhandled' badge or use a distinct shape/border style. Ensure each status has a non-color differentiator such as an icon prefix or distinct border pattern.
       Affects:          Status badges ('Unhandled' in red, 'New' in gray) below the issue title
       WCAG:             1.4.1 Level A

  [03] Icon-only buttons lack accessible labels
       Problem:          The bell icon, export/share icon, and ellipsis overflow button in the action bar appear to be icon-only with no visible text labels. Without aria-labels or tooltips, screen reader users cannot determine their purpose, and sighted users must guess their function.
       What to implement: Add aria-label attributes to each icon button (e.g., 'Notifications', 'Share issue', 'More actions'). Also add visible tooltips on hover/focus to aid all users.
       Affects:          Action bar icons — bell/notification, share/export, and '...' overflow menu next to Resolve/Archive
       WCAG:             1.1.1 Level A

  [04] Excessive information density creates cognitive overload
       Problem:          The issue detail screen presents event metadata, a chart, highlights, stack trace, and a promotional banner all in a single scrollable view. This creates a very high cognitive load, making it difficult for users to focus on the most relevant debugging information. The promotional 'Connect with Git Providers' banner further competes for attention.
       What to implement: Use progressive disclosure — collapse the Highlights and event chart by default and let users expand them. Move the Git provider promotion to a less prominent position (e.g., settings page or a subtle inline hint) rather than a large card within the debugging flow.
       Affects:          Main content area — Highlights, Stack Trace, event chart, tags, and Git provider banner all visible simultaneously

MEDIUM PRIORITY  (must be resolved — no exceptions):
  NOTE: Same rule — fix means the issue does not exist in the output HTML.

  [05] Promotional banner disrupts task flow
       Problem:          A large promotional card for Git provider integration interrupts the stack trace debugging workflow. It includes a decorative illustration that takes significant vertical space, pushing critical stack trace frames below the fold. This is a non-essential CTA placed within a high-focus debugging context.
       What to implement: Move this promotion to a dismissible toast, an onboarding checklist, or the project settings page. If kept inline, reduce its size significantly and allow permanent dismissal. The decorative illustration should be removed or minimized.
       Affects:          'Connect with Git Providers' card with illustration near bottom of main content

  [06] Small touch/click targets on navigation tabs
       Problem:          The horizontal tab labels for navigating within the event (Highlights, Stack Trace, Replay, etc.) appear to have small clickable areas with tight spacing. On touch devices or for users with motor impairments, these targets may be difficult to activate accurately.
       What to implement: Increase the padding around each tab to ensure at least 24×24px target size, ideally 44×44px for touch. Add clear visual separators or spacing between tabs to reduce accidental activation of adjacent items.
       Affects:          Event navigation tabs: 'Highlights', 'Stack Trace', 'Replay', 'Breadcrumbs', 'Logs', 'Trace…' horizontal tab bar
       WCAG:             2.5.8 Level AA

  [07] Ambiguous 'Seer' AI feature labeling
       Problem:          The 'Seer' feature uses a brand name that doesn't clearly communicate its purpose (AI-assisted root cause analysis). The term 'Initial Guess' further undermines user confidence in the suggestion. New users cannot quickly understand what this section does or how reliable it is.
       What to implement: Rename 'Seer' to something descriptive like 'AI Root Cause Analysis' or add a subtitle. Replace 'Initial Guess' with 'AI Analysis' or 'Suggested Root Cause' with a confidence indicator to set proper expectations.
       Affects:          Right sidebar — 'Seer' section with warning icon and 'Initial Guess' label

  [08] Activity feed lacks clear visual grouping
       Problem:          The activity feed lists status changes (Unresolved, Archived, Unresolved) with identical visual treatment and minimal spacing. The repeated 'Unresolved' entries are visually indistinguishable at a glance, and the crossed-out icon for status changes is subtle. Users must read each entry carefully to understand the issue's lifecycle.
       What to implement: Use distinct background colors or left-border colors for different activity types (e.g., green for resolved, orange for archived, red for unresolved). Group related status changes and add clearer iconography to differentiate action types at a glance.
       Affects:          Right sidebar — Activity section showing Unresolved/Archived status changes

LOW PRIORITY  (implement where possible):
  [09] Dropdown filter labels lack visible selected state clarity — Add a subtle background fill or border to indicate these are interactive dropdowns. When a non-default filter is active, use a badge count or distinct highlight color to signal the active filter state.
  [10] Truncated tag values reduce scannability — Use middle-truncation (e.g., 'http://…/sentry-example-page') or expand the tag area width. Alternatively, show the path portion only since localhost:3000 is less distinctive than the page path.

====================================================================
STEP 1 — LAYOUT & COMPOSITION  (reason through this BEFORE anything else)
====================================================================

Layout and content distribution are non-negotiable design decisions.
Do not assume the original layout is correct — reason from first principles.
Ask yourself: does each element sit in the right zone for its purpose?
If not, move it. The composition must serve the user's primary goal.

1. VISUAL HIERARCHY
   The most important action or information must be visually dominant.
   Use size, weight, contrast, and position to establish a clear reading order.
   Secondary content must be visually subordinate — smaller, lighter, lower.
   (Malewicz — Designing User Interfaces: contrast creates hierarchy)
   (Refactoring UI: use size and weight to communicate importance, not just color)

2. F-PATTERN & READING FLOW
   Users scan left-to-right, top-to-bottom in an F-shape on content-heavy screens.
   Place primary actions and key labels at the start of scan lines.
   Never interrupt the scan path with secondary, promotional, or personalisation content.
   (Nielsen Norman Group — F-Pattern reading behavior)

3. PROXIMITY & GROUPING (Gestalt)
   Related elements must be spatially close. Unrelated elements need clear separation.
   If two elements answer different user questions, they belong in different zones.
   Never mix contextual content with navigational content in the same region.
   (Psych 101 — Gestalt: Law of Proximity, Law of Similarity)

4. CONTENT PRIORITISATION
   Identify the user's single primary goal on this screen.
   Every element that does not serve that goal directly must be secondary in
   visual weight and position. Upsell, personalisation, and promotional content
   must never compete with or interrupt the primary task flow.
   (Don't Make Me Think — Krug: do not make users think about what to do next)
   (Psychology of Design — Hick's Law: fewer competing choices = faster decisions)

5. SPATIAL ZONES
   Divide the screen into logical zones: navigation, primary content, secondary
   context, actions. Each zone has exactly one job.
   If an element is in the wrong zone, move it to the correct zone or defer it
   to a modal or drawer. The layout must match the user's mental model of the task.
   (Universal Principles of Design — Progressive Disclosure)

6. SIGNAL-TO-NOISE
   Every element must earn its place. If an element does not directly help the
   user complete their current task, reduce its visual weight or remove it from
   the primary viewport. Clutter is a UX failure, not a neutral default.
   (Universal Principles of Design — Signal-to-Noise Ratio)

COMPOSITION RULE: If the original layout violates these principles — with or
without an explicit audit finding — correct it. Do not replicate bad composition
because it existed in the screenshot. A version 2 must be compositionally sound.

APPLY THESE COMPOSITION DECISIONS TO RESOLVE THE FOLLOWING AUDIT ISSUES:
Every High and Medium issue in the MANDATORY FIX CHECKLIST above must map to a
specific, visible structural decision in your layout. Before moving to Step 2,
trace each issue to a layout zone, spacing decision, or hierarchy change.
If the fix requires restructuring — restructure. If it requires removing an element
from the primary viewport — remove it. Composition fixes are not optional.

====================================================================
STEP 2 — VISUAL IDENTITY  (preserve brand, not broken placement)
====================================================================

Once composition is decided, preserve these brand elements exactly:
  - Colors: every background, text, brand, accent, and surface hex value
  - Typography: font families, sizes, weights, line-heights, letter-spacing
  - Spacing scale: the relative rhythm of padding/margin (not the positions)
  - Shapes: border-radius, border styles, shadows, elevation
  - Icons: style, weight, and size (placement may change if composition requires)
  - Components: card designs, button styles, badge styles, input styles

NOTE: Layout and element positioning are governed by Step 1, not this list.
Preserving the brand does NOT mean preserving where broken things sit.
The output must feel like a version 2 — same brand, better UX.

====================================================================
FULL AUDIT REPORT  —  Issue Detail — SentryExampleFrontendError
====================================================================
Overall Score:        7.1/10  —  Good
Accessibility Score:  6.2/10  (WCAG 2.2 AA)
Issues Found:         10  (4 High · 4 Medium · 2 Low)

EXECUTIVE SUMMARY
Sentry's issue detail screen provides a dense but well-organized layout for debugging. Key concerns include information overload without progressive disclosure, small contrast issues on secondary text, and a promotional banner that competes with critical debugging context. The activity feed and right sidebar could benefit from clearer visual hierarchy.

ALL ISSUES  (full detail for reference):

[01] HIGH  —  Low contrast on secondary/metadata text
     Location:  Metadata labels like 'handled', 'level', 'url', 'Trace: Trace ID' in the Highlights section and timestamps in Activity
     Problem:   Multiple secondary text elements (e.g., 'handled', 'level', 'url', timestamp labels) appear in a muted gray that may not meet the 4.5:1 minimum contrast ratio against the white background. Users with low vision will struggle to read these critical debugging details.
     Fix:       Increase the color of all secondary text labels to at least #595959 on white backgrounds to ensure a 4.5:1 ratio. Use font weight or size differentiation rather than reduced contrast alone to denote secondary hierarchy.
     Source:    WCAG 2.2 — 1.4.3 Contrast Minimum (AA): normal text requires 4.5:1 contrast ratio
     WCAG:      1.4.3  Level AA

[02] HIGH  —  Color-only status indicators for 'Unhandled' and 'New' badges
     Location:  Status badges ('Unhandled' in red, 'New' in gray) below the issue title
     Problem:   The 'Unhandled' badge relies heavily on its red color to communicate urgency, while 'New' uses a neutral gray. Users with color vision deficiencies (deuteranopia, protanopia) may not distinguish the severity difference between these badges. No icon or pattern supplements the color coding.
     Fix:       Add a warning icon (e.g., ⚠) to the 'Unhandled' badge or use a distinct shape/border style. Ensure each status has a non-color differentiator such as an icon prefix or distinct border pattern.
     Source:    WCAG 2.2 — 1.4.1 Use of Color (A): information must not be conveyed by color alone
     WCAG:      1.4.1  Level A

[03] HIGH  —  Icon-only buttons lack accessible labels
     Location:  Action bar icons — bell/notification, share/export, and '...' overflow menu next to Resolve/Archive
     Problem:   The bell icon, export/share icon, and ellipsis overflow button in the action bar appear to be icon-only with no visible text labels. Without aria-labels or tooltips, screen reader users cannot determine their purpose, and sighted users must guess their function.
     Fix:       Add aria-label attributes to each icon button (e.g., 'Notifications', 'Share issue', 'More actions'). Also add visible tooltips on hover/focus to aid all users.
     Source:    WCAG 2.2 — 1.1.1 Non-text Content (A): all non-text content must have a text alternative
     WCAG:      1.1.1  Level A

[04] HIGH  —  Excessive information density creates cognitive overload
     Location:  Main content area — Highlights, Stack Trace, event chart, tags, and Git provider banner all visible simultaneously
     Problem:   The issue detail screen presents event metadata, a chart, highlights, stack trace, and a promotional banner all in a single scrollable view. This creates a very high cognitive load, making it difficult for users to focus on the most relevant debugging information. The promotional 'Connect with Git Providers' banner further competes for attention.
     Fix:       Use progressive disclosure — collapse the Highlights and event chart by default and let users expand them. Move the Git provider promotion to a less prominent position (e.g., settings page or a subtle inline hint) rather than a large card within the debugging flow.
     Source:    Universal Principles of Design — Progressive Disclosure: show only what is needed at each step

[05] MEDIUM  —  Promotional banner disrupts task flow
     Location:  'Connect with Git Providers' card with illustration near bottom of main content
     Problem:   A large promotional card for Git provider integration interrupts the stack trace debugging workflow. It includes a decorative illustration that takes significant vertical space, pushing critical stack trace frames below the fold. This is a non-essential CTA placed within a high-focus debugging context.
     Fix:       Move this promotion to a dismissible toast, an onboarding checklist, or the project settings page. If kept inline, reduce its size significantly and allow permanent dismissal. The decorative illustration should be removed or minimized.
     Source:    Universal Principles of Design — Signal-to-Noise Ratio: maximise relevant information, minimise clutter

[06] MEDIUM  —  Small touch/click targets on navigation tabs
     Location:  Event navigation tabs: 'Highlights', 'Stack Trace', 'Replay', 'Breadcrumbs', 'Logs', 'Trace…' horizontal tab bar
     Problem:   The horizontal tab labels for navigating within the event (Highlights, Stack Trace, Replay, etc.) appear to have small clickable areas with tight spacing. On touch devices or for users with motor impairments, these targets may be difficult to activate accurately.
     Fix:       Increase the padding around each tab to ensure at least 24×24px target size, ideally 44×44px for touch. Add clear visual separators or spacing between tabs to reduce accidental activation of adjacent items.
     Source:    WCAG 2.2 — 2.5.8 Target Size Minimum (AA): interactive targets must be at least 24x24 CSS pixels
     WCAG:      2.5.8  Level AA

[07] MEDIUM  —  Ambiguous 'Seer' AI feature labeling
     Location:  Right sidebar — 'Seer' section with warning icon and 'Initial Guess' label
     Problem:   The 'Seer' feature uses a brand name that doesn't clearly communicate its purpose (AI-assisted root cause analysis). The term 'Initial Guess' further undermines user confidence in the suggestion. New users cannot quickly understand what this section does or how reliable it is.
     Fix:       Rename 'Seer' to something descriptive like 'AI Root Cause Analysis' or add a subtitle. Replace 'Initial Guess' with 'AI Analysis' or 'Suggested Root Cause' with a confidence indicator to set proper expectations.
     Source:    The Design of Everyday Things (Norman) — Gulf of Evaluation: user cannot tell if their action worked

[08] MEDIUM  —  Activity feed lacks clear visual grouping
     Location:  Right sidebar — Activity section showing Unresolved/Archived status changes
     Problem:   The activity feed lists status changes (Unresolved, Archived, Unresolved) with identical visual treatment and minimal spacing. The repeated 'Unresolved' entries are visually indistinguishable at a glance, and the crossed-out icon for status changes is subtle. Users must read each entry carefully to understand the issue's lifecycle.
     Fix:       Use distinct background colors or left-border colors for different activity types (e.g., green for resolved, orange for archived, red for unresolved). Group related status changes and add clearer iconography to differentiate action types at a glance.
     Source:    Psych 101 (Kleinman) — Gestalt: Law of Similarity

[09] LOW  —  Dropdown filter labels lack visible selected state clarity
     Location:  Filter bar — 'All Envs' and 'Since First Seen' dropdown buttons
     Problem:   The filter dropdowns 'All Envs' and 'Since First Seen' use plain text with small chevrons. When default values are selected, there is no visual indication that these are active filters versus labels. Users may not realize they can refine the view or that the current view is already filtered.
     Fix:       Add a subtle background fill or border to indicate these are interactive dropdowns. When a non-default filter is active, use a badge count or distinct highlight color to signal the active filter state.
     Source:    The Design of Everyday Things (Norman) — Signifier: visible cues must indicate where to act

[10] LOW  —  Truncated tag values reduce scannability
     Location:  Right side of event chart — tag breakdown showing '…ntry-example-page' for url
     Problem:   The url tag value is truncated to '…ntry-example-page', hiding the meaningful prefix of the URL. Users debugging an issue need to see the full URL at a glance to quickly identify the affected page without additional interaction.
     Fix:       Use middle-truncation (e.g., 'http://…/sentry-example-page') or expand the tag area width. Alternatively, show the path portion only since localhost:3000 is less distinctive than the page path.
     Source:    Don't Make Me Think (Krug) — Billboard Design 101: users scan, not read

====================================================================
SELF-CHECK — RUN THIS BEFORE OUTPUTTING THE HTML
====================================================================

For each item below, verify the SPECIFIC FIX is concretely implemented —
not just acknowledged, not just styled differently. The fix must be visible.

  [ ] [01] Low contrast on secondary/metadata text
       Required fix: Increase the color of all secondary text labels to at least #595959 on white backgrounds to ensure a 4.5:1 ratio. Use font weight or size differentiation rather than reduced contrast alone to denote secondary hierarchy.
       Location:     Metadata labels like 'handled', 'level', 'url', 'Trace: Trace ID' in the Highlights section and timestamps in Activity
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [02] Color-only status indicators for 'Unhandled' and 'New' badges
       Required fix: Add a warning icon (e.g., ⚠) to the 'Unhandled' badge or use a distinct shape/border style. Ensure each status has a non-color differentiator such as an icon prefix or distinct border pattern.
       Location:     Status badges ('Unhandled' in red, 'New' in gray) below the issue title
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [03] Icon-only buttons lack accessible labels
       Required fix: Add aria-label attributes to each icon button (e.g., 'Notifications', 'Share issue', 'More actions'). Also add visible tooltips on hover/focus to aid all users.
       Location:     Action bar icons — bell/notification, share/export, and '...' overflow menu next to Resolve/Archive
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [04] Excessive information density creates cognitive overload
       Required fix: Use progressive disclosure — collapse the Highlights and event chart by default and let users expand them. Move the Git provider promotion to a less prominent position (e.g., settings page or a subtle inline hint) rather than a large card within the debugging flow.
       Location:     Main content area — Highlights, Stack Trace, event chart, tags, and Git provider banner all visible simultaneously
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [05] Promotional banner disrupts task flow
       Required fix: Move this promotion to a dismissible toast, an onboarding checklist, or the project settings page. If kept inline, reduce its size significantly and allow permanent dismissal. The decorative illustration should be removed or minimized.
       Location:     'Connect with Git Providers' card with illustration near bottom of main content
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [06] Small touch/click targets on navigation tabs
       Required fix: Increase the padding around each tab to ensure at least 24×24px target size, ideally 44×44px for touch. Add clear visual separators or spacing between tabs to reduce accidental activation of adjacent items.
       Location:     Event navigation tabs: 'Highlights', 'Stack Trace', 'Replay', 'Breadcrumbs', 'Logs', 'Trace…' horizontal tab bar
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [07] Ambiguous 'Seer' AI feature labeling
       Required fix: Rename 'Seer' to something descriptive like 'AI Root Cause Analysis' or add a subtitle. Replace 'Initial Guess' with 'AI Analysis' or 'Suggested Root Cause' with a confidence indicator to set proper expectations.
       Location:     Right sidebar — 'Seer' section with warning icon and 'Initial Guess' label
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [08] Activity feed lacks clear visual grouping
       Required fix: Use distinct background colors or left-border colors for different activity types (e.g., green for resolved, orange for archived, red for unresolved). Group related status changes and add clearer iconography to differentiate action types at a glance.
       Location:     Right sidebar — Activity section showing Unresolved/Archived status changes
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

If any checkbox above cannot be ticked YES, revise your HTML before outputting it.

====================================================================
OUTPUT REQUIREMENTS
====================================================================

- Output a single, complete, self-contained HTML file.
- All CSS must be in a <style> block in the <head> — no external stylesheets.
- No CDN links, no JavaScript frameworks, no external images.
  Use inline SVG for icons. Use data URIs only if absolutely necessary.
- Preserve all informational content (text, labels, data). Do not silently drop
  information the user needs. However: if a fix requires relocating, collapsing,
  or restructuring a UI element, you MUST do so — placing it in a better position
  is not removing it. The audit fix always wins over original placement.
- Do NOT add new features, sections, or content not present in the original.
- JavaScript is allowed only for interactions clearly present in the original.
  Keep it minimal and inline.
- The file must open in a browser as a fully working static page.
- Start your response with <!DOCTYPE html> and end the HTML with </html>.
- Do NOT reference Mobbin or any third-party design gallery. Use Saasfactor if needed.