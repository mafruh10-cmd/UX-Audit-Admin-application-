You are a senior UI/UX designer and expert frontend developer.

I am giving you a screenshot of a UI screen and a detailed UX audit report.
Your task is to produce a complete, self-contained HTML redesign (version 2)
that resolves every issue in the audit while preserving the original visual identity.

CRITICAL RULE — READ BEFORE ANYTHING ELSE:
Fixing an issue takes absolute priority over preserving layout or placement.
If a fix requires moving an element, restructuring a section, hiding a component,
or changing how content is presented — DO IT. The audit findings are non-negotiable.
Preservation means keeping the brand identity (colors, typography, component style).
It does NOT mean keeping broken elements in the same broken position.

====================================================================
MANDATORY FIX CHECKLIST  —  VERIFY EACH BEFORE WRITING A SINGLE LINE OF HTML
====================================================================

Before you write the HTML, mentally confirm that your design addresses EVERY item
in this list. If your HTML does not contain a concrete, visible solution for a
High or Medium issue, your response is incomplete — revise it until it does.
Some fixes require structural changes (relocating, collapsing, or restructuring
UI elements). Make those structural changes — do not style over them.

HIGH PRIORITY  (must be resolved — zero exceptions):
  NOTE: 'fixing' means the issue is GONE from the redesign — not styled differently.
  If the fix says to move, collapse, or restructure something, do exactly that.

  [01] Content calendar is rendered as an image of text
       Problem:          The entire content calendar table appears to be rendered as an image rather than semantic HTML. Screen readers cannot parse the table structure, rows, columns, or cell content. Users cannot select, copy, or interact with individual cell text.
       What to implement: Render the calendar as a semantic HTML <table> with proper <th> headers, <tr> rows, and <td> cells. Include a <caption> element describing the table. This also enables responsive behavior on smaller screens.
       Affects:          Main content area — Content Calendar table for March 2026
       WCAG:             1.4.5 Level AA

  [02] Multiple spelling errors in AI-generated calendar content
       Problem:          The calendar contains at least five typographical errors: 'Photd' (Photo), 'Behind Ive scener' (Behind the scenes), 'Tentmonial' (Testimonial), 'Tips and Tucks' (Tips and Tricks), and 'Triday' (Friday). These errors severely undermine trust in the AI tool and the product's professionalism.
       What to implement: Implement spell-checking and content validation on AI-generated outputs before rendering. Display a disclaimer or allow inline editing of generated content. Consider a review step before finalizing the calendar.
       Affects:          Content Calendar table cells — 'Photd', 'Ive scener', 'Tentmonial', 'Tucks', 'Triday'

  [03] Download and thumbs-up icon buttons lack accessible labels
       Problem:          The two icon-only buttons beneath the calendar (download and like/feedback) have no visible text labels or apparent aria-labels. Screen reader users cannot determine the purpose of these interactive controls.
       What to implement: Add aria-label attributes such as 'Download calendar' and 'Rate this response' to each button. Consider adding visible tooltip text on hover/focus to aid sighted users as well.
       Affects:          Below the calendar table — download icon and thumbs-up icon
       WCAG:             1.1.1 Level A

MEDIUM PRIORITY  (must be resolved — no exceptions):
  NOTE: Same rule — fix means the issue does not exist in the output HTML.

  [04] Tag 'salmobbin' appears to be a misspelling or unclear label
       Problem:          The tag 'salmobbin' in the input field appears to be a misspelled or unclear label that provides no recognizable meaning to the user. Users cannot understand what this tag represents or why it was applied, creating confusion about the input context.
       What to implement: Ensure tag labels are human-readable and meaningful (e.g., a brand name or content category). If auto-generated, validate against known terms. Provide a tooltip explaining the tag's purpose on hover.
       Affects:          Chat input area — 'salmobbin' tag chip

  [05] Insufficient contrast on table header text against white background
       Problem:          The bold header text in the table uses a dark color on a white/light gray background, but some cell text within the table body may not meet the 4.5:1 minimum contrast ratio, especially the lighter-weight body text. The overall table rendering as an image also prevents zoom-based contrast improvements.
       What to implement: Ensure all table text meets a minimum 4.5:1 contrast ratio against its background. Convert the image-based table to HTML text so users can apply custom styles or zoom without degradation.
       Affects:          Content Calendar table header row — 'Monday', 'Tuesday', etc.
       WCAG:             1.4.3 Level AA

  [06] No visible focus indicators on interactive elements
       Problem:          There are no visible focus indicators on any interactive elements in the screenshot. Keyboard users navigating through the sidebar, input field, or action buttons would have no visual indication of which element is currently focused.
       What to implement: Add a visible focus ring (e.g., 2px solid blue outline with offset) to all focusable elements. Ensure the focus indicator has at least a 3:1 contrast ratio against adjacent colors per WCAG 2.4.13.
       Affects:          Sidebar navigation items, input field, icon buttons
       WCAG:             2.4.7 Level AA

  [07] Calendar lacks any editing or interaction affordances
       Problem:          The generated calendar appears to be a static image with no way for users to edit individual cells, rearrange content types, or customize the output. Users must regenerate the entire calendar to make any changes, creating an inefficient workflow.
       What to implement: Make the calendar an editable table where users can click cells to modify content types. Add drag-and-drop functionality for rearranging items. Provide an 'Edit' button that converts the static view into an editable mode.
       Affects:          Content Calendar table area

  [08] Input placeholder text disappears without persistent label
       Problem:          The placeholder text 'Describe the image you want to create' serves as the only label for the input field. Once a user begins typing, this instructional text disappears, leaving no persistent label to remind users of the field's purpose. Additionally, the placeholder says 'image' but the feature generates content calendars, creating a mismatch.
       What to implement: Add a persistent visible label above the input field (e.g., 'Ask OwlyGPT'). Update the placeholder to accurately reflect the tool's capabilities: 'Describe what you want to create — calendars, posts, images, and more.'
       Affects:          Chat input field — 'Describe the image you want to create'
       WCAG:             1.3.1 Level A

LOW PRIORITY  (implement where possible):
  [09] Sidebar active state relies primarily on color/fill change — Add a secondary indicator such as a left border accent bar, bold text label, or distinct shape change to reinforce the active state. Ensure at least 3:1 contrast between active and inactive states.
  [10] No confirmation or success feedback after download action — Add a tooltip showing 'Download as PNG' (or relevant format) on hover. After clicking, display a brief toast notification confirming 'Calendar downloaded successfully.' Consider offering format options (PNG, CSV, PDF).

====================================================================
STEP 1 — LAYOUT & COMPOSITION  (reason through this BEFORE anything else)
====================================================================

Layout and content distribution are non-negotiable design decisions.
Do not assume the original layout is correct — reason from first principles.
Ask yourself: does each element sit in the right zone for its purpose?
If not, move it. The composition must serve the user's primary goal.

1. VISUAL HIERARCHY
   The most important action or information must be visually dominant.
   Use size, weight, contrast, and position to establish a clear reading order.
   Secondary content must be visually subordinate — smaller, lighter, lower.
   (Malewicz — Designing User Interfaces: contrast creates hierarchy)
   (Refactoring UI: use size and weight to communicate importance, not just color)

2. F-PATTERN & READING FLOW
   Users scan left-to-right, top-to-bottom in an F-shape on content-heavy screens.
   Place primary actions and key labels at the start of scan lines.
   Never interrupt the scan path with secondary, promotional, or personalisation content.
   (Nielsen Norman Group — F-Pattern reading behavior)

3. PROXIMITY & GROUPING (Gestalt)
   Related elements must be spatially close. Unrelated elements need clear separation.
   If two elements answer different user questions, they belong in different zones.
   Never mix contextual content with navigational content in the same region.
   (Psych 101 — Gestalt: Law of Proximity, Law of Similarity)

4. CONTENT PRIORITISATION
   Identify the user's single primary goal on this screen.
   Every element that does not serve that goal directly must be secondary in
   visual weight and position. Upsell, personalisation, and promotional content
   must never compete with or interrupt the primary task flow.
   (Don't Make Me Think — Krug: do not make users think about what to do next)
   (Psychology of Design — Hick's Law: fewer competing choices = faster decisions)

5. SPATIAL ZONES
   Divide the screen into logical zones: navigation, primary content, secondary
   context, actions. Each zone has exactly one job.
   If an element is in the wrong zone, move it to the correct zone or defer it
   to a modal or drawer. The layout must match the user's mental model of the task.
   (Universal Principles of Design — Progressive Disclosure)

6. SIGNAL-TO-NOISE
   Every element must earn its place. If an element does not directly help the
   user complete their current task, reduce its visual weight or remove it from
   the primary viewport. Clutter is a UX failure, not a neutral default.
   (Universal Principles of Design — Signal-to-Noise Ratio)

COMPOSITION RULE: If the original layout violates these principles — with or
without an explicit audit finding — correct it. Do not replicate bad composition
because it existed in the screenshot. A version 2 must be compositionally sound.

APPLY THESE COMPOSITION DECISIONS TO RESOLVE THE FOLLOWING AUDIT ISSUES:
Every High and Medium issue in the MANDATORY FIX CHECKLIST above must map to a
specific, visible structural decision in your layout. Before moving to Step 2,
trace each issue to a layout zone, spacing decision, or hierarchy change.
If the fix requires restructuring — restructure. If it requires removing an element
from the primary viewport — remove it. Composition fixes are not optional.

====================================================================
STEP 2 — VISUAL IDENTITY  (preserve brand, not broken placement)
====================================================================

Once composition is decided, preserve these brand elements exactly:
  - Colors: every background, text, brand, accent, and surface hex value
  - Typography: font families, sizes, weights, line-heights, letter-spacing
  - Spacing scale: the relative rhythm of padding/margin (not the positions)
  - Shapes: border-radius, border styles, shadows, elevation
  - Icons: style, weight, and size (placement may change if composition requires)
  - Components: card designs, button styles, badge styles, input styles

NOTE: Layout and element positioning are governed by Step 1, not this list.
Preserving the brand does NOT mean preserving where broken things sit.
The output must feel like a version 2 — same brand, better UX.

====================================================================
FULL AUDIT REPORT  —  OwlyGPT Content Calendar Generation
====================================================================
Overall Score:        5.8/10  —  Fair
Accessibility Score:  4.5/10  (WCAG 2.2 AA)
Issues Found:         10  (3 High · 5 Medium · 2 Low)

EXECUTIVE SUMMARY
The OwlyGPT content calendar generation screen suffers from multiple typographical errors that undermine credibility, a table rendered as an image of text which creates significant accessibility barriers, and unclear interaction patterns in the input area. While the overall layout is clean and the sidebar navigation is well-organized, the AI-generated content quality and presentation need substantial improvement.

ALL ISSUES  (full detail for reference):

[01] HIGH  —  Content calendar is rendered as an image of text
     Location:  Main content area — Content Calendar table for March 2026
     Problem:   The entire content calendar table appears to be rendered as an image rather than semantic HTML. Screen readers cannot parse the table structure, rows, columns, or cell content. Users cannot select, copy, or interact with individual cell text.
     Fix:       Render the calendar as a semantic HTML <table> with proper <th> headers, <tr> rows, and <td> cells. Include a <caption> element describing the table. This also enables responsive behavior on smaller screens.
     Source:    WCAG 2.2 — 1.4.5 Images of Text (AA): text should be used to convey information rather than images of text
     WCAG:      1.4.5  Level AA

[02] HIGH  —  Multiple spelling errors in AI-generated calendar content
     Location:  Content Calendar table cells — 'Photd', 'Ive scener', 'Tentmonial', 'Tucks', 'Triday'
     Problem:   The calendar contains at least five typographical errors: 'Photd' (Photo), 'Behind Ive scener' (Behind the scenes), 'Tentmonial' (Testimonial), 'Tips and Tucks' (Tips and Tricks), and 'Triday' (Friday). These errors severely undermine trust in the AI tool and the product's professionalism.
     Fix:       Implement spell-checking and content validation on AI-generated outputs before rendering. Display a disclaimer or allow inline editing of generated content. Consider a review step before finalizing the calendar.
     Source:    Universal Principles of Design — Aesthetic-Usability Effect: beautiful things are perceived as easier to use

[03] HIGH  —  Download and thumbs-up icon buttons lack accessible labels
     Location:  Below the calendar table — download icon and thumbs-up icon
     Problem:   The two icon-only buttons beneath the calendar (download and like/feedback) have no visible text labels or apparent aria-labels. Screen reader users cannot determine the purpose of these interactive controls.
     Fix:       Add aria-label attributes such as 'Download calendar' and 'Rate this response' to each button. Consider adding visible tooltip text on hover/focus to aid sighted users as well.
     Source:    WCAG 2.2 — 1.1.1 Non-text Content (A): all non-text content must have a text alternative
     WCAG:      1.1.1  Level A

[04] MEDIUM  —  Tag 'salmobbin' appears to be a misspelling or unclear label
     Location:  Chat input area — 'salmobbin' tag chip
     Problem:   The tag 'salmobbin' in the input field appears to be a misspelled or unclear label that provides no recognizable meaning to the user. Users cannot understand what this tag represents or why it was applied, creating confusion about the input context.
     Fix:       Ensure tag labels are human-readable and meaningful (e.g., a brand name or content category). If auto-generated, validate against known terms. Provide a tooltip explaining the tag's purpose on hover.
     Source:    The Design of Everyday Things (Norman) — Signifier: visible cues must indicate where to act

[05] MEDIUM  —  Insufficient contrast on table header text against white background
     Location:  Content Calendar table header row — 'Monday', 'Tuesday', etc.
     Problem:   The bold header text in the table uses a dark color on a white/light gray background, but some cell text within the table body may not meet the 4.5:1 minimum contrast ratio, especially the lighter-weight body text. The overall table rendering as an image also prevents zoom-based contrast improvements.
     Fix:       Ensure all table text meets a minimum 4.5:1 contrast ratio against its background. Convert the image-based table to HTML text so users can apply custom styles or zoom without degradation.
     Source:    WCAG 2.2 — 1.4.3 Contrast (Minimum) (AA): normal text requires 4.5:1 contrast ratio
     WCAG:      1.4.3  Level AA

[06] MEDIUM  —  No visible focus indicators on interactive elements
     Location:  Sidebar navigation items, input field, icon buttons
     Problem:   There are no visible focus indicators on any interactive elements in the screenshot. Keyboard users navigating through the sidebar, input field, or action buttons would have no visual indication of which element is currently focused.
     Fix:       Add a visible focus ring (e.g., 2px solid blue outline with offset) to all focusable elements. Ensure the focus indicator has at least a 3:1 contrast ratio against adjacent colors per WCAG 2.4.13.
     Source:    WCAG 2.2 — 2.4.7 Focus Visible (AA): keyboard focus indicator must be visible
     WCAG:      2.4.7  Level AA

[07] MEDIUM  —  Calendar lacks any editing or interaction affordances
     Location:  Content Calendar table area
     Problem:   The generated calendar appears to be a static image with no way for users to edit individual cells, rearrange content types, or customize the output. Users must regenerate the entire calendar to make any changes, creating an inefficient workflow.
     Fix:       Make the calendar an editable table where users can click cells to modify content types. Add drag-and-drop functionality for rearranging items. Provide an 'Edit' button that converts the static view into an editable mode.
     Source:    The Design of Everyday Things (Norman) — Gulf of Execution: user cannot figure out how to do what they want

[08] MEDIUM  —  Input placeholder text disappears without persistent label
     Location:  Chat input field — 'Describe the image you want to create'
     Problem:   The placeholder text 'Describe the image you want to create' serves as the only label for the input field. Once a user begins typing, this instructional text disappears, leaving no persistent label to remind users of the field's purpose. Additionally, the placeholder says 'image' but the feature generates content calendars, creating a mismatch.
     Fix:       Add a persistent visible label above the input field (e.g., 'Ask OwlyGPT'). Update the placeholder to accurately reflect the tool's capabilities: 'Describe what you want to create — calendars, posts, images, and more.'
     Source:    WCAG 2.2 — 1.3.1 Info and Relationships (A): information and relationships conveyed visually must be programmatically determinable
     WCAG:      1.3.1  Level A

[09] LOW  —  Sidebar active state relies primarily on color/fill change
     Location:  Left sidebar — OwlyGPT nav item (highlighted with dark background)
     Problem:   The active navigation item (OwlyGPT) is indicated primarily through a filled dark background circle, while inactive items use outline-style icons. The differentiation is subtle and may not be perceivable by users with color vision deficiencies or low vision.
     Fix:       Add a secondary indicator such as a left border accent bar, bold text label, or distinct shape change to reinforce the active state. Ensure at least 3:1 contrast between active and inactive states.
     Source:    WCAG 2.2 — 1.4.1 Use of Color (A): color is not the only visual means of conveying information
     WCAG:      1.4.1  Level A

[10] LOW  —  No confirmation or success feedback after download action
     Location:  Download icon button below the calendar
     Problem:   The download button provides no visible indication of what will happen when clicked (file format, destination) and likely provides no confirmation feedback after the action is completed. Users cannot tell what they're downloading or if it succeeded.
     Fix:       Add a tooltip showing 'Download as PNG' (or relevant format) on hover. After clicking, display a brief toast notification confirming 'Calendar downloaded successfully.' Consider offering format options (PNG, CSV, PDF).
     Source:    The Design of Everyday Things (Norman) — Feedback: every action must produce an immediate, clear response

====================================================================
SELF-CHECK — RUN THIS BEFORE OUTPUTTING THE HTML
====================================================================

For each item below, verify the SPECIFIC FIX is concretely implemented —
not just acknowledged, not just styled differently. The fix must be visible.

  [ ] [01] Content calendar is rendered as an image of text
       Required fix: Render the calendar as a semantic HTML <table> with proper <th> headers, <tr> rows, and <td> cells. Include a <caption> element describing the table. This also enables responsive behavior on smaller screens.
       Location:     Main content area — Content Calendar table for March 2026
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [02] Multiple spelling errors in AI-generated calendar content
       Required fix: Implement spell-checking and content validation on AI-generated outputs before rendering. Display a disclaimer or allow inline editing of generated content. Consider a review step before finalizing the calendar.
       Location:     Content Calendar table cells — 'Photd', 'Ive scener', 'Tentmonial', 'Tucks', 'Triday'
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [03] Download and thumbs-up icon buttons lack accessible labels
       Required fix: Add aria-label attributes such as 'Download calendar' and 'Rate this response' to each button. Consider adding visible tooltip text on hover/focus to aid sighted users as well.
       Location:     Below the calendar table — download icon and thumbs-up icon
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [04] Tag 'salmobbin' appears to be a misspelling or unclear label
       Required fix: Ensure tag labels are human-readable and meaningful (e.g., a brand name or content category). If auto-generated, validate against known terms. Provide a tooltip explaining the tag's purpose on hover.
       Location:     Chat input area — 'salmobbin' tag chip
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [05] Insufficient contrast on table header text against white background
       Required fix: Ensure all table text meets a minimum 4.5:1 contrast ratio against its background. Convert the image-based table to HTML text so users can apply custom styles or zoom without degradation.
       Location:     Content Calendar table header row — 'Monday', 'Tuesday', etc.
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [06] No visible focus indicators on interactive elements
       Required fix: Add a visible focus ring (e.g., 2px solid blue outline with offset) to all focusable elements. Ensure the focus indicator has at least a 3:1 contrast ratio against adjacent colors per WCAG 2.4.13.
       Location:     Sidebar navigation items, input field, icon buttons
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [07] Calendar lacks any editing or interaction affordances
       Required fix: Make the calendar an editable table where users can click cells to modify content types. Add drag-and-drop functionality for rearranging items. Provide an 'Edit' button that converts the static view into an editable mode.
       Location:     Content Calendar table area
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [08] Input placeholder text disappears without persistent label
       Required fix: Add a persistent visible label above the input field (e.g., 'Ask OwlyGPT'). Update the placeholder to accurately reflect the tool's capabilities: 'Describe what you want to create — calendars, posts, images, and more.'
       Location:     Chat input field — 'Describe the image you want to create'
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

If any checkbox above cannot be ticked YES, revise your HTML before outputting it.

====================================================================
OUTPUT REQUIREMENTS
====================================================================

- Output a single, complete, self-contained HTML file.
- All CSS must be in a <style> block in the <head> — no external stylesheets.
- No CDN links, no JavaScript frameworks, no external images.
  Use inline SVG for icons. Use data URIs only if absolutely necessary.
- Preserve all informational content (text, labels, data). Do not silently drop
  information the user needs. However: if a fix requires relocating, collapsing,
  or restructuring a UI element, you MUST do so — placing it in a better position
  is not removing it. The audit fix always wins over original placement.
- Do NOT add new features, sections, or content not present in the original.
- JavaScript is allowed only for interactions clearly present in the original.
  Keep it minimal and inline.
- The file must open in a browser as a fully working static page.
- Start your response with <!DOCTYPE html> and end the HTML with </html>.
- Do NOT reference Mobbin or any third-party design gallery. Use Saasfactor if needed.