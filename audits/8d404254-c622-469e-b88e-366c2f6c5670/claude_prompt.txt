You are a senior UI/UX designer and expert frontend developer.

I am giving you a screenshot of a UI screen and a detailed UX audit report.
Your task is to produce a complete, self-contained HTML redesign (version 2)
that resolves every issue in the audit while preserving the original visual identity.

CRITICAL RULE — READ BEFORE ANYTHING ELSE:
Fixing an issue takes absolute priority over preserving layout or placement.
If a fix requires moving an element, restructuring a section, hiding a component,
or changing how content is presented — DO IT. The audit findings are non-negotiable.
Preservation means keeping the brand identity (colors, typography, component style).
It does NOT mean keeping broken elements in the same broken position.

====================================================================
MANDATORY FIX CHECKLIST  —  VERIFY EACH BEFORE WRITING A SINGLE LINE OF HTML
====================================================================

Before you write the HTML, mentally confirm that your design addresses EVERY item
in this list. If your HTML does not contain a concrete, visible solution for a
High or Medium issue, your response is incomplete — revise it until it does.
Some fixes require structural changes (relocating, collapsing, or restructuring
UI elements). Make those structural changes — do not style over them.

HIGH PRIORITY  (must be resolved — zero exceptions):
  NOTE: 'fixing' means the issue is GONE from the redesign — not styled differently.
  If the fix says to move, collapse, or restructure something, do exactly that.

  [01] Status badges rely on color alone to convey meaning
       Problem:          The 'Active' status is conveyed solely through a green color badge with no supporting icon or pattern. Users with color vision deficiencies cannot distinguish active from other statuses. This affects all four project cards in the top row.
       What to implement: Add a secondary indicator such as an icon (e.g., a checkmark or dot with a border pattern) alongside the color. Consider adding a distinct shape or text styling treatment so status is perceivable without color.
       Affects:          Status: Active badges on the project cards (top row)
       WCAG:             1.4.1 Level A

  [02] Low contrast on secondary text throughout dashboard
       Problem:          Multiple secondary text elements appear in a light gray that likely fails the 4.5:1 contrast ratio against the white background. This includes creator attribution, descriptions on cards, and metric subtitles. This makes the text difficult to read for users with low vision or in bright environments.
       What to implement: Increase the text color to at least #767676 or darker to achieve the 4.5:1 minimum contrast ratio. Verify all secondary text elements with a contrast checker tool and establish a design token for secondary text that meets AA requirements.
       Affects:          Gray metadata text such as 'Created by Alex Smith', 'Collections added', 'Not shared • 6 minutes ago • 1 attribute'
       WCAG:             1.4.3 Level AA

  [03] Truncated card descriptions hide critical information
       Problem:          Card descriptions are truncated mid-word with no tooltip or expand mechanism visible. Users cannot access the full search criteria without clicking through, which forces unnecessary navigation and increases cognitive load when comparing projects.
       What to implement: Add a tooltip on hover showing the full description text, or increase the card height to accommodate 2-3 lines. Alternatively, provide an expand/collapse mechanism inline to allow quick scanning without navigation.
       Affects:          Project cards in the top row — 'Looking for a Senior Software Engineer with 5...' and 'Product Designer with 3+ years of experience i...'

  [04] No visible focus indicators for interactive elements
       Problem:          There are no visible focus indicators shown on any interactive elements in the interface. Keyboard-only users cannot determine which element currently has focus, making the dashboard essentially unusable without a mouse.
       What to implement: Implement a visible focus ring (at least 2px solid outline in a contrasting color) on all focusable elements including cards, links, buttons, and list items. Use CSS :focus-visible to avoid showing focus rings on mouse clicks while maintaining them for keyboard navigation.
       Affects:          All interactive elements — navigation links, project cards, candidate rows, buttons
       WCAG:             2.4.7 Level AA

MEDIUM PRIORITY  (must be resolved — no exceptions):
  NOTE: Same rule — fix means the issue does not exist in the output HTML.

  [05] Bar chart lacks text alternatives and axis labels
       Problem:          The bar chart showing '106 Profiles added today' has minimal axis labeling — only dates on the x-axis with no y-axis values. Screen reader users would have no way to interpret the data, and sighted users cannot determine exact values for each bar.
       What to implement: Provide an accessible data table as an alternative or use ARIA labels to describe the chart data. Add y-axis labels with values, and implement tooltips on hover showing exact daily counts for each bar.
       Affects:          Profiles added today bar chart (right side of dashboard)
       WCAG:             1.1.1 Level A

  [06] Candidate list items lack clear clickable affordance
       Problem:          The candidate rows show a subtle chevron (>) on the right but lack hover states, underlines, or other conventional affordances to indicate interactivity. Users may not immediately recognize these as clickable elements, reducing discoverability.
       What to implement: Add a visible hover state with a background color change or subtle elevation. Consider adding a text link style (e.g., 'View profile') or make the entire row more obviously interactive with cursor changes and visual feedback on hover.
       Affects:          Candidate list on the left side (Claire G., Robert Barker, etc.)

  [07] Metrics section lacks contextual meaning and actionability
       Problem:          The four metric cards present raw numbers without trends, goals, or comparisons. The '0 No emails sent yet' metric provides no guidance on next steps. Users cannot determine if these numbers represent good or poor performance without context.
       What to implement: Add trend indicators (up/down arrows with percentages), comparison to previous period, or progress toward a goal. For the '0 emails' metric, add a CTA like 'Start your first outreach campaign' to guide the user toward meaningful action.
       Affects:          Summary stats row — Total Lists (3), Candidates Saved (203), Total Searches (9), Candidates Outreached (0)

  [08] Trial notification competes with primary navigation
       Problem:          The trial notification is placed directly above important navigation items (Invite team, Knowledge hub, Account settings), creating visual clutter and potentially obscuring navigation on smaller screens. The green progress bar draws attention away from the primary dashboard content.
       What to implement: Move the trial notification to a less intrusive location such as a top banner or integrate it into Account settings. Alternatively, make it dismissible so users can remove it once acknowledged. Reduce visual weight by using a subtler design.
       Affects:          Bottom-left trial banner — 'Your Wrangle trial is active'

  [09] Horizontal card scrolling hides content with no visible scroll indicator
       Problem:          The fourth project card is truncated at the right edge of the viewport with no visible scroll indicator, pagination dots, or arrow buttons. Users may not realize there are additional cards to view, leading to missed content and incomplete awareness of their projects.
       What to implement: Add left/right navigation arrows at the edges of the card container, or include pagination dots below the cards. Alternatively, show a partial fade effect or 'peek' of the next card to signal scrollability. Consider a 'View all' link if there are many projects.
       Affects:          Top project cards row — fourth card 'Product Designer' is cut off at the right edge

LOW PRIORITY  (implement where possible):
  [10] Inconsistent card metadata labeling across project types — Standardize the card layout so all cards show the same fields in the same order: type badge, description, status, candidate count (with consistent terminology), sharing status, and creator. Use a consistent label like 'X candidates' regardless of their status.

====================================================================
STEP 1 — LAYOUT & COMPOSITION  (reason through this BEFORE anything else)
====================================================================

Layout and content distribution are non-negotiable design decisions.
Do not assume the original layout is correct — reason from first principles.
Ask yourself: does each element sit in the right zone for its purpose?
If not, move it. The composition must serve the user's primary goal.

1. VISUAL HIERARCHY
   The most important action or information must be visually dominant.
   Use size, weight, contrast, and position to establish a clear reading order.
   Secondary content must be visually subordinate — smaller, lighter, lower.
   (Malewicz — Designing User Interfaces: contrast creates hierarchy)
   (Refactoring UI: use size and weight to communicate importance, not just color)

2. F-PATTERN & READING FLOW
   Users scan left-to-right, top-to-bottom in an F-shape on content-heavy screens.
   Place primary actions and key labels at the start of scan lines.
   Never interrupt the scan path with secondary, promotional, or personalisation content.
   (Nielsen Norman Group — F-Pattern reading behavior)

3. PROXIMITY & GROUPING (Gestalt)
   Related elements must be spatially close. Unrelated elements need clear separation.
   If two elements answer different user questions, they belong in different zones.
   Never mix contextual content with navigational content in the same region.
   (Psych 101 — Gestalt: Law of Proximity, Law of Similarity)

4. CONTENT PRIORITISATION
   Identify the user's single primary goal on this screen.
   Every element that does not serve that goal directly must be secondary in
   visual weight and position. Upsell, personalisation, and promotional content
   must never compete with or interrupt the primary task flow.
   (Don't Make Me Think — Krug: do not make users think about what to do next)
   (Psychology of Design — Hick's Law: fewer competing choices = faster decisions)

5. SPATIAL ZONES
   Divide the screen into logical zones: navigation, primary content, secondary
   context, actions. Each zone has exactly one job.
   If an element is in the wrong zone, move it to the correct zone or defer it
   to a modal or drawer. The layout must match the user's mental model of the task.
   (Universal Principles of Design — Progressive Disclosure)

6. SIGNAL-TO-NOISE
   Every element must earn its place. If an element does not directly help the
   user complete their current task, reduce its visual weight or remove it from
   the primary viewport. Clutter is a UX failure, not a neutral default.
   (Universal Principles of Design — Signal-to-Noise Ratio)

COMPOSITION RULE: If the original layout violates these principles — with or
without an explicit audit finding — correct it. Do not replicate bad composition
because it existed in the screenshot. A version 2 must be compositionally sound.

APPLY THESE COMPOSITION DECISIONS TO RESOLVE THE FOLLOWING AUDIT ISSUES:
Every High and Medium issue in the MANDATORY FIX CHECKLIST above must map to a
specific, visible structural decision in your layout. Before moving to Step 2,
trace each issue to a layout zone, spacing decision, or hierarchy change.
If the fix requires restructuring — restructure. If it requires removing an element
from the primary viewport — remove it. Composition fixes are not optional.

====================================================================
STEP 2 — VISUAL IDENTITY  (preserve brand, not broken placement)
====================================================================

Once composition is decided, preserve these brand elements exactly:
  - Colors: every background, text, brand, accent, and surface hex value
  - Typography: font families, sizes, weights, line-heights, letter-spacing
  - Spacing scale: the relative rhythm of padding/margin (not the positions)
  - Shapes: border-radius, border styles, shadows, elevation
  - Icons: style, weight, and size (placement may change if composition requires)
  - Components: card designs, button styles, badge styles, input styles

NOTE: Layout and element positioning are governed by Step 1, not this list.
Preserving the brand does NOT mean preserving where broken things sit.
The output must feel like a version 2 — same brand, better UX.

====================================================================
FULL AUDIT REPORT  —  Dashboard
====================================================================
Overall Score:        6.8/10  —  Good
Accessibility Score:  5.5/10  (WCAG 2.2 AA)
Issues Found:         10  (4 High · 5 Medium · 1 Low)

EXECUTIVE SUMMARY
Wrangle's dashboard provides a solid overview of recruitment activity with clear metrics and candidate lists. However, several accessibility issues around color contrast, color-only status indicators, and truncated content hurt usability. The information density is high but could benefit from better visual hierarchy and clearer interactive affordances.

ALL ISSUES  (full detail for reference):

[01] HIGH  —  Status badges rely on color alone to convey meaning
     Location:  Status: Active badges on the project cards (top row)
     Problem:   The 'Active' status is conveyed solely through a green color badge with no supporting icon or pattern. Users with color vision deficiencies cannot distinguish active from other statuses. This affects all four project cards in the top row.
     Fix:       Add a secondary indicator such as an icon (e.g., a checkmark or dot with a border pattern) alongside the color. Consider adding a distinct shape or text styling treatment so status is perceivable without color.
     Source:    WCAG 2.2 — 1.4.1 Use of Color (A): information must not be conveyed by color alone
     WCAG:      1.4.1  Level A

[02] HIGH  —  Low contrast on secondary text throughout dashboard
     Location:  Gray metadata text such as 'Created by Alex Smith', 'Collections added', 'Not shared • 6 minutes ago • 1 attribute'
     Problem:   Multiple secondary text elements appear in a light gray that likely fails the 4.5:1 contrast ratio against the white background. This includes creator attribution, descriptions on cards, and metric subtitles. This makes the text difficult to read for users with low vision or in bright environments.
     Fix:       Increase the text color to at least #767676 or darker to achieve the 4.5:1 minimum contrast ratio. Verify all secondary text elements with a contrast checker tool and establish a design token for secondary text that meets AA requirements.
     Source:    WCAG 2.2 — 1.4.3 Contrast Minimum (AA): normal text requires 4.5:1 contrast ratio
     WCAG:      1.4.3  Level AA

[03] HIGH  —  Truncated card descriptions hide critical information
     Location:  Project cards in the top row — 'Looking for a Senior Software Engineer with 5...' and 'Product Designer with 3+ years of experience i...'
     Problem:   Card descriptions are truncated mid-word with no tooltip or expand mechanism visible. Users cannot access the full search criteria without clicking through, which forces unnecessary navigation and increases cognitive load when comparing projects.
     Fix:       Add a tooltip on hover showing the full description text, or increase the card height to accommodate 2-3 lines. Alternatively, provide an expand/collapse mechanism inline to allow quick scanning without navigation.
     Source:    The Design of Everyday Things (Norman) — Gulf of Evaluation: user cannot tell if their action worked

[04] HIGH  —  No visible focus indicators for interactive elements
     Location:  All interactive elements — navigation links, project cards, candidate rows, buttons
     Problem:   There are no visible focus indicators shown on any interactive elements in the interface. Keyboard-only users cannot determine which element currently has focus, making the dashboard essentially unusable without a mouse.
     Fix:       Implement a visible focus ring (at least 2px solid outline in a contrasting color) on all focusable elements including cards, links, buttons, and list items. Use CSS :focus-visible to avoid showing focus rings on mouse clicks while maintaining them for keyboard navigation.
     Source:    WCAG 2.2 — 2.4.7 Focus Visible (AA): keyboard focus indicator must be visible
     WCAG:      2.4.7  Level AA

[05] MEDIUM  —  Bar chart lacks text alternatives and axis labels
     Location:  Profiles added today bar chart (right side of dashboard)
     Problem:   The bar chart showing '106 Profiles added today' has minimal axis labeling — only dates on the x-axis with no y-axis values. Screen reader users would have no way to interpret the data, and sighted users cannot determine exact values for each bar.
     Fix:       Provide an accessible data table as an alternative or use ARIA labels to describe the chart data. Add y-axis labels with values, and implement tooltips on hover showing exact daily counts for each bar.
     Source:    WCAG 2.2 — 1.1.1 Non-text Content (A): all non-text content must have a text alternative
     WCAG:      1.1.1  Level A

[06] MEDIUM  —  Candidate list items lack clear clickable affordance
     Location:  Candidate list on the left side (Claire G., Robert Barker, etc.)
     Problem:   The candidate rows show a subtle chevron (>) on the right but lack hover states, underlines, or other conventional affordances to indicate interactivity. Users may not immediately recognize these as clickable elements, reducing discoverability.
     Fix:       Add a visible hover state with a background color change or subtle elevation. Consider adding a text link style (e.g., 'View profile') or make the entire row more obviously interactive with cursor changes and visual feedback on hover.
     Source:    The Design of Everyday Things (Norman) — Signifier: visible cues must indicate where to act

[07] MEDIUM  —  Metrics section lacks contextual meaning and actionability
     Location:  Summary stats row — Total Lists (3), Candidates Saved (203), Total Searches (9), Candidates Outreached (0)
     Problem:   The four metric cards present raw numbers without trends, goals, or comparisons. The '0 No emails sent yet' metric provides no guidance on next steps. Users cannot determine if these numbers represent good or poor performance without context.
     Fix:       Add trend indicators (up/down arrows with percentages), comparison to previous period, or progress toward a goal. For the '0 emails' metric, add a CTA like 'Start your first outreach campaign' to guide the user toward meaningful action.
     Source:    Universal Principles of Design — Signal-to-Noise Ratio: maximise relevant information, minimise clutter

[08] MEDIUM  —  Trial notification competes with primary navigation
     Location:  Bottom-left trial banner — 'Your Wrangle trial is active'
     Problem:   The trial notification is placed directly above important navigation items (Invite team, Knowledge hub, Account settings), creating visual clutter and potentially obscuring navigation on smaller screens. The green progress bar draws attention away from the primary dashboard content.
     Fix:       Move the trial notification to a less intrusive location such as a top banner or integrate it into Account settings. Alternatively, make it dismissible so users can remove it once acknowledged. Reduce visual weight by using a subtler design.
     Source:    Psych 101 (Kleinman) — Gestalt: Law of Proximity

[09] MEDIUM  —  Horizontal card scrolling hides content with no visible scroll indicator
     Location:  Top project cards row — fourth card 'Product Designer' is cut off at the right edge
     Problem:   The fourth project card is truncated at the right edge of the viewport with no visible scroll indicator, pagination dots, or arrow buttons. Users may not realize there are additional cards to view, leading to missed content and incomplete awareness of their projects.
     Fix:       Add left/right navigation arrows at the edges of the card container, or include pagination dots below the cards. Alternatively, show a partial fade effect or 'peek' of the next card to signal scrollability. Consider a 'View all' link if there are many projects.
     Source:    Universal Principles of Design — Visibility: system status and available actions must be visible

[10] LOW  —  Inconsistent card metadata labeling across project types
     Location:  Project cards — 'Sourcing' vs 'Collection' labels and varying metadata formats
     Problem:   The four project cards use inconsistent metadata patterns: some show 'Sourcing' labels, one shows 'Collection'; candidates are described as 'shortlisted', 'saved', or with a count; sharing status varies. This inconsistency forces users to parse each card differently.
     Fix:       Standardize the card layout so all cards show the same fields in the same order: type badge, description, status, candidate count (with consistent terminology), sharing status, and creator. Use a consistent label like 'X candidates' regardless of their status.
     Source:    Designing User Interfaces (Malewicz) — Contrast and visual hierarchy

====================================================================
SELF-CHECK — RUN THIS BEFORE OUTPUTTING THE HTML
====================================================================

For each item below, verify the SPECIFIC FIX is concretely implemented —
not just acknowledged, not just styled differently. The fix must be visible.

  [ ] [01] Status badges rely on color alone to convey meaning
       Required fix: Add a secondary indicator such as an icon (e.g., a checkmark or dot with a border pattern) alongside the color. Consider adding a distinct shape or text styling treatment so status is perceivable without color.
       Location:     Status: Active badges on the project cards (top row)
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [02] Low contrast on secondary text throughout dashboard
       Required fix: Increase the text color to at least #767676 or darker to achieve the 4.5:1 minimum contrast ratio. Verify all secondary text elements with a contrast checker tool and establish a design token for secondary text that meets AA requirements.
       Location:     Gray metadata text such as 'Created by Alex Smith', 'Collections added', 'Not shared • 6 minutes ago • 1 attribute'
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [03] Truncated card descriptions hide critical information
       Required fix: Add a tooltip on hover showing the full description text, or increase the card height to accommodate 2-3 lines. Alternatively, provide an expand/collapse mechanism inline to allow quick scanning without navigation.
       Location:     Project cards in the top row — 'Looking for a Senior Software Engineer with 5...' and 'Product Designer with 3+ years of experience i...'
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [04] No visible focus indicators for interactive elements
       Required fix: Implement a visible focus ring (at least 2px solid outline in a contrasting color) on all focusable elements including cards, links, buttons, and list items. Use CSS :focus-visible to avoid showing focus rings on mouse clicks while maintaining them for keyboard navigation.
       Location:     All interactive elements — navigation links, project cards, candidate rows, buttons
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [05] Bar chart lacks text alternatives and axis labels
       Required fix: Provide an accessible data table as an alternative or use ARIA labels to describe the chart data. Add y-axis labels with values, and implement tooltips on hover showing exact daily counts for each bar.
       Location:     Profiles added today bar chart (right side of dashboard)
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [06] Candidate list items lack clear clickable affordance
       Required fix: Add a visible hover state with a background color change or subtle elevation. Consider adding a text link style (e.g., 'View profile') or make the entire row more obviously interactive with cursor changes and visual feedback on hover.
       Location:     Candidate list on the left side (Claire G., Robert Barker, etc.)
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [07] Metrics section lacks contextual meaning and actionability
       Required fix: Add trend indicators (up/down arrows with percentages), comparison to previous period, or progress toward a goal. For the '0 emails' metric, add a CTA like 'Start your first outreach campaign' to guide the user toward meaningful action.
       Location:     Summary stats row — Total Lists (3), Candidates Saved (203), Total Searches (9), Candidates Outreached (0)
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [08] Trial notification competes with primary navigation
       Required fix: Move the trial notification to a less intrusive location such as a top banner or integrate it into Account settings. Alternatively, make it dismissible so users can remove it once acknowledged. Reduce visual weight by using a subtler design.
       Location:     Bottom-left trial banner — 'Your Wrangle trial is active'
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [09] Horizontal card scrolling hides content with no visible scroll indicator
       Required fix: Add left/right navigation arrows at the edges of the card container, or include pagination dots below the cards. Alternatively, show a partial fade effect or 'peek' of the next card to signal scrollability. Consider a 'View all' link if there are many projects.
       Location:     Top project cards row — fourth card 'Product Designer' is cut off at the right edge
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

If any checkbox above cannot be ticked YES, revise your HTML before outputting it.

====================================================================
OUTPUT REQUIREMENTS
====================================================================

- Output a single, complete, self-contained HTML file.
- All CSS must be in a <style> block in the <head> — no external stylesheets.
- No CDN links, no JavaScript frameworks, no external images.
  Use inline SVG for icons. Use data URIs only if absolutely necessary.
- Preserve all informational content (text, labels, data). Do not silently drop
  information the user needs. However: if a fix requires relocating, collapsing,
  or restructuring a UI element, you MUST do so — placing it in a better position
  is not removing it. The audit fix always wins over original placement.
- Do NOT add new features, sections, or content not present in the original.
- JavaScript is allowed only for interactions clearly present in the original.
  Keep it minimal and inline.
- The file must open in a browser as a fully working static page.
- Start your response with <!DOCTYPE html> and end the HTML with </html>.
- Do NOT reference Mobbin or any third-party design gallery. Use Saasfactor if needed.