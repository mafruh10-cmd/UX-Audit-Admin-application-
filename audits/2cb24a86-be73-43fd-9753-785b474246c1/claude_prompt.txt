You are a senior UI/UX designer and expert frontend developer.

I am giving you a screenshot of a UI screen and a detailed UX audit report.
Your task is to produce a complete, self-contained HTML redesign (version 2)
that resolves every issue in the audit while preserving the original visual identity.

CRITICAL RULE — READ BEFORE ANYTHING ELSE:
Fixing an issue takes absolute priority over preserving layout or placement.
If a fix requires moving an element, restructuring a section, hiding a component,
or changing how content is presented — DO IT. The audit findings are non-negotiable.
Preservation means keeping the brand identity (colors, typography, component style).
It does NOT mean keeping broken elements in the same broken position.

====================================================================
MANDATORY FIX CHECKLIST  —  VERIFY EACH BEFORE WRITING A SINGLE LINE OF HTML
====================================================================

Before you write the HTML, mentally confirm that your design addresses EVERY item
in this list. If your HTML does not contain a concrete, visible solution for a
High or Medium issue, your response is incomplete — revise it until it does.
Some fixes require structural changes (relocating, collapsing, or restructuring
UI elements). Make those structural changes — do not style over them.

HIGH PRIORITY  (must be resolved — zero exceptions):
  NOTE: 'fixing' means the issue is GONE from the redesign — not styled differently.
  If the fix says to move, collapse, or restructure something, do exactly that.

  [01] Chart data series distinguished by color alone
       Problem:          The current period (solid blue line) and comparison period (dotted gray line) are differentiated primarily through color. Users with color vision deficiencies may not be able to distinguish between the two data series, leading to misinterpretation of trend data.
       What to implement: Use distinct line patterns (solid vs. dashed) with different stroke weights, and add direct labels or data markers (circles, squares) to each line. The legend already shows solid vs. dotted but ensure the actual chart lines are visually distinct enough at all sizes.
       Affects:          Line charts in Total sales, Online store sessions, Total orders, and Average order value cards
       WCAG:             1.4.1 Level A

  [02] Low contrast on secondary text and axis labels
       Problem:          The gray axis labels and date range text in chart legends appear to have insufficient contrast against the white background, likely falling below the 4.5:1 minimum ratio for normal-sized text. This makes data interpretation difficult for users with low vision.
       What to implement: Increase the text color darkness to at least #767676 or darker on white backgrounds to meet the 4.5:1 ratio. For the smaller axis labels, consider using a slightly larger font size as well to aid readability.
       Affects:          Chart axis labels (Jan 2, Jan 4, Jan 6, $0, $20, $40) and comparison period date ranges
       WCAG:             1.4.3 Level AA

  [03] No visible keyboard focus indicators
       Problem:          There are no visible focus indicators on any interactive elements in the screenshot. Keyboard-only users and assistive technology users cannot determine which element currently has focus, making the dashboard essentially unusable without a mouse.
       What to implement: Add a clearly visible focus ring (minimum 2px solid outline in a contrasting color) to all interactive elements. Use CSS :focus-visible to show focus for keyboard users while hiding it for mouse interactions.
       Affects:          All interactive elements including buttons (Last 7 days, Compare, Customize, Fullscreen), navigation items, and copy icons
       WCAG:             2.4.7 Level AA

MEDIUM PRIORITY  (must be resolved — no exceptions):
  NOTE: Same rule — fix means the issue does not exist in the output HTML.

  [04] Icon-only copy buttons lack visible text labels
       Problem:          Each card has a small icon button in the upper-right corner with no visible label or tooltip. Users must guess the button's function, and screen reader users may not have adequate context if aria-labels are missing.
       What to implement: Add a visible tooltip on hover/focus that describes the action (e.g., 'Copy chart data'). Ensure each button has an accessible name via aria-label. Consider adding a text label alongside the icon for clarity.
       Affects:          Copy/clipboard icons in the top-right corner of each metric card (Total sales, Sales by channel, Online store sessions, etc.)

  [05] Sales by channel card lacks chart or breakdown visualization
       Problem:          The Sales by channel card displays a single text value without any visual breakdown (pie chart, bar chart, or list). When there is only one channel, the card feels empty and underutilized. When multiple channels exist, a text-only presentation would not scale well.
       What to implement: Add a proportional visualization (horizontal bar or donut chart) to show channel distribution. For single-channel scenarios, display a message encouraging channel diversification or show the channel as a proportional bar at 100%.
       Affects:          Center card in top row — 'Sales by channel' showing only 'Draft Orders $34.00'

  [06] Dash indicators provide no actionable context for trend changes
       Problem:          Each KPI displays an em-dash next to the value, presumably indicating no change or insufficient data for comparison. This is ambiguous — users cannot tell if it means 0% change, no data available, or a calculation error. The lack of explanation creates uncertainty.
       What to implement: Replace the em-dash with explicit text such as '0% change', 'No comparison data', or 'N/A' with a tooltip explanation. Use color-coded arrows (↑ green, ↓ red) with percentage text when comparison data is available.
       Affects:          Em-dash (—) symbols next to metric values ($34.00 —, 2 —, 0% —, 3 —, $18.00 —)

  [07] No empty state guidance for zero-conversion funnel
       Problem:          The conversion rate card shows all zeros with no contextual help or guidance. New store owners seeing this will not understand whether their setup is incomplete, tracking is broken, or they simply have no traffic. This is a missed opportunity for onboarding support.
       What to implement: Display an empty state message like 'No sessions recorded yet. Make sure your online store is published and tracking is enabled.' Include a link to relevant setup documentation or marketing tools.
       Affects:          Online store conversion rate card showing 0% with 0 sessions across all funnel stages

  [08] Touch target size may be insufficient for small icon buttons
       Problem:          The small copy icons in each card appear to be approximately 16-20px, which is below the WCAG 2.5.8 minimum target size of 24x24px. This makes them difficult to tap on touch devices and hard to click precisely with a mouse.
       What to implement: Increase the clickable area of icon buttons to at least 44x44px (iOS guideline) or minimum 24x24px (WCAG). This can be done with padding around the icon without changing its visual size.
       Affects:          Copy/clipboard icons on each card and the Auto-refresh toggle
       WCAG:             2.5.8 Level AA

LOW PRIORITY  (implement where possible):
  [09] Date range filter lacks clear visual hierarchy as primary control — Give the date range selector slightly more visual weight — consider a filled background or a more prominent position. Ensure the 'Compare' toggle is clearly secondary. Add a subtle background highlight to indicate the active filter state.
  [10] Inconsistent card heights create visual imbalance — Normalize card heights within each row by setting a consistent minimum height, or reorganize the conversion funnel data into its own full-width section. Alternatively, collapse the funnel details behind an expandable section.

====================================================================
STEP 1 — LAYOUT & COMPOSITION  (reason through this BEFORE anything else)
====================================================================

Layout and content distribution are non-negotiable design decisions.
Do not assume the original layout is correct — reason from first principles.
Ask yourself: does each element sit in the right zone for its purpose?
If not, move it. The composition must serve the user's primary goal.

1. VISUAL HIERARCHY
   The most important action or information must be visually dominant.
   Use size, weight, contrast, and position to establish a clear reading order.
   Secondary content must be visually subordinate — smaller, lighter, lower.
   (Malewicz — Designing User Interfaces: contrast creates hierarchy)
   (Refactoring UI: use size and weight to communicate importance, not just color)

2. F-PATTERN & READING FLOW
   Users scan left-to-right, top-to-bottom in an F-shape on content-heavy screens.
   Place primary actions and key labels at the start of scan lines.
   Never interrupt the scan path with secondary, promotional, or personalisation content.
   (Nielsen Norman Group — F-Pattern reading behavior)

3. PROXIMITY & GROUPING (Gestalt)
   Related elements must be spatially close. Unrelated elements need clear separation.
   If two elements answer different user questions, they belong in different zones.
   Never mix contextual content with navigational content in the same region.
   (Psych 101 — Gestalt: Law of Proximity, Law of Similarity)

4. CONTENT PRIORITISATION
   Identify the user's single primary goal on this screen.
   Every element that does not serve that goal directly must be secondary in
   visual weight and position. Upsell, personalisation, and promotional content
   must never compete with or interrupt the primary task flow.
   (Don't Make Me Think — Krug: do not make users think about what to do next)
   (Psychology of Design — Hick's Law: fewer competing choices = faster decisions)

5. SPATIAL ZONES
   Divide the screen into logical zones: navigation, primary content, secondary
   context, actions. Each zone has exactly one job.
   If an element is in the wrong zone, move it to the correct zone or defer it
   to a modal or drawer. The layout must match the user's mental model of the task.
   (Universal Principles of Design — Progressive Disclosure)

6. SIGNAL-TO-NOISE
   Every element must earn its place. If an element does not directly help the
   user complete their current task, reduce its visual weight or remove it from
   the primary viewport. Clutter is a UX failure, not a neutral default.
   (Universal Principles of Design — Signal-to-Noise Ratio)

COMPOSITION RULE: If the original layout violates these principles — with or
without an explicit audit finding — correct it. Do not replicate bad composition
because it existed in the screenshot. A version 2 must be compositionally sound.

APPLY THESE COMPOSITION DECISIONS TO RESOLVE THE FOLLOWING AUDIT ISSUES:
Every High and Medium issue in the MANDATORY FIX CHECKLIST above must map to a
specific, visible structural decision in your layout. Before moving to Step 2,
trace each issue to a layout zone, spacing decision, or hierarchy change.
If the fix requires restructuring — restructure. If it requires removing an element
from the primary viewport — remove it. Composition fixes are not optional.

====================================================================
STEP 2 — VISUAL IDENTITY  (preserve brand, not broken placement)
====================================================================

Once composition is decided, preserve these brand elements exactly:
  - Colors: every background, text, brand, accent, and surface hex value
  - Typography: font families, sizes, weights, line-heights, letter-spacing
  - Spacing scale: the relative rhythm of padding/margin (not the positions)
  - Shapes: border-radius, border styles, shadows, elevation
  - Icons: style, weight, and size (placement may change if composition requires)
  - Components: card designs, button styles, badge styles, input styles

NOTE: Layout and element positioning are governed by Step 1, not this list.
Preserving the brand does NOT mean preserving where broken things sit.
The output must feel like a version 2 — same brand, better UX.

====================================================================
FULL AUDIT REPORT  —  Analytics Dashboard
====================================================================
Overall Score:        7.1/10  —  Good
Accessibility Score:  5.8/10  (WCAG 2.2 AA)
Issues Found:         10  (3 High · 5 Medium · 2 Low)

EXECUTIVE SUMMARY
The Shopify Analytics dashboard provides a well-structured overview of key business metrics with clear card-based layouts and comparison period indicators. However, there are notable accessibility concerns around color-only data differentiation in charts, low-contrast secondary text, and a lack of visible focus indicators. Some UX improvements could be made to better guide users toward actionable insights rather than presenting raw data alone.

ALL ISSUES  (full detail for reference):

[01] HIGH  —  Chart data series distinguished by color alone
     Location:  Line charts in Total sales, Online store sessions, Total orders, and Average order value cards
     Problem:   The current period (solid blue line) and comparison period (dotted gray line) are differentiated primarily through color. Users with color vision deficiencies may not be able to distinguish between the two data series, leading to misinterpretation of trend data.
     Fix:       Use distinct line patterns (solid vs. dashed) with different stroke weights, and add direct labels or data markers (circles, squares) to each line. The legend already shows solid vs. dotted but ensure the actual chart lines are visually distinct enough at all sizes.
     Source:    WCAG 2.2 — 1.4.1 Use of Color (A): information must not be conveyed by color alone
     WCAG:      1.4.1  Level A

[02] HIGH  —  Low contrast on secondary text and axis labels
     Location:  Chart axis labels (Jan 2, Jan 4, Jan 6, $0, $20, $40) and comparison period date ranges
     Problem:   The gray axis labels and date range text in chart legends appear to have insufficient contrast against the white background, likely falling below the 4.5:1 minimum ratio for normal-sized text. This makes data interpretation difficult for users with low vision.
     Fix:       Increase the text color darkness to at least #767676 or darker on white backgrounds to meet the 4.5:1 ratio. For the smaller axis labels, consider using a slightly larger font size as well to aid readability.
     Source:    WCAG 2.2 — 1.4.3 Contrast Minimum (AA): normal text requires 4.5:1 contrast ratio
     WCAG:      1.4.3  Level AA

[03] HIGH  —  No visible keyboard focus indicators
     Location:  All interactive elements including buttons (Last 7 days, Compare, Customize, Fullscreen), navigation items, and copy icons
     Problem:   There are no visible focus indicators on any interactive elements in the screenshot. Keyboard-only users and assistive technology users cannot determine which element currently has focus, making the dashboard essentially unusable without a mouse.
     Fix:       Add a clearly visible focus ring (minimum 2px solid outline in a contrasting color) to all interactive elements. Use CSS :focus-visible to show focus for keyboard users while hiding it for mouse interactions.
     Source:    WCAG 2.2 — 2.4.7 Focus Visible (AA): keyboard focus indicator must be visible
     WCAG:      2.4.7  Level AA

[04] MEDIUM  —  Icon-only copy buttons lack visible text labels
     Location:  Copy/clipboard icons in the top-right corner of each metric card (Total sales, Sales by channel, Online store sessions, etc.)
     Problem:   Each card has a small icon button in the upper-right corner with no visible label or tooltip. Users must guess the button's function, and screen reader users may not have adequate context if aria-labels are missing.
     Fix:       Add a visible tooltip on hover/focus that describes the action (e.g., 'Copy chart data'). Ensure each button has an accessible name via aria-label. Consider adding a text label alongside the icon for clarity.
     Source:    The Design of Everyday Things (Norman) — Signifier: visible cues must indicate where to act

[05] MEDIUM  —  Sales by channel card lacks chart or breakdown visualization
     Location:  Center card in top row — 'Sales by channel' showing only 'Draft Orders $34.00'
     Problem:   The Sales by channel card displays a single text value without any visual breakdown (pie chart, bar chart, or list). When there is only one channel, the card feels empty and underutilized. When multiple channels exist, a text-only presentation would not scale well.
     Fix:       Add a proportional visualization (horizontal bar or donut chart) to show channel distribution. For single-channel scenarios, display a message encouraging channel diversification or show the channel as a proportional bar at 100%.
     Source:    Universal Principles of Design — Signal-to-Noise Ratio: maximise relevant information, minimise clutter

[06] MEDIUM  —  Dash indicators provide no actionable context for trend changes
     Location:  Em-dash (—) symbols next to metric values ($34.00 —, 2 —, 0% —, 3 —, $18.00 —)
     Problem:   Each KPI displays an em-dash next to the value, presumably indicating no change or insufficient data for comparison. This is ambiguous — users cannot tell if it means 0% change, no data available, or a calculation error. The lack of explanation creates uncertainty.
     Fix:       Replace the em-dash with explicit text such as '0% change', 'No comparison data', or 'N/A' with a tooltip explanation. Use color-coded arrows (↑ green, ↓ red) with percentage text when comparison data is available.
     Source:    Don't Make Me Think (Krug) — Billboard Design 101: users scan, not read

[07] MEDIUM  —  No empty state guidance for zero-conversion funnel
     Location:  Online store conversion rate card showing 0% with 0 sessions across all funnel stages
     Problem:   The conversion rate card shows all zeros with no contextual help or guidance. New store owners seeing this will not understand whether their setup is incomplete, tracking is broken, or they simply have no traffic. This is a missed opportunity for onboarding support.
     Fix:       Display an empty state message like 'No sessions recorded yet. Make sure your online store is published and tracking is enabled.' Include a link to relevant setup documentation or marketing tools.
     Source:    Universal Principles of Design — Progressive Disclosure: show only what is needed at each step

[08] MEDIUM  —  Touch target size may be insufficient for small icon buttons
     Location:  Copy/clipboard icons on each card and the Auto-refresh toggle
     Problem:   The small copy icons in each card appear to be approximately 16-20px, which is below the WCAG 2.5.8 minimum target size of 24x24px. This makes them difficult to tap on touch devices and hard to click precisely with a mouse.
     Fix:       Increase the clickable area of icon buttons to at least 44x44px (iOS guideline) or minimum 24x24px (WCAG). This can be done with padding around the icon without changing its visual size.
     Source:    WCAG 2.2 — 2.5.8 Target Size Minimum (AA): interactive targets must be at least 24x24 CSS pixels
     WCAG:      2.5.8  Level AA

[09] LOW  —  Date range filter lacks clear visual hierarchy as primary control
     Location:  'Last 7 days' and 'Compare: Previous period' buttons below the Analytics heading
     Problem:   The date range filter buttons use a subtle outlined style that doesn't visually distinguish them as the primary control for the entire dashboard. Users may not immediately recognize these as the key filters that govern all displayed data.
     Fix:       Give the date range selector slightly more visual weight — consider a filled background or a more prominent position. Ensure the 'Compare' toggle is clearly secondary. Add a subtle background highlight to indicate the active filter state.
     Source:    Refactoring UI — Use color to support hierarchy, not communicate it alone

[10] LOW  —  Inconsistent card heights create visual imbalance
     Location:  Bottom row of metric cards — Online store conversion rate, Total orders, Average order value
     Problem:   The conversion rate card in the bottom-left is taller than its siblings due to the funnel breakdown content, while the Total orders and Average order value cards have charts. This creates visual imbalance and makes scanning across the row less efficient.
     Fix:       Normalize card heights within each row by setting a consistent minimum height, or reorganize the conversion funnel data into its own full-width section. Alternatively, collapse the funnel details behind an expandable section.
     Source:    Psych 101 (Kleinman) — Gestalt: Law of Similarity

====================================================================
SELF-CHECK — RUN THIS BEFORE OUTPUTTING THE HTML
====================================================================

For each item below, verify the SPECIFIC FIX is concretely implemented —
not just acknowledged, not just styled differently. The fix must be visible.

  [ ] [01] Chart data series distinguished by color alone
       Required fix: Use distinct line patterns (solid vs. dashed) with different stroke weights, and add direct labels or data markers (circles, squares) to each line. The legend already shows solid vs. dotted but ensure the actual chart lines are visually distinct enough at all sizes.
       Location:     Line charts in Total sales, Online store sessions, Total orders, and Average order value cards
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [02] Low contrast on secondary text and axis labels
       Required fix: Increase the text color darkness to at least #767676 or darker on white backgrounds to meet the 4.5:1 ratio. For the smaller axis labels, consider using a slightly larger font size as well to aid readability.
       Location:     Chart axis labels (Jan 2, Jan 4, Jan 6, $0, $20, $40) and comparison period date ranges
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [03] No visible keyboard focus indicators
       Required fix: Add a clearly visible focus ring (minimum 2px solid outline in a contrasting color) to all interactive elements. Use CSS :focus-visible to show focus for keyboard users while hiding it for mouse interactions.
       Location:     All interactive elements including buttons (Last 7 days, Compare, Customize, Fullscreen), navigation items, and copy icons
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [04] Icon-only copy buttons lack visible text labels
       Required fix: Add a visible tooltip on hover/focus that describes the action (e.g., 'Copy chart data'). Ensure each button has an accessible name via aria-label. Consider adding a text label alongside the icon for clarity.
       Location:     Copy/clipboard icons in the top-right corner of each metric card (Total sales, Sales by channel, Online store sessions, etc.)
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [05] Sales by channel card lacks chart or breakdown visualization
       Required fix: Add a proportional visualization (horizontal bar or donut chart) to show channel distribution. For single-channel scenarios, display a message encouraging channel diversification or show the channel as a proportional bar at 100%.
       Location:     Center card in top row — 'Sales by channel' showing only 'Draft Orders $34.00'
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [06] Dash indicators provide no actionable context for trend changes
       Required fix: Replace the em-dash with explicit text such as '0% change', 'No comparison data', or 'N/A' with a tooltip explanation. Use color-coded arrows (↑ green, ↓ red) with percentage text when comparison data is available.
       Location:     Em-dash (—) symbols next to metric values ($34.00 —, 2 —, 0% —, 3 —, $18.00 —)
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [07] No empty state guidance for zero-conversion funnel
       Required fix: Display an empty state message like 'No sessions recorded yet. Make sure your online store is published and tracking is enabled.' Include a link to relevant setup documentation or marketing tools.
       Location:     Online store conversion rate card showing 0% with 0 sessions across all funnel stages
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [08] Touch target size may be insufficient for small icon buttons
       Required fix: Increase the clickable area of icon buttons to at least 44x44px (iOS guideline) or minimum 24x24px (WCAG). This can be done with padding around the icon without changing its visual size.
       Location:     Copy/clipboard icons on each card and the Auto-refresh toggle
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

If any checkbox above cannot be ticked YES, revise your HTML before outputting it.

====================================================================
OUTPUT REQUIREMENTS
====================================================================

- Output a single, complete, self-contained HTML file.
- All CSS must be in a <style> block in the <head> — no external stylesheets.
- No CDN links, no JavaScript frameworks, no external images.
  Use inline SVG for icons. Use data URIs only if absolutely necessary.
- Preserve all informational content (text, labels, data). Do not silently drop
  information the user needs. However: if a fix requires relocating, collapsing,
  or restructuring a UI element, you MUST do so — placing it in a better position
  is not removing it. The audit fix always wins over original placement.
- Do NOT add new features, sections, or content not present in the original.
- JavaScript is allowed only for interactions clearly present in the original.
  Keep it minimal and inline.
- The file must open in a browser as a fully working static page.
- Start your response with <!DOCTYPE html> and end the HTML with </html>.
- Do NOT reference Mobbin or any third-party design gallery. Use Saasfactor if needed.