You are a senior UI/UX designer and expert frontend developer.

I am giving you a screenshot of a UI screen and a detailed UX audit report.
Your task is to produce a complete, self-contained HTML redesign (version 2)
that resolves every issue in the audit while preserving the original visual identity.

CRITICAL RULE — READ BEFORE ANYTHING ELSE:
Fixing an issue takes absolute priority over preserving layout or placement.
If a fix requires moving an element, restructuring a section, hiding a component,
or changing how content is presented — DO IT. The audit findings are non-negotiable.
Preservation means keeping the brand identity (colors, typography, component style).
It does NOT mean keeping broken elements in the same broken position.

====================================================================
MANDATORY FIX CHECKLIST  —  VERIFY EACH BEFORE WRITING A SINGLE LINE OF HTML
====================================================================

Before you write the HTML, mentally confirm that your design addresses EVERY item
in this list. If your HTML does not contain a concrete, visible solution for a
High or Medium issue, your response is incomplete — revise it until it does.
Some fixes require structural changes (relocating, collapsing, or restructuring
UI elements). Make those structural changes — do not style over them.

HIGH PRIORITY  (must be resolved — zero exceptions):
  NOTE: 'fixing' means the issue is GONE from the redesign — not styled differently.
  If the fix says to move, collapse, or restructure something, do exactly that.

  [01] Icon-only buttons lack accessible text labels
       Problem:          Multiple interactive icons (pencil edit, clipboard copy, three-dot overflow menu) have no visible text labels or apparent aria-labels. Screen reader users cannot determine the purpose of these controls, and sighted users may also be uncertain of their function.
       What to implement: Add visually hidden aria-label or sr-only text to each icon button (e.g., 'Edit task description', 'Copy task', 'More options'). Consider adding tooltip text on hover for sighted users as well.
       Affects:          Edit (pencil) icon on the Daily Trending Topics Summary card, copy and overflow (…) icons on the scheduled task card
       WCAG:             1.1.1 Level A

  [02] Low contrast on secondary and descriptive text
       Problem:          The light gray text used for secondary descriptions (e.g., 'Scheduled task', truncated summary text) appears to have insufficient contrast against the white/light background, likely falling below the 4.5:1 ratio required for normal-sized text.
       What to implement: Increase the text color to at least #767676 on white backgrounds to meet the 4.5:1 minimum ratio. Test all gray text values with a contrast checker tool like WebAIM's.
       Affects:          'Scheduled task' label under daily_trending_topics_summary, and the truncated description text in the Daily Trending Topics Summary card
       WCAG:             1.4.3 Level AA

  [03] Dismiss and Accept buttons have small touch targets
       Problem:          The 'Dismiss' and 'Accept' buttons appear compact with minimal padding, likely falling below the 24x24px minimum target size. On touch devices, users may accidentally tap the wrong action, especially given that Dismiss is destructive.
       What to implement: Increase the clickable area of both buttons to at least 44x44px (WCAG AAA recommendation) or minimum 24x24px. Add more internal padding and increase spacing between the two buttons to reduce accidental taps.
       Affects:          Dismiss and Accept action buttons on the Daily Trending Topics Summary card
       WCAG:             2.5.8 Level AA

  [04] Destructive 'Dismiss' action lacks confirmation safeguard
       Problem:          The Dismiss button appears to immediately discard the proposed scheduled task with no confirmation dialog. Users could accidentally dismiss a complex task setup with a single click, requiring them to re-enter the entire prompt.
       What to implement: Add a confirmation step before dismissing the task proposal, such as a brief 'Are you sure?' inline confirmation or an undo toast notification that persists for 5-10 seconds after dismissal.
       Affects:          Dismiss button (× Dismiss) on the task proposal card

MEDIUM PRIORITY  (must be resolved — no exceptions):
  NOTE: Same rule — fix means the issue does not exist in the output HTML.

  [05] Unclear visual hierarchy between task steps and final confirmation
       Problem:          Three sequential expandable sections plus a floating bottom confirmation bar create a dense vertical stack where the relationship and flow between steps is not immediately clear. The green checkmarks on some steps but not others adds ambiguity about which steps are complete vs. pending.
       What to implement: Differentiate completed steps with a muted/collapsed visual style and highlight the currently active step. Use a vertical stepper or progress indicator to make the sequential relationship explicit.
       Affects:          The sequence of expandable sections: Daily Trending Topics Summary card → Create scheduled task → Confirm task scheduling

  [06] Truncated task description hides critical information
       Problem:          The task description is truncated with an ellipsis, hiding potentially important details about what was proposed. Users must mentally compare the truncated preview to what they originally typed to verify accuracy before accepting.
       What to implement: Show the full task description by default in the review card, or provide a clear 'Show more' link. At minimum, ensure the visible portion includes all actionable parameters (time, platforms, format).
       Affects:          Daily Trending Topics Summary card description text ending in '…'

  [07] Left sidebar icons lack text labels
       Problem:          The left sidebar contains approximately 7 icon-only navigation items with no visible text labels or tooltips apparent. Users unfamiliar with the icon meanings must guess their function, and screen reader users may lack context if aria-labels are not provided.
       What to implement: Add text labels beside or below each icon, or at minimum provide tooltip labels on hover and aria-label attributes for accessibility. Consider an expandable sidebar that shows labels when space permits.
       Affects:          Left navigation sidebar with icon-only navigation items

  [08] Floating avatar/action button overlaps content
       Problem:          A floating element with the Manus icon and a monitor/screen icon overlaps the bottom of the chat content area. This obscures part of the conversation text and creates visual clutter that competes with the primary content.
       What to implement: Reposition the floating element to avoid overlapping chat content, or dock it in a fixed location outside the scrollable content area. Ensure it does not cover any interactive elements when scrolled.
       Affects:          Bottom-left floating element near the Manus avatar icon at approximately y:77%

  [09] No visible focus indicators on interactive elements
       Problem:          There are no visible keyboard focus indicators on any interactive elements in the screenshot. Keyboard-only users cannot determine which element is currently focused, making navigation impossible without a mouse.
       What to implement: Implement a visible focus ring (e.g., 2px solid outline with offset) on all interactive elements including buttons, links, expand/collapse controls, and the input field. Ensure the focus indicator has at least 3:1 contrast against adjacent colors.
       Affects:          All interactive elements: Dismiss/Accept buttons, expand/collapse chevrons, sidebar icons, text input field
       WCAG:             2.4.7 Level AA

LOW PRIORITY  (implement where possible):
  [10] Step pagination indicator '2 / 2' lacks context — Add contextual labeling such as 'Step 2 of 2' or 'Message 2 of 2'. If navigable, ensure the up/down chevrons have aria-labels explaining their function (e.g., 'Previous message', 'Next message').

====================================================================
STEP 1 — LAYOUT & COMPOSITION  (reason through this BEFORE anything else)
====================================================================

Layout and content distribution are non-negotiable design decisions.
Do not assume the original layout is correct — reason from first principles.
Ask yourself: does each element sit in the right zone for its purpose?
If not, move it. The composition must serve the user's primary goal.

1. VISUAL HIERARCHY
   The most important action or information must be visually dominant.
   Use size, weight, contrast, and position to establish a clear reading order.
   Secondary content must be visually subordinate — smaller, lighter, lower.
   (Malewicz — Designing User Interfaces: contrast creates hierarchy)
   (Refactoring UI: use size and weight to communicate importance, not just color)

2. F-PATTERN & READING FLOW
   Users scan left-to-right, top-to-bottom in an F-shape on content-heavy screens.
   Place primary actions and key labels at the start of scan lines.
   Never interrupt the scan path with secondary, promotional, or personalisation content.
   (Nielsen Norman Group — F-Pattern reading behavior)

3. PROXIMITY & GROUPING (Gestalt)
   Related elements must be spatially close. Unrelated elements need clear separation.
   If two elements answer different user questions, they belong in different zones.
   Never mix contextual content with navigational content in the same region.
   (Psych 101 — Gestalt: Law of Proximity, Law of Similarity)

4. CONTENT PRIORITISATION
   Identify the user's single primary goal on this screen.
   Every element that does not serve that goal directly must be secondary in
   visual weight and position. Upsell, personalisation, and promotional content
   must never compete with or interrupt the primary task flow.
   (Don't Make Me Think — Krug: do not make users think about what to do next)
   (Psychology of Design — Hick's Law: fewer competing choices = faster decisions)

5. SPATIAL ZONES
   Divide the screen into logical zones: navigation, primary content, secondary
   context, actions. Each zone has exactly one job.
   If an element is in the wrong zone, move it to the correct zone or defer it
   to a modal or drawer. The layout must match the user's mental model of the task.
   (Universal Principles of Design — Progressive Disclosure)

6. SIGNAL-TO-NOISE
   Every element must earn its place. If an element does not directly help the
   user complete their current task, reduce its visual weight or remove it from
   the primary viewport. Clutter is a UX failure, not a neutral default.
   (Universal Principles of Design — Signal-to-Noise Ratio)

COMPOSITION RULE: If the original layout violates these principles — with or
without an explicit audit finding — correct it. Do not replicate bad composition
because it existed in the screenshot. A version 2 must be compositionally sound.

APPLY THESE COMPOSITION DECISIONS TO RESOLVE THE FOLLOWING AUDIT ISSUES:
Every High and Medium issue in the MANDATORY FIX CHECKLIST above must map to a
specific, visible structural decision in your layout. Before moving to Step 2,
trace each issue to a layout zone, spacing decision, or hierarchy change.
If the fix requires restructuring — restructure. If it requires removing an element
from the primary viewport — remove it. Composition fixes are not optional.

====================================================================
STEP 2 — VISUAL IDENTITY  (preserve brand, not broken placement)
====================================================================

Once composition is decided, preserve these brand elements exactly:
  - Colors: every background, text, brand, accent, and surface hex value
  - Typography: font families, sizes, weights, line-heights, letter-spacing
  - Spacing scale: the relative rhythm of padding/margin (not the positions)
  - Shapes: border-radius, border styles, shadows, elevation
  - Icons: style, weight, and size (placement may change if composition requires)
  - Components: card designs, button styles, badge styles, input styles

NOTE: Layout and element positioning are governed by Step 1, not this list.
Preserving the brand does NOT mean preserving where broken things sit.
The output must feel like a version 2 — same brand, better UX.

====================================================================
FULL AUDIT REPORT  —  Scheduled Task Creation Chat
====================================================================
Overall Score:        6.8/10  —  Fair
Accessibility Score:  5.5/10  (WCAG 2.2 AA)
Issues Found:         10  (4 High · 5 Medium · 1 Low)

EXECUTIVE SUMMARY
The Manus scheduled task interface demonstrates a clear conversational flow with progressive disclosure of task creation steps, but suffers from several accessibility concerns including low-contrast text, small icon-only buttons without labels, and unclear interactive affordances. The information hierarchy within the chat is generally well-structured, though the density of nested cards and multiple expandable sections can increase cognitive load.

ALL ISSUES  (full detail for reference):

[01] HIGH  —  Icon-only buttons lack accessible text labels
     Location:  Edit (pencil) icon on the Daily Trending Topics Summary card, copy and overflow (…) icons on the scheduled task card
     Problem:   Multiple interactive icons (pencil edit, clipboard copy, three-dot overflow menu) have no visible text labels or apparent aria-labels. Screen reader users cannot determine the purpose of these controls, and sighted users may also be uncertain of their function.
     Fix:       Add visually hidden aria-label or sr-only text to each icon button (e.g., 'Edit task description', 'Copy task', 'More options'). Consider adding tooltip text on hover for sighted users as well.
     Source:    WCAG 2.2 — 1.1.1 Non-text Content (A): all functional non-text content must have a text alternative
     WCAG:      1.1.1  Level A

[02] HIGH  —  Low contrast on secondary and descriptive text
     Location:  'Scheduled task' label under daily_trending_topics_summary, and the truncated description text in the Daily Trending Topics Summary card
     Problem:   The light gray text used for secondary descriptions (e.g., 'Scheduled task', truncated summary text) appears to have insufficient contrast against the white/light background, likely falling below the 4.5:1 ratio required for normal-sized text.
     Fix:       Increase the text color to at least #767676 on white backgrounds to meet the 4.5:1 minimum ratio. Test all gray text values with a contrast checker tool like WebAIM's.
     Source:    WCAG 2.2 — 1.4.3 Contrast Minimum (AA): normal text requires 4.5:1 contrast ratio
     WCAG:      1.4.3  Level AA

[03] HIGH  —  Dismiss and Accept buttons have small touch targets
     Location:  Dismiss and Accept action buttons on the Daily Trending Topics Summary card
     Problem:   The 'Dismiss' and 'Accept' buttons appear compact with minimal padding, likely falling below the 24x24px minimum target size. On touch devices, users may accidentally tap the wrong action, especially given that Dismiss is destructive.
     Fix:       Increase the clickable area of both buttons to at least 44x44px (WCAG AAA recommendation) or minimum 24x24px. Add more internal padding and increase spacing between the two buttons to reduce accidental taps.
     Source:    WCAG 2.2 — 2.5.8 Target Size Minimum (AA): interactive targets must be at least 24x24 CSS pixels
     WCAG:      2.5.8  Level AA

[04] HIGH  —  Destructive 'Dismiss' action lacks confirmation safeguard
     Location:  Dismiss button (× Dismiss) on the task proposal card
     Problem:   The Dismiss button appears to immediately discard the proposed scheduled task with no confirmation dialog. Users could accidentally dismiss a complex task setup with a single click, requiring them to re-enter the entire prompt.
     Fix:       Add a confirmation step before dismissing the task proposal, such as a brief 'Are you sure?' inline confirmation or an undo toast notification that persists for 5-10 seconds after dismissal.
     Source:    The Design of Everyday Things (Norman) — Forcing Function: prevent irreversible actions with confirmation

[05] MEDIUM  —  Unclear visual hierarchy between task steps and final confirmation
     Location:  The sequence of expandable sections: Daily Trending Topics Summary card → Create scheduled task → Confirm task scheduling
     Problem:   Three sequential expandable sections plus a floating bottom confirmation bar create a dense vertical stack where the relationship and flow between steps is not immediately clear. The green checkmarks on some steps but not others adds ambiguity about which steps are complete vs. pending.
     Fix:       Differentiate completed steps with a muted/collapsed visual style and highlight the currently active step. Use a vertical stepper or progress indicator to make the sequential relationship explicit.
     Source:    Psych 101 (Kleinman) — Gestalt: Law of Proximity

[06] MEDIUM  —  Truncated task description hides critical information
     Location:  Daily Trending Topics Summary card description text ending in '…'
     Problem:   The task description is truncated with an ellipsis, hiding potentially important details about what was proposed. Users must mentally compare the truncated preview to what they originally typed to verify accuracy before accepting.
     Fix:       Show the full task description by default in the review card, or provide a clear 'Show more' link. At minimum, ensure the visible portion includes all actionable parameters (time, platforms, format).
     Source:    Don't Make Me Think (Krug) — Billboard Design 101: users scan, not read

[07] MEDIUM  —  Left sidebar icons lack text labels
     Location:  Left navigation sidebar with icon-only navigation items
     Problem:   The left sidebar contains approximately 7 icon-only navigation items with no visible text labels or tooltips apparent. Users unfamiliar with the icon meanings must guess their function, and screen reader users may lack context if aria-labels are not provided.
     Fix:       Add text labels beside or below each icon, or at minimum provide tooltip labels on hover and aria-label attributes for accessibility. Consider an expandable sidebar that shows labels when space permits.
     Source:    The Design of Everyday Things (Norman) — Signifier: visible cues must indicate where to act

[08] MEDIUM  —  Floating avatar/action button overlaps content
     Location:  Bottom-left floating element near the Manus avatar icon at approximately y:77%
     Problem:   A floating element with the Manus icon and a monitor/screen icon overlaps the bottom of the chat content area. This obscures part of the conversation text and creates visual clutter that competes with the primary content.
     Fix:       Reposition the floating element to avoid overlapping chat content, or dock it in a fixed location outside the scrollable content area. Ensure it does not cover any interactive elements when scrolled.
     Source:    Universal Principles of Design — Signal-to-Noise Ratio: maximise relevant information, minimise clutter

[09] MEDIUM  —  No visible focus indicators on interactive elements
     Location:  All interactive elements: Dismiss/Accept buttons, expand/collapse chevrons, sidebar icons, text input field
     Problem:   There are no visible keyboard focus indicators on any interactive elements in the screenshot. Keyboard-only users cannot determine which element is currently focused, making navigation impossible without a mouse.
     Fix:       Implement a visible focus ring (e.g., 2px solid outline with offset) on all interactive elements including buttons, links, expand/collapse controls, and the input field. Ensure the focus indicator has at least 3:1 contrast against adjacent colors.
     Source:    WCAG 2.2 — 2.4.7 Focus Visible (AA): keyboard focus indicator must be visible
     WCAG:      2.4.7  Level AA

[10] LOW  —  Step pagination indicator '2 / 2' lacks context
     Location:  '2 / 2' indicator next to 'Confirm task scheduling to user' at the bottom
     Problem:   The '2 / 2' pagination text at the bottom-right of the confirmation step provides no context about what is being counted (steps? messages? confirmations?). Users may not understand what this number represents or how to navigate between items.
     Fix:       Add contextual labeling such as 'Step 2 of 2' or 'Message 2 of 2'. If navigable, ensure the up/down chevrons have aria-labels explaining their function (e.g., 'Previous message', 'Next message').
     Source:    Universal Principles of Design — Visibility: system status and available actions must be visible

====================================================================
SELF-CHECK — RUN THIS BEFORE OUTPUTTING THE HTML
====================================================================

For each item below, verify the SPECIFIC FIX is concretely implemented —
not just acknowledged, not just styled differently. The fix must be visible.

  [ ] [01] Icon-only buttons lack accessible text labels
       Required fix: Add visually hidden aria-label or sr-only text to each icon button (e.g., 'Edit task description', 'Copy task', 'More options'). Consider adding tooltip text on hover for sighted users as well.
       Location:     Edit (pencil) icon on the Daily Trending Topics Summary card, copy and overflow (…) icons on the scheduled task card
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [02] Low contrast on secondary and descriptive text
       Required fix: Increase the text color to at least #767676 on white backgrounds to meet the 4.5:1 minimum ratio. Test all gray text values with a contrast checker tool like WebAIM's.
       Location:     'Scheduled task' label under daily_trending_topics_summary, and the truncated description text in the Daily Trending Topics Summary card
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [03] Dismiss and Accept buttons have small touch targets
       Required fix: Increase the clickable area of both buttons to at least 44x44px (WCAG AAA recommendation) or minimum 24x24px. Add more internal padding and increase spacing between the two buttons to reduce accidental taps.
       Location:     Dismiss and Accept action buttons on the Daily Trending Topics Summary card
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [04] Destructive 'Dismiss' action lacks confirmation safeguard
       Required fix: Add a confirmation step before dismissing the task proposal, such as a brief 'Are you sure?' inline confirmation or an undo toast notification that persists for 5-10 seconds after dismissal.
       Location:     Dismiss button (× Dismiss) on the task proposal card
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [05] Unclear visual hierarchy between task steps and final confirmation
       Required fix: Differentiate completed steps with a muted/collapsed visual style and highlight the currently active step. Use a vertical stepper or progress indicator to make the sequential relationship explicit.
       Location:     The sequence of expandable sections: Daily Trending Topics Summary card → Create scheduled task → Confirm task scheduling
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [06] Truncated task description hides critical information
       Required fix: Show the full task description by default in the review card, or provide a clear 'Show more' link. At minimum, ensure the visible portion includes all actionable parameters (time, platforms, format).
       Location:     Daily Trending Topics Summary card description text ending in '…'
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [07] Left sidebar icons lack text labels
       Required fix: Add text labels beside or below each icon, or at minimum provide tooltip labels on hover and aria-label attributes for accessibility. Consider an expandable sidebar that shows labels when space permits.
       Location:     Left navigation sidebar with icon-only navigation items
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [08] Floating avatar/action button overlaps content
       Required fix: Reposition the floating element to avoid overlapping chat content, or dock it in a fixed location outside the scrollable content area. Ensure it does not cover any interactive elements when scrolled.
       Location:     Bottom-left floating element near the Manus avatar icon at approximately y:77%
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [09] No visible focus indicators on interactive elements
       Required fix: Implement a visible focus ring (e.g., 2px solid outline with offset) on all interactive elements including buttons, links, expand/collapse controls, and the input field. Ensure the focus indicator has at least 3:1 contrast against adjacent colors.
       Location:     All interactive elements: Dismiss/Accept buttons, expand/collapse chevrons, sidebar icons, text input field
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

If any checkbox above cannot be ticked YES, revise your HTML before outputting it.

====================================================================
OUTPUT REQUIREMENTS
====================================================================

- Output a single, complete, self-contained HTML file.
- All CSS must be in a <style> block in the <head> — no external stylesheets.
- No CDN links, no JavaScript frameworks, no external images.
  Use inline SVG for icons. Use data URIs only if absolutely necessary.
- Preserve all informational content (text, labels, data). Do not silently drop
  information the user needs. However: if a fix requires relocating, collapsing,
  or restructuring a UI element, you MUST do so — placing it in a better position
  is not removing it. The audit fix always wins over original placement.
- Do NOT add new features, sections, or content not present in the original.
- JavaScript is allowed only for interactions clearly present in the original.
  Keep it minimal and inline.
- The file must open in a browser as a fully working static page.
- Start your response with <!DOCTYPE html> and end the HTML with </html>.
- Do NOT reference Mobbin or any third-party design gallery. Use Saasfactor if needed.