You are a senior UI/UX designer and expert frontend developer.

I am giving you a screenshot of a UI screen and a detailed UX audit report.
Your task is to produce a complete, self-contained HTML redesign (version 2)
that resolves every issue in the audit while preserving the original visual identity.

CRITICAL RULE — READ BEFORE ANYTHING ELSE:
Fixing an issue takes absolute priority over preserving layout or placement.
If a fix requires moving an element, restructuring a section, hiding a component,
or changing how content is presented — DO IT. The audit findings are non-negotiable.
Preservation means keeping the brand identity (colors, typography, component style).
It does NOT mean keeping broken elements in the same broken position.

====================================================================
MANDATORY FIX CHECKLIST  —  VERIFY EACH BEFORE WRITING A SINGLE LINE OF HTML
====================================================================

Before you write the HTML, mentally confirm that your design addresses EVERY item
in this list. If your HTML does not contain a concrete, visible solution for a
High or Medium issue, your response is incomplete — revise it until it does.
Some fixes require structural changes (relocating, collapsing, or restructuring
UI elements). Make those structural changes — do not style over them.

HIGH PRIORITY  (must be resolved — zero exceptions):
  NOTE: 'fixing' means the issue is GONE from the redesign — not styled differently.
  If the fix says to move, collapse, or restructure something, do exactly that.

  [01] Chart relies solely on color to distinguish data series
       Problem:          The two data series in the chart are differentiated only by color (blue vs red/orange). Users with color vision deficiencies cannot distinguish between the 'Uniques of All Events' and 'Total Events of meaningful interaction' lines. The legend also uses color-only indicators.
       What to implement: Add distinct line patterns (dashed, dotted), different marker shapes (circles, squares, triangles) on data points, or direct labels on each line. Update the legend to include pattern/shape indicators alongside color swatches.
       Affects:          Line chart area — blue line (A) vs red/orange line (B)
       WCAG:             1.4.1 Level A

  [02] Low contrast on axis labels and data table values
       Problem:          The axis labels on the chart and the zero values in the data table appear in light gray against a white background. This likely fails the 4.5:1 minimum contrast ratio for normal-sized text, making the data difficult to read for users with low vision or in bright environments.
       What to implement: Increase the color of axis labels and table cell text to at least #767676 (or darker) on white backgrounds to meet the 4.5:1 ratio. Consider using a slightly darker shade like #595959 for better readability.
       Affects:          Y-axis labels (E0, E10, E20, etc.), X-axis dates, and table cells showing '0'
       WCAG:             1.4.3 Level AA

  [03] Truncated metric label obscures critical information
       Problem:          The metric name 'Total Events of meaningful interaction' is truncated in both the data table and the right query panel, hiding critical information. Users cannot quickly identify what metric B represents without hovering or interacting further.
       What to implement: Use a tooltip on hover that shows the full name, and increase the column width or allow text wrapping for metric names. In the right panel, allow multi-line wrapping for metric titles instead of truncating.
       Affects:          Data table row 'B. Total Events of mea...' and right panel 'Total Events of meaningful interact...'

  [04] Icon-only buttons in chart type and visualization selectors lack accessible labels
       Problem:          Multiple icon-only buttons for chart type selection (right panel) and data view toggles (below chart) have no visible text labels. Screen reader users and unfamiliar users cannot determine what each icon represents without trial and error.
       What to implement: Add aria-label or title attributes to each icon button describing its function (e.g., 'Line chart', 'Bar chart', 'Table view'). Consider adding visible labels or tooltips on hover for all users.
       Affects:          Right panel top row of 4 icon-only buttons (chart types) and bottom chart toggle icons (3 icons below chart)
       WCAG:             1.1.1 Level A

MEDIUM PRIORITY  (must be resolved — no exceptions):
  NOTE: Same rule — fix means the issue does not exist in the output HTML.

  [05] Dual Y-axes create confusion and cognitive overload
       Problem:          The chart uses two different Y-axes with different scales (left: E0–E100, right: 0–40) without clear visual association to their respective data series. Users must mentally map which axis belongs to which line, increasing interpretation difficulty.
       What to implement: Consider using two separate charts stacked vertically, each with its own clearly labeled Y-axis. If dual axes must be used, color-code the axis labels to match their respective lines and add clear axis titles.
       Affects:          Chart area — left Y-axis (E0–E100) and right Y-axis (0–40)

  [06] Untitled report creates findability and recognition issues
       Problem:          The report is labeled 'Untitled' in the header. If saved in this state, it will be indistinguishable from other untitled reports, making future retrieval difficult and cluttering the user's workspace.
       What to implement: Prompt the user to name the report before or during the first save action. Consider auto-generating a descriptive name based on the metrics and filters used (e.g., 'Uniques & Interactions by Country - 30D').
       Affects:          Top breadcrumb area showing 'SLMobbin / Untitled'

  [07] Complex query configuration increases cognitive load with minimal progressive disclosure
       Problem:          The right query panel displays all metrics, their event configurations, measurement options, filters, and breakdowns simultaneously. This dense layout presents too many options at once, overwhelming users who may only need to adjust one parameter.
       What to implement: Implement progressive disclosure by collapsing metric sections by default and expanding only the one being actively edited. Add a summary view for configured metrics that can be expanded on click. Group Filter and Breakdown into a collapsible 'Advanced' section.
       Affects:          Right panel — Metrics A, B, C sections with nested dropdowns and filters

  [08] Small interactive targets for filter removal and dropdown toggles
       Problem:          The close/remove button next to 'Cumulative Sum' and the small dropdown toggle chevrons throughout the query panel appear to be very small click targets. This makes precise interaction difficult, especially on touch devices.
       What to implement: Increase all interactive targets (close buttons, dropdown toggles) to a minimum of 24×24px, ideally 44×44px for touch. Add adequate padding around small icons to expand the clickable area without changing visual size.
       Affects:          Small '×' close button next to 'Cumulative Sum' and various dropdown chevrons
       WCAG:             2.5.8 Level AA

  [09] Y-axis label notation 'E' prefix is ambiguous
       Problem:          The left Y-axis uses 'E' prefix notation (E0, E10, E40, E100) which could be interpreted as scientific notation (e.g., E10 = 10^10) or as a custom abbreviation. The non-linear scale progression (0, 10, 20, 40, 100) further confuses interpretation.
       What to implement: Use standard numeric labels (0, 10, 20, 40, 100) or clarify the axis title (e.g., 'Events count'). If using a logarithmic or custom scale, add an axis title that explains the unit and scale type.
       Affects:          Left Y-axis labels: E0, E10, E20, E40, E100

LOW PRIORITY  (implement where possible):
  [10] Inconsistent data table date range versus chart date range — Synchronize the data table with the chart's visible date range. Add horizontal scrolling or pagination to the table to show all dates. Highlight the currently visible chart range in the table or allow table-chart interaction for filtering.

====================================================================
STEP 1 — LAYOUT & COMPOSITION  (reason through this BEFORE anything else)
====================================================================

Layout and content distribution are non-negotiable design decisions.
Do not assume the original layout is correct — reason from first principles.
Ask yourself: does each element sit in the right zone for its purpose?
If not, move it. The composition must serve the user's primary goal.

1. VISUAL HIERARCHY
   The most important action or information must be visually dominant.
   Use size, weight, contrast, and position to establish a clear reading order.
   Secondary content must be visually subordinate — smaller, lighter, lower.
   (Malewicz — Designing User Interfaces: contrast creates hierarchy)
   (Refactoring UI: use size and weight to communicate importance, not just color)

2. F-PATTERN & READING FLOW
   Users scan left-to-right, top-to-bottom in an F-shape on content-heavy screens.
   Place primary actions and key labels at the start of scan lines.
   Never interrupt the scan path with secondary, promotional, or personalisation content.
   (Nielsen Norman Group — F-Pattern reading behavior)

3. PROXIMITY & GROUPING (Gestalt)
   Related elements must be spatially close. Unrelated elements need clear separation.
   If two elements answer different user questions, they belong in different zones.
   Never mix contextual content with navigational content in the same region.
   (Psych 101 — Gestalt: Law of Proximity, Law of Similarity)

4. CONTENT PRIORITISATION
   Identify the user's single primary goal on this screen.
   Every element that does not serve that goal directly must be secondary in
   visual weight and position. Upsell, personalisation, and promotional content
   must never compete with or interrupt the primary task flow.
   (Don't Make Me Think — Krug: do not make users think about what to do next)
   (Psychology of Design — Hick's Law: fewer competing choices = faster decisions)

5. SPATIAL ZONES
   Divide the screen into logical zones: navigation, primary content, secondary
   context, actions. Each zone has exactly one job.
   If an element is in the wrong zone, move it to the correct zone or defer it
   to a modal or drawer. The layout must match the user's mental model of the task.
   (Universal Principles of Design — Progressive Disclosure)

6. SIGNAL-TO-NOISE
   Every element must earn its place. If an element does not directly help the
   user complete their current task, reduce its visual weight or remove it from
   the primary viewport. Clutter is a UX failure, not a neutral default.
   (Universal Principles of Design — Signal-to-Noise Ratio)

COMPOSITION RULE: If the original layout violates these principles — with or
without an explicit audit finding — correct it. Do not replicate bad composition
because it existed in the screenshot. A version 2 must be compositionally sound.

APPLY THESE COMPOSITION DECISIONS TO RESOLVE THE FOLLOWING AUDIT ISSUES:
Every High and Medium issue in the MANDATORY FIX CHECKLIST above must map to a
specific, visible structural decision in your layout. Before moving to Step 2,
trace each issue to a layout zone, spacing decision, or hierarchy change.
If the fix requires restructuring — restructure. If it requires removing an element
from the primary viewport — remove it. Composition fixes are not optional.

====================================================================
STEP 2 — VISUAL IDENTITY  (preserve brand, not broken placement)
====================================================================

Once composition is decided, preserve these brand elements exactly:
  - Colors: every background, text, brand, accent, and surface hex value
  - Typography: font families, sizes, weights, line-heights, letter-spacing
  - Spacing scale: the relative rhythm of padding/margin (not the positions)
  - Shapes: border-radius, border styles, shadows, elevation
  - Icons: style, weight, and size (placement may change if composition requires)
  - Components: card designs, button styles, badge styles, input styles

NOTE: Layout and element positioning are governed by Step 1, not this list.
Preserving the brand does NOT mean preserving where broken things sit.
The output must feel like a version 2 — same brand, better UX.

====================================================================
FULL AUDIT REPORT  —  Insights Query Builder
====================================================================
Overall Score:        6.4/10  —  Fair
Accessibility Score:  5.2/10  (WCAG 2.2 AA)
Issues Found:         10  (4 High · 5 Medium · 1 Low)

EXECUTIVE SUMMARY
The Mixpanel Insights screen presents a complex analytics query builder with a dense information layout. While it provides powerful configuration options, it suffers from low contrast text, truncated labels, color-only data differentiation in the chart, and cognitive overload from simultaneously visible query panels, chart, and data table. Several accessibility concerns exist around color reliance and text legibility.

ALL ISSUES  (full detail for reference):

[01] HIGH  —  Chart relies solely on color to distinguish data series
     Location:  Line chart area — blue line (A) vs red/orange line (B)
     Problem:   The two data series in the chart are differentiated only by color (blue vs red/orange). Users with color vision deficiencies cannot distinguish between the 'Uniques of All Events' and 'Total Events of meaningful interaction' lines. The legend also uses color-only indicators.
     Fix:       Add distinct line patterns (dashed, dotted), different marker shapes (circles, squares, triangles) on data points, or direct labels on each line. Update the legend to include pattern/shape indicators alongside color swatches.
     Source:    WCAG 2.2 — 1.4.1 Use of Color (A): information must not be conveyed by color alone
     WCAG:      1.4.1  Level A

[02] HIGH  —  Low contrast on axis labels and data table values
     Location:  Y-axis labels (E0, E10, E20, etc.), X-axis dates, and table cells showing '0'
     Problem:   The axis labels on the chart and the zero values in the data table appear in light gray against a white background. This likely fails the 4.5:1 minimum contrast ratio for normal-sized text, making the data difficult to read for users with low vision or in bright environments.
     Fix:       Increase the color of axis labels and table cell text to at least #767676 (or darker) on white backgrounds to meet the 4.5:1 ratio. Consider using a slightly darker shade like #595959 for better readability.
     Source:    WCAG 2.2 — 1.4.3 Contrast Minimum (AA): normal text requires 4.5:1 contrast ratio
     WCAG:      1.4.3  Level AA

[03] HIGH  —  Truncated metric label obscures critical information
     Location:  Data table row 'B. Total Events of mea...' and right panel 'Total Events of meaningful interact...'
     Problem:   The metric name 'Total Events of meaningful interaction' is truncated in both the data table and the right query panel, hiding critical information. Users cannot quickly identify what metric B represents without hovering or interacting further.
     Fix:       Use a tooltip on hover that shows the full name, and increase the column width or allow text wrapping for metric names. In the right panel, allow multi-line wrapping for metric titles instead of truncating.
     Source:    Don't Make Me Think (Krug) — Billboard Design 101: users scan, not read

[04] HIGH  —  Icon-only buttons in chart type and visualization selectors lack accessible labels
     Location:  Right panel top row of 4 icon-only buttons (chart types) and bottom chart toggle icons (3 icons below chart)
     Problem:   Multiple icon-only buttons for chart type selection (right panel) and data view toggles (below chart) have no visible text labels. Screen reader users and unfamiliar users cannot determine what each icon represents without trial and error.
     Fix:       Add aria-label or title attributes to each icon button describing its function (e.g., 'Line chart', 'Bar chart', 'Table view'). Consider adding visible labels or tooltips on hover for all users.
     Source:    WCAG 2.2 — 1.1.1 Non-text Content (A): all non-text content must have a text alternative
     WCAG:      1.1.1  Level A

[05] MEDIUM  —  Dual Y-axes create confusion and cognitive overload
     Location:  Chart area — left Y-axis (E0–E100) and right Y-axis (0–40)
     Problem:   The chart uses two different Y-axes with different scales (left: E0–E100, right: 0–40) without clear visual association to their respective data series. Users must mentally map which axis belongs to which line, increasing interpretation difficulty.
     Fix:       Consider using two separate charts stacked vertically, each with its own clearly labeled Y-axis. If dual axes must be used, color-code the axis labels to match their respective lines and add clear axis titles.
     Source:    Universal Principles of Design — Signal-to-Noise Ratio: maximise relevant information, minimise clutter

[06] MEDIUM  —  Untitled report creates findability and recognition issues
     Location:  Top breadcrumb area showing 'SLMobbin / Untitled'
     Problem:   The report is labeled 'Untitled' in the header. If saved in this state, it will be indistinguishable from other untitled reports, making future retrieval difficult and cluttering the user's workspace.
     Fix:       Prompt the user to name the report before or during the first save action. Consider auto-generating a descriptive name based on the metrics and filters used (e.g., 'Uniques & Interactions by Country - 30D').
     Source:    The Design of Everyday Things (Norman) — Forcing Function: prevent irreversible actions with confirmation

[07] MEDIUM  —  Complex query configuration increases cognitive load with minimal progressive disclosure
     Location:  Right panel — Metrics A, B, C sections with nested dropdowns and filters
     Problem:   The right query panel displays all metrics, their event configurations, measurement options, filters, and breakdowns simultaneously. This dense layout presents too many options at once, overwhelming users who may only need to adjust one parameter.
     Fix:       Implement progressive disclosure by collapsing metric sections by default and expanding only the one being actively edited. Add a summary view for configured metrics that can be expanded on click. Group Filter and Breakdown into a collapsible 'Advanced' section.
     Source:    Universal Principles of Design — Progressive Disclosure: show only what is needed at each step

[08] MEDIUM  —  Small interactive targets for filter removal and dropdown toggles
     Location:  Small '×' close button next to 'Cumulative Sum' and various dropdown chevrons
     Problem:   The close/remove button next to 'Cumulative Sum' and the small dropdown toggle chevrons throughout the query panel appear to be very small click targets. This makes precise interaction difficult, especially on touch devices.
     Fix:       Increase all interactive targets (close buttons, dropdown toggles) to a minimum of 24×24px, ideally 44×44px for touch. Add adequate padding around small icons to expand the clickable area without changing visual size.
     Source:    WCAG 2.2 — 2.5.8 Target Size Minimum (AA): interactive targets must be at least 24x24 CSS pixels
     WCAG:      2.5.8  Level AA

[09] MEDIUM  —  Y-axis label notation 'E' prefix is ambiguous
     Location:  Left Y-axis labels: E0, E10, E20, E40, E100
     Problem:   The left Y-axis uses 'E' prefix notation (E0, E10, E40, E100) which could be interpreted as scientific notation (e.g., E10 = 10^10) or as a custom abbreviation. The non-linear scale progression (0, 10, 20, 40, 100) further confuses interpretation.
     Fix:       Use standard numeric labels (0, 10, 20, 40, 100) or clarify the axis title (e.g., 'Events count'). If using a logarithmic or custom scale, add an axis title that explains the unit and scale type.
     Source:    The Design of Everyday Things (Norman) — Gulf of Evaluation: user cannot tell if their action worked

[10] LOW  —  Inconsistent data table date range versus chart date range
     Location:  Data table headers (Dec 21–Dec 25) vs chart X-axis (Dec 22–Jan 19)
     Problem:   The data table shows dates starting from Dec 21 while the chart X-axis begins at Dec 22 and extends to Jan 19. The table only shows 5 date columns (Dec 21–25) while the chart covers nearly a month, creating a disconnect between the two views.
     Fix:       Synchronize the data table with the chart's visible date range. Add horizontal scrolling or pagination to the table to show all dates. Highlight the currently visible chart range in the table or allow table-chart interaction for filtering.
     Source:    Psych 101 (Kleinman) — Gestalt: Law of Proximity

====================================================================
SELF-CHECK — RUN THIS BEFORE OUTPUTTING THE HTML
====================================================================

For each item below, verify the SPECIFIC FIX is concretely implemented —
not just acknowledged, not just styled differently. The fix must be visible.

  [ ] [01] Chart relies solely on color to distinguish data series
       Required fix: Add distinct line patterns (dashed, dotted), different marker shapes (circles, squares, triangles) on data points, or direct labels on each line. Update the legend to include pattern/shape indicators alongside color swatches.
       Location:     Line chart area — blue line (A) vs red/orange line (B)
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [02] Low contrast on axis labels and data table values
       Required fix: Increase the color of axis labels and table cell text to at least #767676 (or darker) on white backgrounds to meet the 4.5:1 ratio. Consider using a slightly darker shade like #595959 for better readability.
       Location:     Y-axis labels (E0, E10, E20, etc.), X-axis dates, and table cells showing '0'
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [03] Truncated metric label obscures critical information
       Required fix: Use a tooltip on hover that shows the full name, and increase the column width or allow text wrapping for metric names. In the right panel, allow multi-line wrapping for metric titles instead of truncating.
       Location:     Data table row 'B. Total Events of mea...' and right panel 'Total Events of meaningful interact...'
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [04] Icon-only buttons in chart type and visualization selectors lack accessible labels
       Required fix: Add aria-label or title attributes to each icon button describing its function (e.g., 'Line chart', 'Bar chart', 'Table view'). Consider adding visible labels or tooltips on hover for all users.
       Location:     Right panel top row of 4 icon-only buttons (chart types) and bottom chart toggle icons (3 icons below chart)
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [05] Dual Y-axes create confusion and cognitive overload
       Required fix: Consider using two separate charts stacked vertically, each with its own clearly labeled Y-axis. If dual axes must be used, color-code the axis labels to match their respective lines and add clear axis titles.
       Location:     Chart area — left Y-axis (E0–E100) and right Y-axis (0–40)
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [06] Untitled report creates findability and recognition issues
       Required fix: Prompt the user to name the report before or during the first save action. Consider auto-generating a descriptive name based on the metrics and filters used (e.g., 'Uniques & Interactions by Country - 30D').
       Location:     Top breadcrumb area showing 'SLMobbin / Untitled'
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [07] Complex query configuration increases cognitive load with minimal progressive disclosure
       Required fix: Implement progressive disclosure by collapsing metric sections by default and expanding only the one being actively edited. Add a summary view for configured metrics that can be expanded on click. Group Filter and Breakdown into a collapsible 'Advanced' section.
       Location:     Right panel — Metrics A, B, C sections with nested dropdowns and filters
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [08] Small interactive targets for filter removal and dropdown toggles
       Required fix: Increase all interactive targets (close buttons, dropdown toggles) to a minimum of 24×24px, ideally 44×44px for touch. Add adequate padding around small icons to expand the clickable area without changing visual size.
       Location:     Small '×' close button next to 'Cumulative Sum' and various dropdown chevrons
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [09] Y-axis label notation 'E' prefix is ambiguous
       Required fix: Use standard numeric labels (0, 10, 20, 40, 100) or clarify the axis title (e.g., 'Events count'). If using a logarithmic or custom scale, add an axis title that explains the unit and scale type.
       Location:     Left Y-axis labels: E0, E10, E20, E40, E100
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

If any checkbox above cannot be ticked YES, revise your HTML before outputting it.

====================================================================
OUTPUT REQUIREMENTS
====================================================================

- Output a single, complete, self-contained HTML file.
- All CSS must be in a <style> block in the <head> — no external stylesheets.
- No CDN links, no JavaScript frameworks, no external images.
  Use inline SVG for icons. Use data URIs only if absolutely necessary.
- Preserve all informational content (text, labels, data). Do not silently drop
  information the user needs. However: if a fix requires relocating, collapsing,
  or restructuring a UI element, you MUST do so — placing it in a better position
  is not removing it. The audit fix always wins over original placement.
- Do NOT add new features, sections, or content not present in the original.
- JavaScript is allowed only for interactions clearly present in the original.
  Keep it minimal and inline.
- The file must open in a browser as a fully working static page.
- Start your response with <!DOCTYPE html> and end the HTML with </html>.
- Do NOT reference Mobbin or any third-party design gallery. Use Saasfactor if needed.