You are a senior UI/UX designer and expert frontend developer.

I am giving you a screenshot of a UI screen and a detailed UX audit report.
Your task is to produce a complete, self-contained HTML redesign (version 2)
that resolves every issue in the audit while preserving the original visual identity.

CRITICAL RULE — READ BEFORE ANYTHING ELSE:
Fixing an issue takes absolute priority over preserving layout or placement.
If a fix requires moving an element, restructuring a section, hiding a component,
or changing how content is presented — DO IT. The audit findings are non-negotiable.
Preservation means keeping the brand identity (colors, typography, component style).
It does NOT mean keeping broken elements in the same broken position.

====================================================================
MANDATORY FIX CHECKLIST  —  VERIFY EACH BEFORE WRITING A SINGLE LINE OF HTML
====================================================================

Before you write the HTML, mentally confirm that your design addresses EVERY item
in this list. If your HTML does not contain a concrete, visible solution for a
High or Medium issue, your response is incomplete — revise it until it does.
Some fixes require structural changes (relocating, collapsing, or restructuring
UI elements). Make those structural changes — do not style over them.

HIGH PRIORITY  (must be resolved — zero exceptions):
  NOTE: 'fixing' means the issue is GONE from the redesign — not styled differently.
  If the fix says to move, collapse, or restructure something, do exactly that.

  [01] Primary sidebar uses icon-only buttons without accessible labels
       Problem:          The far-left sidebar contains approximately 12 icon-only buttons with no visible text labels or tooltips. Users must guess the function of each icon, and screen reader users may encounter unlabeled controls. This creates a significant barrier for both discoverability and assistive technology users.
       What to implement: Add aria-label or aria-labelledby attributes to every icon button with a descriptive name (e.g., 'Listening', 'Reports'). Implement tooltips on hover/focus to aid sighted users. Consider an expandable sidebar with text labels for first-time users.
       Affects:          Primary navigation sidebar (far left vertical bar)
       WCAG:             1.1.1 Level A

  [02] Low contrast text in secondary sidebar navigation
       Problem:          Light gray text on the dark gray sidebar background appears to have insufficient contrast, particularly for items like 'Listening Home', 'Twitter Search', and the trial information copy. This makes reading difficult for users with low vision or in bright ambient lighting conditions.
       What to implement: Increase text color brightness to at least #C0C0C0 or higher to meet the 4.5:1 minimum ratio against the dark background. Use a contrast checker tool to verify every text/background combination in the sidebar. Active/selected items should have even stronger contrast.
       Affects:          Secondary sidebar — navigation labels like 'Listening Home', 'Twitter Search', and trial text
       WCAG:             1.4.3 Level AA

  [03] No visible focus indicators for interactive elements
       Problem:          There are no visible keyboard focus indicators on any of the interactive elements shown. Keyboard-only users cannot determine which element is currently focused, making the interface essentially unusable without a mouse.
       What to implement: Implement a visible focus ring (e.g., 2px solid blue outline with 2px offset) on all focusable elements. Ensure the focus indicator has at least a 3:1 contrast ratio against adjacent colors per WCAG 2.4.11. Test the full interface with keyboard-only navigation.
       Affects:          All interactive elements across sidebar icons, filter dropdowns, and buttons
       WCAG:             2.4.7 Level AA

  [04] Right-side vertical icon toolbar lacks labels and has small touch targets
       Problem:          The right-edge toolbar contains a series of small icon-only buttons with no text labels, tooltips, or accessible names. The icons appear to be below the 24×24 CSS pixel minimum target size, making them difficult to activate for users with motor impairments.
       What to implement: Increase button touch targets to at least 44×44px (preferred) or 24×24px minimum with adequate spacing. Add aria-labels and visible tooltips on hover/focus. Consider grouping these tools into a labeled panel or popover to reduce visual clutter.
       Affects:          Vertical icon toolbar on the far right edge of the screen
       WCAG:             2.5.8 Level AA

MEDIUM PRIORITY  (must be resolved — no exceptions):
  NOTE: Same rule — fix means the issue does not exist in the output HTML.

  [05] Dual sidebar creates excessive cognitive load and navigation confusion
       Problem:          Two distinct sidebars stacked horizontally create navigational ambiguity. Users must understand that one is global navigation and the other is contextual, but the visual hierarchy between them is weak. This dual-panel pattern consumes significant horizontal space and increases the learning curve.
       What to implement: Consider collapsing the primary sidebar into the secondary one, using a unified navigation with icon+text labels. Alternatively, make the primary sidebar auto-collapse when a section is selected, freeing horizontal space for content. Provide clear visual differentiation between global and contextual navigation.
       Affects:          Primary (icon) sidebar + Secondary (text) sidebar combined

  [06] Filter dropdowns lack clear affordance and state indication
       Problem:          The filter controls appear as simple text labels with small down-arrows rather than clearly styled dropdown buttons. The current filter state ('Viewing All Sources', '0 Applied') is in a secondary visual weight, making it easy to miss whether filters are active. Users may not realize they can interact with these elements.
       What to implement: Style filter controls as clearly bordered dropdown buttons with a distinct interactive appearance (e.g., visible borders, hover states). When filters are active, use a badge or color highlight to indicate applied filters. Increase the click target area for each filter control.
       Affects:          Filter bar — Sources, Keyword Filters, Sentiment, More Filters dropdowns

  [07] Trial subscription CTA buried in low-visibility sidebar location
       Problem:          The trial information and subscription CTA are placed at the bottom of the secondary sidebar, an area of low visual priority. The '30 days remaining' message and blue 'Start my subscription' button compete poorly against the main content area for attention, potentially causing users to miss important account status information.
       What to implement: Move trial status to a top-of-page banner or a persistent notification bar within the main content area. Use progressive urgency cues (color changes) as the trial period decreases. Consider a modal or in-content prompt at key engagement moments rather than a static sidebar element.
       Affects:          Secondary sidebar — 'Start my subscription' button near bottom

  [08] Purple sparkle icons used decoratively without clear meaning
       Problem:          Purple sparkle icons appear next to multiple navigation items and buttons with no explanation of their meaning. Users cannot determine whether they indicate AI-powered features, new features, premium features, or something else. The inconsistent context of their placement increases confusion.
       What to implement: Add a tooltip or label explaining what the sparkle icon means (e.g., 'AI-powered' or 'New feature'). Use the icon consistently for one purpose only. On first encounter, consider an onboarding callout that explains the icon's significance.
       Affects:          Multiple locations — next to 'Listening Home', 'New Topic', 'Espresso', and 'Track your own Topics' button

LOW PRIORITY  (implement where possible):
  [09] 'Preview' badge lacks context and actionability — Add a tooltip or inline help text explaining the preview state (e.g., 'This is sample data. Create your own topic to see real results.'). Include a clear CTA to transition from preview to active state. Use a distinct color like amber to differentiate preview from production data.
  [10] Edit button (blue circle with pencil icon) lacks label and clear purpose — Add an aria-label ('Edit topic settings') and a visible tooltip. Position the button closer to the content it modifies (e.g., next to the topic title). Consider replacing with a text+icon button like 'Edit Topic' for better discoverability.

====================================================================
STEP 1 — LAYOUT & COMPOSITION  (reason through this BEFORE anything else)
====================================================================

Layout and content distribution are non-negotiable design decisions.
Do not assume the original layout is correct — reason from first principles.
Ask yourself: does each element sit in the right zone for its purpose?
If not, move it. The composition must serve the user's primary goal.

1. VISUAL HIERARCHY
   The most important action or information must be visually dominant.
   Use size, weight, contrast, and position to establish a clear reading order.
   Secondary content must be visually subordinate — smaller, lighter, lower.
   (Malewicz — Designing User Interfaces: contrast creates hierarchy)
   (Refactoring UI: use size and weight to communicate importance, not just color)

2. F-PATTERN & READING FLOW
   Users scan left-to-right, top-to-bottom in an F-shape on content-heavy screens.
   Place primary actions and key labels at the start of scan lines.
   Never interrupt the scan path with secondary, promotional, or personalisation content.
   (Nielsen Norman Group — F-Pattern reading behavior)

3. PROXIMITY & GROUPING (Gestalt)
   Related elements must be spatially close. Unrelated elements need clear separation.
   If two elements answer different user questions, they belong in different zones.
   Never mix contextual content with navigational content in the same region.
   (Psych 101 — Gestalt: Law of Proximity, Law of Similarity)

4. CONTENT PRIORITISATION
   Identify the user's single primary goal on this screen.
   Every element that does not serve that goal directly must be secondary in
   visual weight and position. Upsell, personalisation, and promotional content
   must never compete with or interrupt the primary task flow.
   (Don't Make Me Think — Krug: do not make users think about what to do next)
   (Psychology of Design — Hick's Law: fewer competing choices = faster decisions)

5. SPATIAL ZONES
   Divide the screen into logical zones: navigation, primary content, secondary
   context, actions. Each zone has exactly one job.
   If an element is in the wrong zone, move it to the correct zone or defer it
   to a modal or drawer. The layout must match the user's mental model of the task.
   (Universal Principles of Design — Progressive Disclosure)

6. SIGNAL-TO-NOISE
   Every element must earn its place. If an element does not directly help the
   user complete their current task, reduce its visual weight or remove it from
   the primary viewport. Clutter is a UX failure, not a neutral default.
   (Universal Principles of Design — Signal-to-Noise Ratio)

COMPOSITION RULE: If the original layout violates these principles — with or
without an explicit audit finding — correct it. Do not replicate bad composition
because it existed in the screenshot. A version 2 must be compositionally sound.

APPLY THESE COMPOSITION DECISIONS TO RESOLVE THE FOLLOWING AUDIT ISSUES:
Every High and Medium issue in the MANDATORY FIX CHECKLIST above must map to a
specific, visible structural decision in your layout. Before moving to Step 2,
trace each issue to a layout zone, spacing decision, or hierarchy change.
If the fix requires restructuring — restructure. If it requires removing an element
from the primary viewport — remove it. Composition fixes are not optional.

====================================================================
STEP 2 — VISUAL IDENTITY  (preserve brand, not broken placement)
====================================================================

Once composition is decided, preserve these brand elements exactly:
  - Colors: every background, text, brand, accent, and surface hex value
  - Typography: font families, sizes, weights, line-heights, letter-spacing
  - Spacing scale: the relative rhythm of padding/margin (not the positions)
  - Shapes: border-radius, border styles, shadows, elevation
  - Icons: style, weight, and size (placement may change if composition requires)
  - Components: card designs, button styles, badge styles, input styles

NOTE: Layout and element positioning are governed by Step 1, not this list.
Preserving the brand does NOT mean preserving where broken things sit.
The output must feel like a version 2 — same brand, better UX.

====================================================================
FULL AUDIT REPORT  —  Listening - Espresso Topic Dashboard
====================================================================
Overall Score:        6.8/10  —  Good
Accessibility Score:  4.8/10  (WCAG 2.2 AA)
Issues Found:         10  (4 High · 4 Medium · 2 Low)

EXECUTIVE SUMMARY
The Listening dashboard provides a reasonable data visualization layout with clear topic context, but suffers from significant accessibility concerns including icon-only navigation without labels, low contrast text in the sidebar, and reliance on color alone to convey information. The dual-sidebar navigation adds cognitive overhead and the trial CTA competes for attention in a secondary location.

ALL ISSUES  (full detail for reference):

[01] HIGH  —  Primary sidebar uses icon-only buttons without accessible labels
     Location:  Primary navigation sidebar (far left vertical bar)
     Problem:   The far-left sidebar contains approximately 12 icon-only buttons with no visible text labels or tooltips. Users must guess the function of each icon, and screen reader users may encounter unlabeled controls. This creates a significant barrier for both discoverability and assistive technology users.
     Fix:       Add aria-label or aria-labelledby attributes to every icon button with a descriptive name (e.g., 'Listening', 'Reports'). Implement tooltips on hover/focus to aid sighted users. Consider an expandable sidebar with text labels for first-time users.
     Source:    WCAG 2.2 — 1.1.1 Non-text Content (A): all non-text content must have a text alternative
     WCAG:      1.1.1  Level A

[02] HIGH  —  Low contrast text in secondary sidebar navigation
     Location:  Secondary sidebar — navigation labels like 'Listening Home', 'Twitter Search', and trial text
     Problem:   Light gray text on the dark gray sidebar background appears to have insufficient contrast, particularly for items like 'Listening Home', 'Twitter Search', and the trial information copy. This makes reading difficult for users with low vision or in bright ambient lighting conditions.
     Fix:       Increase text color brightness to at least #C0C0C0 or higher to meet the 4.5:1 minimum ratio against the dark background. Use a contrast checker tool to verify every text/background combination in the sidebar. Active/selected items should have even stronger contrast.
     Source:    WCAG 2.2 — 1.4.3 Contrast (Minimum) (AA): text must have at least 4.5:1 contrast ratio against background
     WCAG:      1.4.3  Level AA

[03] HIGH  —  No visible focus indicators for interactive elements
     Location:  All interactive elements across sidebar icons, filter dropdowns, and buttons
     Problem:   There are no visible keyboard focus indicators on any of the interactive elements shown. Keyboard-only users cannot determine which element is currently focused, making the interface essentially unusable without a mouse.
     Fix:       Implement a visible focus ring (e.g., 2px solid blue outline with 2px offset) on all focusable elements. Ensure the focus indicator has at least a 3:1 contrast ratio against adjacent colors per WCAG 2.4.11. Test the full interface with keyboard-only navigation.
     Source:    WCAG 2.2 — 2.4.7 Focus Visible (AA): keyboard focus indicator must be visible
     WCAG:      2.4.7  Level AA

[04] HIGH  —  Right-side vertical icon toolbar lacks labels and has small touch targets
     Location:  Vertical icon toolbar on the far right edge of the screen
     Problem:   The right-edge toolbar contains a series of small icon-only buttons with no text labels, tooltips, or accessible names. The icons appear to be below the 24×24 CSS pixel minimum target size, making them difficult to activate for users with motor impairments.
     Fix:       Increase button touch targets to at least 44×44px (preferred) or 24×24px minimum with adequate spacing. Add aria-labels and visible tooltips on hover/focus. Consider grouping these tools into a labeled panel or popover to reduce visual clutter.
     Source:    WCAG 2.2 — 2.5.8 Target Size (Minimum) (AA): interactive targets must be at least 24x24 CSS pixels
     WCAG:      2.5.8  Level AA

[05] MEDIUM  —  Dual sidebar creates excessive cognitive load and navigation confusion
     Location:  Primary (icon) sidebar + Secondary (text) sidebar combined
     Problem:   Two distinct sidebars stacked horizontally create navigational ambiguity. Users must understand that one is global navigation and the other is contextual, but the visual hierarchy between them is weak. This dual-panel pattern consumes significant horizontal space and increases the learning curve.
     Fix:       Consider collapsing the primary sidebar into the secondary one, using a unified navigation with icon+text labels. Alternatively, make the primary sidebar auto-collapse when a section is selected, freeing horizontal space for content. Provide clear visual differentiation between global and contextual navigation.
     Source:    Psychology of Design — Hick's Law: too many choices slows decisions

[06] MEDIUM  —  Filter dropdowns lack clear affordance and state indication
     Location:  Filter bar — Sources, Keyword Filters, Sentiment, More Filters dropdowns
     Problem:   The filter controls appear as simple text labels with small down-arrows rather than clearly styled dropdown buttons. The current filter state ('Viewing All Sources', '0 Applied') is in a secondary visual weight, making it easy to miss whether filters are active. Users may not realize they can interact with these elements.
     Fix:       Style filter controls as clearly bordered dropdown buttons with a distinct interactive appearance (e.g., visible borders, hover states). When filters are active, use a badge or color highlight to indicate applied filters. Increase the click target area for each filter control.
     Source:    The Design of Everyday Things (Norman) — Affordance: element must communicate how it is used

[07] MEDIUM  —  Trial subscription CTA buried in low-visibility sidebar location
     Location:  Secondary sidebar — 'Start my subscription' button near bottom
     Problem:   The trial information and subscription CTA are placed at the bottom of the secondary sidebar, an area of low visual priority. The '30 days remaining' message and blue 'Start my subscription' button compete poorly against the main content area for attention, potentially causing users to miss important account status information.
     Fix:       Move trial status to a top-of-page banner or a persistent notification bar within the main content area. Use progressive urgency cues (color changes) as the trial period decreases. Consider a modal or in-content prompt at key engagement moments rather than a static sidebar element.
     Source:    Universal Principles of Design — Visibility: system status and available actions must be visible

[08] MEDIUM  —  Purple sparkle icons used decoratively without clear meaning
     Location:  Multiple locations — next to 'Listening Home', 'New Topic', 'Espresso', and 'Track your own Topics' button
     Problem:   Purple sparkle icons appear next to multiple navigation items and buttons with no explanation of their meaning. Users cannot determine whether they indicate AI-powered features, new features, premium features, or something else. The inconsistent context of their placement increases confusion.
     Fix:       Add a tooltip or label explaining what the sparkle icon means (e.g., 'AI-powered' or 'New feature'). Use the icon consistently for one purpose only. On first encounter, consider an onboarding callout that explains the icon's significance.
     Source:    Universal Principles of Design — Signal-to-Noise Ratio: maximise relevant information, minimise clutter

[09] LOW  —  'Preview' badge lacks context and actionability
     Location:  Main content header — 'Preview' badge next to 'Espresso' heading
     Problem:   A small 'Preview' badge appears next to the topic title with no explanation of what 'Preview' means in this context — whether it indicates sample data, a limited feature, or a draft state. Users cannot determine the implications or how to move beyond the preview state.
     Fix:       Add a tooltip or inline help text explaining the preview state (e.g., 'This is sample data. Create your own topic to see real results.'). Include a clear CTA to transition from preview to active state. Use a distinct color like amber to differentiate preview from production data.
     Source:    The Design of Everyday Things (Norman) — Gulf of Evaluation: user cannot tell if their action worked

[10] LOW  —  Edit button (blue circle with pencil icon) lacks label and clear purpose
     Location:  Top right of main content area — blue circular button with pencil/edit icon
     Problem:   A floating blue circle with a white pencil icon appears at the top right with no label or context. It is unclear what this button edits — the topic configuration, the dashboard layout, or something else. Its isolated position away from related content weakens the association.
     Fix:       Add an aria-label ('Edit topic settings') and a visible tooltip. Position the button closer to the content it modifies (e.g., next to the topic title). Consider replacing with a text+icon button like 'Edit Topic' for better discoverability.
     Source:    Psych 101 (Kleinman) — Gestalt: Law of Proximity

====================================================================
SELF-CHECK — RUN THIS BEFORE OUTPUTTING THE HTML
====================================================================

For each item below, verify the SPECIFIC FIX is concretely implemented —
not just acknowledged, not just styled differently. The fix must be visible.

  [ ] [01] Primary sidebar uses icon-only buttons without accessible labels
       Required fix: Add aria-label or aria-labelledby attributes to every icon button with a descriptive name (e.g., 'Listening', 'Reports'). Implement tooltips on hover/focus to aid sighted users. Consider an expandable sidebar with text labels for first-time users.
       Location:     Primary navigation sidebar (far left vertical bar)
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [02] Low contrast text in secondary sidebar navigation
       Required fix: Increase text color brightness to at least #C0C0C0 or higher to meet the 4.5:1 minimum ratio against the dark background. Use a contrast checker tool to verify every text/background combination in the sidebar. Active/selected items should have even stronger contrast.
       Location:     Secondary sidebar — navigation labels like 'Listening Home', 'Twitter Search', and trial text
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [03] No visible focus indicators for interactive elements
       Required fix: Implement a visible focus ring (e.g., 2px solid blue outline with 2px offset) on all focusable elements. Ensure the focus indicator has at least a 3:1 contrast ratio against adjacent colors per WCAG 2.4.11. Test the full interface with keyboard-only navigation.
       Location:     All interactive elements across sidebar icons, filter dropdowns, and buttons
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [04] Right-side vertical icon toolbar lacks labels and has small touch targets
       Required fix: Increase button touch targets to at least 44×44px (preferred) or 24×24px minimum with adequate spacing. Add aria-labels and visible tooltips on hover/focus. Consider grouping these tools into a labeled panel or popover to reduce visual clutter.
       Location:     Vertical icon toolbar on the far right edge of the screen
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [05] Dual sidebar creates excessive cognitive load and navigation confusion
       Required fix: Consider collapsing the primary sidebar into the secondary one, using a unified navigation with icon+text labels. Alternatively, make the primary sidebar auto-collapse when a section is selected, freeing horizontal space for content. Provide clear visual differentiation between global and contextual navigation.
       Location:     Primary (icon) sidebar + Secondary (text) sidebar combined
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [06] Filter dropdowns lack clear affordance and state indication
       Required fix: Style filter controls as clearly bordered dropdown buttons with a distinct interactive appearance (e.g., visible borders, hover states). When filters are active, use a badge or color highlight to indicate applied filters. Increase the click target area for each filter control.
       Location:     Filter bar — Sources, Keyword Filters, Sentiment, More Filters dropdowns
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [07] Trial subscription CTA buried in low-visibility sidebar location
       Required fix: Move trial status to a top-of-page banner or a persistent notification bar within the main content area. Use progressive urgency cues (color changes) as the trial period decreases. Consider a modal or in-content prompt at key engagement moments rather than a static sidebar element.
       Location:     Secondary sidebar — 'Start my subscription' button near bottom
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

  [ ] [08] Purple sparkle icons used decoratively without clear meaning
       Required fix: Add a tooltip or label explaining what the sparkle icon means (e.g., 'AI-powered' or 'New feature'). Use the icon consistently for one purpose only. On first encounter, consider an onboarding callout that explains the icon's significance.
       Location:     Multiple locations — next to 'Listening Home', 'New Topic', 'Espresso', and 'Track your own Topics' button
       Is this fix structurally present and visible in the HTML? (yes/no — if no, revise)

If any checkbox above cannot be ticked YES, revise your HTML before outputting it.

====================================================================
OUTPUT REQUIREMENTS
====================================================================

- Output a single, complete, self-contained HTML file.
- All CSS must be in a <style> block in the <head> — no external stylesheets.
- No CDN links, no JavaScript frameworks, no external images.
  Use inline SVG for icons. Use data URIs only if absolutely necessary.
- Preserve all informational content (text, labels, data). Do not silently drop
  information the user needs. However: if a fix requires relocating, collapsing,
  or restructuring a UI element, you MUST do so — placing it in a better position
  is not removing it. The audit fix always wins over original placement.
- Do NOT add new features, sections, or content not present in the original.
- JavaScript is allowed only for interactions clearly present in the original.
  Keep it minimal and inline.
- The file must open in a browser as a fully working static page.
- Start your response with <!DOCTYPE html> and end the HTML with </html>.
- Do NOT reference Mobbin or any third-party design gallery. Use Saasfactor if needed.